# Records index — reviews, research, trial logs (FW-27)

*A discovery surface over `reviews/`, `research/`, and the `fixtures/`
trial logs: one row per record (per major dated section for the trial
logs), with the decision/correction/finding each one pinned. Maintained
manually at round-close as part of the records step — a table row per
round is near-zero marginal cost (per `forward/process-and-horizon.md`
§2 / FW-27).*

## Reviews

Blind-review and gate records (two-gate discipline: spec/build → blind
review → fold → delta). Newest first.

| Date | File | Subject | Outcome / verdict | What it pinned |
|---|---|---|---|---|
| 2026-07-18 | [forward-work-map](reviews/2026-07-18-forward-work-map.md) | Forward work map (doc 14 + `forward/*`) | NOT SOUND → SOUND (delta) | Struck an F1 session-opener ledger fetch that silently weakened D3 and reframed the G-6 staleness scope-narrowing as a spec-time proposal (the row wins until then), ratifying FW-1…FW-29 as the gated planning baseline. |
| 2026-07-18 | [ux-round-1](reviews/2026-07-18-ux-round-1.md) | UX round 1 — survey + U16/U17/U18 (first Opus-reviews / Sonnet-builds round) | all three units CLEAN/SOUND, shipped | The miner brief must be composed BEFORE the security scan (U18 F1), the budget indicator's "near cap" clause must gate on actual proximity rather than render unconditionally on an empty surface (U17), and `reference` is excluded from surface-fill as the cap-free overflow sink. |
| 2026-07-18 | [ui-feedback-r3](reviews/2026-07-18-ui-feedback-r3.md) | U14 registration (Y-16/Y-17) + U15 rehome (Y-18), agent-built | both spec+code gates CLEAN/SOUND, shipped | `--init` git-init runs only when the arm banner disclosed it AND confirm-time re-derivation still holds, "registration did not complete" is the only copy true in every failure leg, and rehome re-checks the bucket at arm AND confirm. |
| 2026-07-18 | [ui-feedback-r2](reviews/2026-07-18-ui-feedback-r2.md) | Three parallel agent-built fixes (scope-filtered destinations, monitor placement, Y-15 non-blocking pane) | all three two-gated CLEAN, shipped | The `o` destination cycle never advanced in a real browser (handler read a `current` field no template sent — the field-mismatch class now fails loudly via a rendered-form test), and non-blocking-pane completion must ride a `pane_result`-triggered panel re-fetch because app.js ignores `pane_result`. |
| 2026-07-18 | [interrupt-ladder](reviews/2026-07-18-interrupt-ladder.md) | Esc-interrupt ladder tuning (09 §4.2: grace 2→1 s, kill 5→2.5 s) | NOT CLEAN → CLEAN | A raw asyncio cancel pierces the SDK transport's shielded SIGTERM/SIGKILL escalation, so teardown must shield-and-abandon (caller bounded, kill completes in the background) under one Esc-anchored ≤5 s deadline. |
| 2026-07-18 | [idle-lifecycle-build](reviews/2026-07-18-idle-lifecycle-build.md) | U13 idle-lifecycle build (Y-14) | both blind gates CLEAN; live trial caught a production BLOCKER | SIGTERM-to-self exits 143 because uvicorn re-raises the captured signal (→ `Restart=on-failure` restarts it), so exit must set `server.should_exit`; signal/systemd semantics are live-trial-only facts. |
| 2026-07-18 | [idle-lifecycle-spec](reviews/2026-07-18-idle-lifecycle-spec.md) | Y-14 idle-lifecycle spec amendment | NOT SOUND → SOUND | The exit predicate must block only on INTERRUPTIBLE states (not `awaiting-input`, where every parked turn sits) and tear a parked session down THEN defer to a later sample, the clock stamps at request completion not entry, and self-exit arms only under systemd. |
| 2026-07-17 | [armed-host-add](reviews/2026-07-17-armed-host-add.md) | Armed host-add UI action (feedback r1 item 5, Y-11) | spec AMENDMENT SOUND + code CLEAN | An armed registration must use the SERVER-derived bucket path never a client value, the consent render must carry the canon-write consequence in Y-9 words, and `scope != project` is refused first. |
| 2026-07-17 | [chat-panes-build](reviews/2026-07-17-chat-panes-build.md) | U12 chat-panes build (Y-13, CODE gate) | NOT CLEAN (2 MAJOR) → CLEAN | Proposal intake must check record STATUS not existence (resolved records are locatable), the bar leads with the Y-9 human line not the `lrn-` id, and a per-proposal server nonce is required on all four arm/disarm/dismiss/confirm routes. |
| 2026-07-17 | [chat-panes-spec](reviews/2026-07-17-chat-panes-spec.md) | Y-13 chat-panes spec amendment (agentic verb proposals) | NEEDS REWORK → SOUND | Proposals render WAITING never armed (the agent gets exactly the human's own two-keystroke consent path) via a server-held single proposal slot with an exhaustive clear-set — closing the proposal-riding-a-human-keystroke race by construction. |
| 2026-07-17 | [keymap-wasd](reviews/2026-07-17-keymap-wasd.md) | Gaming-centric WASD keymap remap | NOT CLEAN (doc drift only) → CLEAN | The remap is functionally correct (22 globally-unique keys; armed-state disarm intercept fires before keymap lookup); the only findings were stale j/k/l teaching in app.js comments and the 09/10 keymap descriptions. |
| 2026-07-17 | [ui-feedback-r1](reviews/2026-07-17-ui-feedback-r1.md) | Feedback round 1, batches A+B (items 1/2/3/4/6/8) | NOT CLEAN (no MAJOR) → CLEAN | install.sh's `mkdir` must sit inside the `run()` guard (`--dry-run` created dirs), and the Deferred count keys by bucket NAME only (documented limitation; the real fix needs scope on `list --json` items). |
| 2026-07-17 | [ui-polish](reviews/2026-07-17-ui-polish.md) | First dedicated visual/UX pass | NOT CLEAN (1 MAJOR) → CLEAN | The new sticky header occluded k-selected rows (needs `scroll-margin-top`), and the theme-CSS line filter dropped github-dark's whole `.highlight` rule and with it the dark theme's default foreground color. |
| 2026-07-16 | [session-post-mortem](reviews/2026-07-16-session-post-mortem.md) | Neutral transcript post-mortem of the 25-day / 7-compaction build session | factual record (§1–5) + author's non-binding reading (§6–7) | The whole arc from the transcript alone — the user-caught SDK-auth falsification, the twice-reversed TUI, the H-5 preference-dressed-as-a-consequence built and audited before the user ever saw it, and green suites blind to their blockers "because fixtures pre-satisfy the conditions the pins forbid"; 754 tests verified, the miner never yet run. |
| 2026-07-12 | [surface-rederivation-gates](reviews/2026-07-12-surface-rederivation-gates.md) | Post-correction /goal re-derivation of the G-3 surface (09/10 web) | TERMINAL VERDICT PASS (2026-07-13) | Localhost web app + Agent SDK pane engine on the user's four binding answers, on an honestly-priced option space; the framing lens caught render-path XSS as a map mispricing, and 7-of-8 remediation batches minted new findings caught only by independent re-check. |
| 2026-07-12 | [tui-phased-gates](reviews/2026-07-12-tui-phased-gates.md) | The superseded TUI 09/10 authoring gates + post-gate correction | TERMINAL PASS then platform/engine REOPENED | The terminal verdict's ground (SDK API-key-only economics) was FALSE — three reviewers cited the doc without testing it — reopening the platform/framework/engine decisions and originating README ground rule 4 (sourced≠true, frame narrowing, true≠right-for-us). |
| 2026-07-12 | [phased-gate-process](reviews/2026-07-12-phased-gate-process.md) | M1/M2/M3 phased implementability gates | all three PASS + terminal full-plan PASS | A mid-tier literal-minded agent can build the full plan from the docs alone, and the recurring discipline that remediations mint new blockers (M2-4 → M2-21: models can't hash, so the CLI must stamp `record_sha` at validation). |
| 2026-07-12 | [implementability-review](reviews/2026-07-12-implementability-review.md) | Gaps that would mislead literal-minded builder sub-agents | six blockers + risks folded; near-executable | The six inventable-interface blockers (packaging, sentinel contract, markerless-target bootstrap, `route` input, already-canon criterion, S-14/M3 version mapping) and the judgment calls that cannot be spec'd away (routed to 08 §4). |
| 2026-07-12 | [fixture-review](reviews/2026-07-12-fixture-review.md) | Independent assessment of the three acceptance-fixture candidates | A qualified-hook-only, B strongest, C REPLACE | The sourcing rule was self-defeating (picking lessons from canon selects for baselines that already pass), and baselines must be demonstrated not assumed (≥3 fresh-session provocations, ≥2 failures, binary predicate written first). |
| 2026-07-12 | [blind-adjudication](reviews/2026-07-12-blind-adjudication.md) | Blind review of the refinement pass's material items | ADOPT + build M1 after 4 fixes | Freeze-at-routing adopted (pending is inert, git versions drafts), review must self-push (the sync branch never pushes), correction = supersede+recompile not `git revert`, and worker merges become proposals so the worker is fully append-only (designated host + claim marker deleted). |
| 2026-07-11 | [refinement-review](reviews/2026-07-11-refinement-review.md) | Enhance/refine the gen-2 corpus without changing its goals | draft edits (A/B/C); two items flagged for blind review | The `evidence`-immutability contradiction, the live-verified AskUserQuestion 2–4-option limit forcing the four-action review card, and freeze-substance-at-routing (C7, reopening S-8/S-12 for blind review). |

## Research

Empirical probes, trade studies, and external-evidence memos (shareable
with blind reviewers). Newest first.

| Date | File | Subject | Outcome / verdict | What it pinned |
|---|---|---|---|---|
| 2026-07-18 | [ux-enhancement-survey](research/2026-07-18-ux-enhancement-survey.md) | UX-enhancement survey (automation / preload / routing-time info / miner brief) | analysis-only; ranked top-5 shortlist | The UI is a pure reader of the proposal drafter (no worker-kick affordance exists), the residual friction to remove is navigation/staleness/waiting and never the decision gate, and a benefit÷cost shortlist (prefetch, worker Force-run, auto-focus, budget indicator, miner brief) with explicit non-recommendations. |
| 2026-07-18 | [sdk-bundle-exclusion](research/2026-07-18-sdk-bundle-exclusion.md) | Can the packaged UI binary exclude the SDK's vendored node bundle | YES — verified in source AND empirically | `_find_cli()` checks `_bundled/` then falls back to PATH `claude`, so excluding `_bundled/**` drops ~305 MB → ~50 MB with a hard, preflight-checkable "claude on PATH" runtime requirement. |
| 2026-07-17 | [go-port-fleet-sketch](research/2026-07-17-go-port-fleet-sketch.md) | Sizing sketch for a full Go rewrite ("seatbelt YOLO") | SKETCH, not a plan of record | Three non-negotiable seatbelts (sandbox-only until parity, canonicalization goldens first, Python stays sole canon writer until compile-diff is empty), an ~5–8 M-token / 2–4-day fleet shape, and ~70–80% odds of a trustworthy CLI with the seatbelts. |
| 2026-07-12 | [adjudication-surface-problem-space](research/2026-07-12-adjudication-surface-problem-space.md) | Problem-space map that reopened 09/10's platform decision | shareable map; four binding user answers | The UI-agnostic invariant substrate + nine circumstance facts + an honestly-priced option space (TUI / web / hybrids / do-less), routing V1–V4 to the user before any freeze → localhost web app + SDK engine. |
| 2026-07-12 | [sdk-auth-empirical-test](research/2026-07-12-sdk-auth-empirical-test.md) | Empirical test of the "SDK is API-key-only" claim | claim FALSE (policy-only) | The Python SDK ran on Max subscription OAuth with every API-key env var unset (proven twice), voiding the auth/economics leg of the engine decision; plus the measured 68k-token cache write from default `~/.claude` loading. |
| 2026-07-12 | [sdk-pane-probes](research/2026-07-12-sdk-pane-probes.md) | Three empirical SDK probes for the pane design | all three VERIFIED | Partial streaming is chunk-level ~5 Hz (usable for live render), `can_use_tool` gives exact-file allow/deny but only via `ClaudeSDKClient` with narrow `allowed_tools` (three footguns), and `setting_sources=[]` is mandatory (unset loads the full ~30k-token user env and neuters the permission gate). |
| 2026-07-12 | [agent-sdk-verification](research/2026-07-12-agent-sdk-verification.md) | Live-doc capability report on the Claude Agent SDK | docs-only survey, later partially falsified | The SDK capability surface (sessions, caching, `canUseTool` permission flow, streaming shapes, packaging) with the auth note carrying a dated CORRECTION and three orchestrator verify-at-build flags (path-scoped allow rules, partial streaming, cross-process cache). |
| 2026-07-12 | [tui-vs-web-dx-study](research/2026-07-12-tui-vs-web-dx-study.md) | User-commissioned Textual-vs-localhost-web developer-experience study | web wins, and not narrowly | Web dominates on styling, agent-leverage (training-data depth + the Playwright see-judge-fix loop), maintainability, deep-link and lifecycle (two TUI subsystems vanish); Textual keeps only total keyboard ownership and zero network surface. |
| 2026-07-12 | [tui-framework-trade-study](research/2026-07-12-tui-framework-trade-study.md) | TUI framework trade study (Textual/Ink/OpenTUI/Bubble Tea/Ratatui) | Textual dominant, "not genuinely close" | Textual wins six of nine axes with `MarkdownStream` + Pygments DiffLexer shipped first-party; mitigate its bus-factor-1 and v4→v8 churn by pinning; the one human values call is 5-year longevity vs build cost (a view-layer-only switch later). |
| 2026-07-12 | [tui-environment-grounding](research/2026-07-12-tui-environment-grounding.md) | Live-host facts for the G-3 design | measured facts of record | Ghostty not kitty, swaync (`notify-send -A` implies `--wait` and blocks the sender), and every pane-critical `claude` 2.1.207 flag verified present (stream-json, `--include-partial-messages`, `--fallback-model`, `--setting-sources`). |
| 2026-07-12 | [sota-survey](research/2026-07-12-sota-survey.md) | State-of-the-art survey of agent self-improvement tooling | external evidence, shareable | The field splits high-volume-machine-scored vs low-volume-human-judged with nothing bridging, automatic consolidation is the failure point while curation gates decide harm-vs-help, and SkillOpt-Sleep is the G-1 reference implementation to evaluate before building anything bespoke. |

## Fixtures / trial logs

Live-execution and baseline-qualification records; one row per major
dated section. Newest first.

| Date | File | Subject | Outcome / verdict | What it pinned |
|---|---|---|---|---|
| 2026-07-18 | [ui-trials.md](fixtures/ui-trials.md) — UX-round DoD (U16+U17+U18) | Queue-walk trio + budget indicator + miner brief | all PASS (sandbox) | Auto-select + keyboard-live on load, worker Force-run detached (Y-14 posture intact), the bare 0/10 budget line with no false-nearness clause (the F1 case), and the collapsed "Episode brief (b)" disclosure; the live miner-run leg rides the next real cycle. |
| 2026-07-18 | [ui-trials.md](fixtures/ui-trials.md) — Round-3 DoD (U14+U15) | Registration + rehome in a full sandbox | all three PASS | git-init-on-register with the disclosure banner + pinned init commit, the failed-registration error surviving the exact front-scope push that used to wipe it, and a pane-proposed rehome landing the pinned ledger commit with the ancestor-project doctrine visibly reasoning (mtime-driven recompile, zero code changes). |
| 2026-07-18 | [ui-trials.md](fixtures/ui-trials.md) — Y-15 re-trials | Non-blocking pane start | (i)(iii) PASS; (ii) pass-by-composition | Instant split with the starting line already cleared by the first streamed frame and completion swap landing the result footer; the third forcing lever surfaced that an unreadable record 500s the Detail page (new backlog degradation gap). |
| 2026-07-18 | [ui-trials.md](fixtures/ui-trials.md) — T-E addendum | Tuned interrupt-ladder retrial | PASS | Interrupt→Result teardown dropped from ~5.3 s to 2.90 s, inside the amendment's 5 s arithmetic ceiling. |
| 2026-07-18 | [ui-trials.md](fixtures/ui-trials.md) — U13 DoD | Idle-lifecycle live trials (overnight autonomous run) | (a) FAILED→PASS, (b)(c) PASS, (d) unhittable | The SIGTERM-143 restart-loop BLOCKER that both blind reviews passed, and the walk-away teardown-then-defer choreography visible in ui.log (parked session torn down at sample 1, clean exit at sample 2). |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — Degradation walk | 09 §5 row-by-row (U9 ledger) | SATISFIED (CI + live) | The material degradation rows confirmed live (SSE, launcher/notify fallbacks, verb-nonzero, scan-blocked); the remainder are OS-level documented degradations needing no live repro. |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — U12 DoD | Live bucket-pane proposal, full keyboard consent (browser) | PASS end-to-end | p → real bucket session → propose_verb → SSE WAITING bar → y → Enter → real `defer`, with the bucket session surviving the confirm (the §4.5 exemption, live). |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — T-B rows (6)–(8) | Y-13 verb proposals (live SDK, real model) | (6) PASS, (7) pass-by-composition, (8) PASS | `propose_verb` routes through the charter callback and only occupies WAITING (footgun-B answered on SDK 0.2.121); the dict-of-types tool schema marked every param required and refused valid routes (fixed to real JSON Schema + empty-string normalization). |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — Browser-level acceptance (X-5) | Real-browser Playwright walk | PASS, two findings fixed | token→cookie→clean-URL, SSE partial swap, and arm→confirm→effect in a real browser; htmx's boot-time inline `<style>` violated the pinned CSP (fixed via `htmx-config` meta). |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — T-D Deep-link chain | notify → click → dedicated window (user present) | PASS, one env finding fixed | An `--app` window from an already-running chromium derives its app_id from the URL and ignores `--class`, so focus must match by page-title/address and never gate on `focuswindow`'s exit-0-on-no-match. |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — T-C End-to-end adjudication | Real HTTP route through the ledger→host chain (no model) | PASS | The two-phase ledger→host compile, proposal `git rm`, sentinel release, and soft push-failure (commit kept) — with the backlog note that a web user never sees the CLI's unpushed warning. |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — T-E Stream + interrupt smoke | Live streaming + interrupt (real model) | PASS (streaming); interrupt within 5 s bound + UX note | The SDK fast-interrupt is ineffective on the subscription-auth stream — Esc rides the ~5 s kill backstop — the finding that later drove the ladder tuning. |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — T-B Pane permission live refusal | Live SDK charter refusal, 5 actions | PASS 5/5 after a BLOCKER | The charter double-prefixed `lrn-lrn-<hex>` and denied the agent every edit of its own record (the unit suite missed it with bare ids); the Y-2 narrowed-scope non-canon-read refusal was live-confirmed. |
| 2026-07-17 | [ui-trials.md](fixtures/ui-trials.md) — T-A Headless suite | CI acceptance suite | PASS (478) | Every 09 §2/§3/§11 behavior has an inversion-failing test — the T-A predicate floor for every later gate. |
| 2026-07-14 | [trials.md](fixtures/trials.md) — Phase 0 round 3 (B2/B3) | uv-monorepo pytest (B2) + notify-send action-button hang under swaync (B3) | B2 killed at gate 0; **B3 QUALIFIES** (3/3 FAIL → 3/3 PASS post-route) | B3 (lrn-c9044f8c) is the first fully-qualified, fully-proven acceptance fixture — unbounded `notify-send -A` hangs forever on swaync — and teach instructions must front-load the operative content because the compiler cuts at sentence one. |
| 2026-07-13 | [trials.md](fixtures/trials.md) — Phase 0 round 2 (replacement probes) | C-backup (registry-write-batching) + B-replacement B1 (hyprctl focus-vs-launch) | both DOES NOT QUALIFY | The hardened gate — absence proof over the predicate BEHAVIOR on every surface loaded during the trial — kills both: the SKILL.md body already teaches live-API-verify, and the baseline natively queries `hyprctl clients` rather than branching on `dispatch focuswindow`'s exit status. |
| 2026-07-13 | [trials.md](fixtures/trials.md) — Phase 0 (baseline qualification) | Fixture B (silent substitution) + Fixture C (data.host reload), 3 runs each | both DOES NOT QUALIFY (baseline passed 3/3) | The candidate predicates measure behavior the frontier baseline already does (grep-before-bulk-edit; SKILL.md-carried stop→edit→start), so there is no delta arm — a fixture must isolate the fact actually under test. |
| 2026-07-16 | [drain-runbook.md](fixtures/m3-worklist/drain-runbook.md) | User-run drain of the three open hook follow-ups | ready-to-run material (M3 build was read-only on the real ledger) | The per-record hook-route flow (capture → proposal → validate → route; never one-motion) for the chezmoi-cd / sudo-npm-global / uv-venv-copy guards, with the supersede-escalation that drops the old advisory line in the same motion. |
