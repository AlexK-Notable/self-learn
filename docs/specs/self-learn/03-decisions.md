# 03 — Decision register

*Three states. **SETTLED** = decided with rationale; reopens automatically if
its stated inputs change (P10). **OPEN** = genuinely undecided; lean noted.
**V2-GATED** = designed (mostly in gen 1) but not built until an explicit,
observable trigger fires.*

## Settled

| # | Decision | Rationale (inputs) |
|---|---|---|
| S-1 | **Triage-to-canon architecture; no runtime injection layer.** Pending is inert; routed lessons live in natively-loaded surfaces. | E-1 (SessionStart injection structurally wrong), E-6 (preloading hurts), P1/P2. Reopens if Claude Code ships per-skill native memory that changes the delivery calculus (E-9). |
| S-2 | **v1 review surface = Claude Code itself** (`/self-learn:review` + AskUserQuestion cards); CLI is the portable core. | Agent-conversation-in-UI is the expensive half of a standalone app and already exists in-terminal; data layer stays UI-ready. Standalone UI = O-1. *Amended 2026-07-11:* cards are four options max — Apply/Discuss/Reject/Defer, Edit via Discuss — per AskUserQuestion's hard limit (E-16); diffs render via option `preview`; bulk ops via `multiSelect`; review works a bounded batch (default ~10), sets a TTL'd+heartbeated autosync pause sentinel, and **self-pushes its commits** (the sync's clean-tree branch never pushes — blind re-review 2026-07-12). *Amended 2026-07-12 (UI direction):* the v1 surface is **scaffolding for the resident TUI** (`07-review-ui.md`; platform since re-decided 2026-07-12 → localhost web app, see the G-3 row + 09 — the substrate consequence below is unchanged); consequence — all resolution mechanics (route/reject/defer verbs, compile+commit, sentinel, self-push, `--note`) live in the **CLI**, `--json` on read verbs, and the slash command stays a thin caller so later surfaces inherit the substrate. |
| S-3 | **v1 supply = explicit `teach` + auto-memory importer + one-shot backlog importer.** No silent capture pipeline in v1.0. | E-2 (base rate), E-7 (auto-memory already captures), P3/P4. |
| S-4 | **Both lesson types flow through one pipeline**: `type: behavior \| knowledge`. | Behavior-only scoping starved gen 1 (E-2: ~90% of real lessons are knowledge); destinations differ, machinery doesn't. |
| S-5 | **Pre-analysis worker = detached `claude -p`** (home-net-capture pattern), coalesced runs, lazy clustering over the pending set. | Proven in-repo; SDK worker is a swap behind the same contract if needed. Clustering-at-analysis replaces eager corroboration infra. *Amended 2026-07-11:* proposals are sibling files (pending records untouched by analysis). *Re-amended 2026-07-12 (blind re-review):* **the worker is fully append-only — merges included.** Clustering emits `proposals/merge-*.yaml`; the human collapses clusters at review. Record mutation, the designated-host rule, and the claim marker are all removed — any machine may run the worker; `--allowedTools` write surface = new proposal files only. *Amended 2026-08-19 (U-docs, per the register's own rule — this row's stated input changed):* the rationale's conditional — *"SDK worker is a swap behind the same contract if needed"* — **has been built.** The contract is `S-34`'s invocation seam and the swap is `S-35`'s backend selector. `S-5` is unchanged as a decision: the worker is still a detached, coalesced, append-only pre-analysis pass, and its default transport is still a `claude -p` subprocess. What changed is that "detached `claude -p`" is now the **default**, not the definition. |
| S-6 | **Compilers: SKILL.md / CLAUDE.md managed sections, references append, new-skill scaffold *(amended 2026-07-12, M3 implementability review: CLI-owned deterministic template — plugin-dev demoted to optional post-hoc enrichment, `08-build-plan.md` §8.1)*, hook scaffold (hookify pattern).** Diff-first, idempotent, marker-bounded. Hook target: always human-approved diff, never auto-registered. | P8, P9, repo settings.json doctrine. *Amended 2026-07-11:* draft diffs are **previews** — compilers apply records, regenerating output at apply time (staleness harmless by construction); **one commit per routed lesson** (surgical revert; the routed-and-reverted metric needs it); the user-scope compiler is chezmoi-aware (E-17); managed sections carry a mechanical overflow cap (`02-schema.md` §4). *Re-amended 2026-07-12 (blind re-review):* correction of a routed lesson = **supersede + recompile** — whole-section regeneration makes per-lesson `git revert` unsound; per-lesson commits are kept for attribution, with the record id in the commit *message* as the record→commit link (a commit's own hash can't live in a file it contains); the user-scope compiler must also **commit+push the dotfiles repo** (`chezmoi re-add` alone is same-machine-only); graduation's owning verb is `self-learn graduate <id>`. |
| S-7 | **Storage: in-repo, record-per-file, pending/→resolved/ dirs; transient state in `~/.cache`.** | E-8 (autosync storm excluded by construction), multi-machine via autosync, znote-compatible format. *Amended 2026-07-11:* `routed/` renamed `resolved/` (it also holds rejected/superseded); `proposals/` holds the full worker analysis, not just diffs; `teach` secret-scans captures (autosync publishes pending records pre-review); deferral gains `deferred_until`/`deferred_count` semantics. *Amended 2026-07-15 (11 ratification, user-delegated):* a declared third storage class exists — `.self-learn/telemetry/` (committed, append-only, actor-scoped monthly JSONL, written ONLY by human-triggered verb flushes from the cache spool; 11 §4). E-8's both halves hold: no per-session tracked writes (sessions write cache), no unscanned publication (scan-at-flush). |
| S-8 | **Schema per `02-schema.md`** — trigger/instruction substance, kind-for-routing, sightings-set-at-collapse; counters/confidence/classification dropped. **Substance freezes at routing, not capture.** | Reopened 2026-07-11; **settled 2026-07-12 — blind-adjudicated ADOPT** (the old capture-freeze contradicted the Edit-in-Discuss flow, the cluster pass, and the evidence note; pending is inert per P1, so nothing downstream trusts pre-routing content; git versions drafts). Rider adopted with it: the secret scan runs on **every** record-body write, not just `teach`. `superseded_by ∈ {null, id, canon}` formally defined (`02` §2). |
| S-9 | **Notifications: ambient-informative, never demanding.** Per-worker-run events carrying the standing aggregate ("proposed update to X — N pending across M scopes"), record ids in the payload (TUI deep-link contract); threshold nudges remain as unattended-list escalation; staleness alarm unchanged. | Notification fatigue = queue death in different clothes (E-3) — but so is the invisible backlog. *Amended 2026-07-12 (UI direction, `07-review-ui.md` §1):* "never per-item" refined to **never per-item-demanding** — per-run informative events replace pure thresholds as the default; attend-at-convenience is the participation model, and worker-run coalescing rate-limits for free. Original rationale intact. |
| S-10 | **Immediate path always exists**: `teach --route` captures, analyzes, applies, and commits in one motion. | P3 — the system is useful with the inbox permanently ignored. *Amended 2026-07-16 (user ruling on the M3 build's hard-coded refusal — "this is exactly the kind of thing that shouldn't be hard-coded. make it configurable."):* one-motion routing to `hook`/`new-skill` is **config-gated, default OFF** — the default is the review-gated flow exactly as the M3 adversarial review verified it. The opt-in is a **committed** `<ledger-home>/config.yaml` (`one_motion_route: {hook: true, new-skill: true}`), never an env var: policy that changes what executable code the CLI may auto-commit belongs in git history, synced and revocable by commit. Parsing is fail-closed — anything but the YAML boolean `true` (missing/malformed/false/strings) refuses, malformed shapes warn on stderr. Opting in is the operator explicitly accepting the unseen-bytes tradeoff for hooks: the CLI still runs the **full integrity chain** (CLI-generated script, schema validation, whole-input secret scan, M3-12 example replay — all pre-commit) and **prints the applied script bytes**, so visibility survives without the gate; and **activation stays manual either way** — the settings.json registration step is untouched, so no guard can fire without a human edit. Per-destination gates are independent. *Scope ruling 2026-07-16 (user, closing the delta review's n-2 question — "when the flag flips and opens the gate, it opens it fully."):* `hook: true` enables BOTH one-motion roads — the explicit `--hook-input` path AND the analyst-authored bare `teach --route` path (the model may pick `hook` and author the compile input). Considered and rejected: splitting the knob per authorship. The delta review's finding stands as the basis: both roads run the identical integrity chain and the analyst can smuggle nothing the explicit path couldn't; the settings.json registration step remains the human gate on activation either way. |
| S-11 | **Language: Python core + bash shims; name: `self-learn`.** | Matches ha-note/cron-claude precedent (carried from gen 1 A4, unchallenged in every review). |
| S-12 | **Append-only substance from routing onward + supersede; git is history/blame.** | Carried from gen 1 / ha-note. Reopened 2026-07-11; **settled 2026-07-12 with S-8** (freeze-at-routing). Supersede-and-recompile is the only correction path for *routed* records — per-lesson `git revert` is explicitly NOT a correction mechanism against regenerating sections (blind re-review 2026-07-12). |
| S-13 | **Auto-memory pruning is a post-processing step.** Imported MEMORY.md entries are pruned only after their record reaches a terminal status (routed / rejected) — never while in flight, and never inline with adjudication. Decisions first; a batched cleanup sweep after (end of review session, or the next import run); visible confirmation, never silent. | User directive 2026-07-12 (settles O-5, including its prune-on-reject extension). Keeps MEMORY.md lean (E-7) without coupling cleanup to decision latency, and keeps the adjudication flow free of side-effect confirmations. |
| S-14 | **Auto-memory importer ships in v1.0** (settles O-2). Dedupe by `evidence.origin` across all records in all statuses; prune per S-13; other-projects sweep remains O-2's G-2-gated extension. | Largest existing supply (E-7), cheap (read a directory, copy records), and the heavy-use sizing (E-2 caveat) means the inbox drain matters from day one. Delegated lock-in 2026-07-12, consistent with the user's pattern: supply wide and frictionless, control at adjudication. Fallback intact: if the memory-dir format proves unstable mid-M1, slip to v1.1 without redesign. *Milestone pinned 2026-07-12 (implementability review G-6): the importer builds in **M1** — the version↔milestone mapping is **v1.0 = M1+M2, v1.1 = M3+**; 04's former M3 ("v1.1") placement was stale against this row and has been corrected.* |
| S-15 | **Model-prompted teach offers ship in v1.0, quality-gated** (settles O-6). One standing instruction; the filter words carry the bar (significance, not count — no per-session cap); revocable in one line. | Capture-at-the-moment is where the shipped gate-the-write products won (E-20), it's the only automatic supply the skill scope has, and adjudication load lands downstream in the attend-at-convenience surface (`07-review-ui.md`) so offers need no rationing. Delegated lock-in 2026-07-12. *Amended 2026-07-15 (11 ratification):* the pinned offer line gains a decline-logging clause (`self-learn telemetry note offer-declined [--reason <enum>]`) — applied to `~/.claude/CLAUDE.md` when the spool verb ships (11 §7 now-tranche), not before; offer semantics unchanged; the derived capture-rate metric is pinned lower-bound. |

| S-16 | **Post-routing lifecycle + telemetry layer per `11-telemetry-and-lifecycle.md` (v2)** — follow-ups, recurrence suspect→confirm, capture-context fields, cache-spool→verb-flush observation plane, disposable index + report, five multi-machine posture principles as standing review criteria. | Ratified 2026-07-15 (user-delegated after the four-agent audit; delegation + the self-answered values Q&A recorded in the README revision log). No new lifecycle status; S-8/S-12 untouched; S-7/S-15 amended per their rows; worker riders bind T13 (08 §7). |
| S-17 | **Hosting / product separation per `13-hosting-and-separation.md` (D1–D3)** — promoted to the register 2026-07-18 (they were standing decisions living only in doc 13 + the README log): D1 guards compile into `<skills_root>/hooks/self-learn/`; D2 the product is its own repo (`~/repos/self-learn`, corpus inside), the ledger its own repo (`~/.self-learn`); **D3 pushes are MANUAL** — no autosync anywhere in the product or ledger; verbs commit, the human (or a ship-completion step, by precedent) pushes. | Ratified 2026-07-16 (user-directed, four calls answered live — doc 13 §1). D3 especially is load-bearing operating posture that reviewers and orchestrators must not "fix" by adding auto-push. |
| S-18 | **Subagent model split: the reviewer tier authors specs and reviews; the builder tier builds; the orchestrator model is never spawned as a subagent.** Concretely today: Opus = spec authors, blind reviewers, delta checks, judges; Sonnet = builders and mechanical work; Fable = the main-session orchestrator/planner only. `model:` passed explicitly on every agent spawn. | User cost ruling 2026-07-18 ("fable is too expensive for multi-agent use"), after the round-3 orchestration spawned ~8 orchestrator-tier subagents. First full round under the split (UX round 1, same day) held it with no quality loss — all gates reached CLEAN/SOUND. Reopens if the model lineup or pricing changes materially. |
| S-19 | **The product repo is PUBLIC and source-available under FSL-1.1-MIT; contributions require a CLA granting a broad relicensing right.** Licence text at the root `LICENSE`; `plugin.json` + both `pyproject.toml` declare the SPDX id; installable via the root `.claude-plugin/marketplace.json`. | Reverses D2's `private` half (doc 13 §7.3, amended 2026-07-24) and the derived pin P-C1.4; the LEDGER stays private. FSL was chosen over MIT/Apache to bar Competing Use while permitting internal and commercial use, with each version converting to MIT on its second anniversary — so openness is deferred, not withheld. The CLA (not a DCO) exists because GitHub's inbound = outbound default leaves each contributor holding copyright, which would permanently foreclose the close-source / dual-licence option the user wishes to keep. Reopens if the user decides to relicense, or if contributor volume makes manual CLA checking impractical (a bot is the pre-agreed answer — spec §6.3). |
| S-20 | **In-flight/progress perceptibility moves into CI for the feedback one unit introduces; taste stays human** (`drafts/ui-inflight-feedback-spec.md`, gated SOUND, builder-landed 2026-07-25; amends `09 §7`). The oracle is `page.locator("body").aria_snapshot()` inequality, never source-text or bare DOM-existence assertions, paired with targeted `to_have_css` checks where the oracle's own measured blind spot (opacity/colour/cursor — `aria_snapshot()`/`is_visible()` are both blind to `opacity: 0`) applies. A hand-rolled computed-style digest was tried first and **rejected**: measured to drift on an idle page because it counted content inside a collapsed `<details>` as visible — a false-positive generator, not a stricter oracle. **The boundary move is narrow, not general**: perceptibility is asserted for the NEW feedback this unit ships (in-flight disabling, the applying/bulk-progress strip, two never-tested banners) — roughly twenty pre-existing feedback sites (R-2, `drafts/ui-inflight-feedback-spec.md` §9.1) stay unasserted, and colour/opacity/contrast/geometry/occlusion/`text-transform` stay outside the automated oracle entirely; visual polish ("does it look good") is unchanged, still a human acceptance item (`10 §4`). **R-1** (indicators land only on the three confirm routes + the bulk-graduate loop) is an explicit refusal to decide the other 39 htmx verbs without data, not a claim they're fast — deferred pending measured per-verb latency (forward work, `14-forward-work-map.md`). **R-6 compelled three scope expansions**, none discretionary: `routes.py` (the bulk-progress emitter couldn't narrate a first item or emit a terminal frame — F5), `keymap.py` (one of ten never-pressed keymap actions was bound to a name no template carried — F6), and `tests/test_keymap.py` (F6's fix renames a **ratified** action name three existing tests pin). On that rename specifically: `action_bar.html`'s holding-context button already rendered `data-key-action="confirm_recurrence"`, but `KEYMAP` bound key `c` to action `confirm` — dead, because `confirm_recurrence` appeared in neither `keymap.py` nor `app.js`. The template-side alternative (renaming the button attribute to `confirm` instead) was rejected on evidence: `data-key-action="confirm"` already exists at three other sites (`action_bar.html`'s armed block, `proposal_bar.html`, `host_add_bar.html`), and `host_add_bar.html` is included by both `bucket.html` and `detail.html` — a holding Detail page for a record with an unregistered host could co-render two identical targets, and `clickAction`'s `document.querySelector` would resolve ambiguously by document order. Renaming the keymap action instead leaves every existing target unambiguous. `test_keymap.py::test_keymap_covers_every_pinned_action`, `test_pinned_key_bindings`, and `test_holding_row_keys_are_t_and_c` (the last citing `09 §11 Y-4`) were updated to the new name under this same human-ratified authority (2026-07-24) — edited, not silently fixed up. **§4.3's in-flight tracking mechanism is a keyed `Map<key, {badge, detail}>`, deliberately not a counter+flag pair.** Five consecutive gate rounds (R2–R6) each found a defect in the counter design, the last three sharing one shape — new state added without reconciling an older rule that touched it (two state machines arbitrating one element; N+1 increments vs one decrement; a derived `bulkInFlight` flag; connection loss not clearing it; a clamp rule that broke the flag's own invariant). The Map replaces six interacting rules over two variables with three rules over one: `set` on start (idempotent — a duplicate frame is a no-op by construction), `delete` on terminal (a no-op on an absent key, which is exactly what the counter's clamp was approximating), `clear()` on SSE connection loss (one operation, nothing can be left behind). Render is re-derived from Map contents after every operation, so stale text cannot lag state. The reasoning is the reusable part — a future reader tempted to simplify this back to a counter should be able to see what that costs before doing it. **A residual gap survives and is disclosed, not papered over** (measured, `09 §7`'s new amendment does not claim to close it): `app.js`'s `source.onerror`/`source.onopen` pair does not distinguish a transient SSE reconnect from a real drop, so a verb that is still running *across* a reconnect ends with **neither** the applying strip (cleared by the connection-loss `clear()`) **nor** the reconnect strip (removed by `onopen`) showing — a silent window, bounded to observer tabs only (the submitting tab's own `hx-disabled-elt` cue is local to its request and survives). Closing it needs an in-flight state-resync envelope (`sse.py` + a new route) — out of scope here (a fourth R-6 expansion this unit declined) and carried forward (`14-forward-work-map.md`, FW-38). | Blind spot and rejected alternative measured 2026-07-24 (R1, re-confirmed R2 against the real stylesheet with htmx's exact `setAttribute("disabled","")` form). Mechanism defects measured across R2–R6 gate rounds against the counter design before the Map pivot (R6). Per the gate record (`reviews/2026-07-24-ui-inflight-feedback-spec.md`): the Map pivot was verified mechanically (12 sequences, 14 mutations) then fuzz-tested (20,000 interleavings) at the spec gate, before this row's own code was built — this row does not itself assert the builder re-ran that verification; the two-gate discipline's blind code-gate mutation check (§11) is separate, deliberately not run by the builder, and is that gate's own job. Opus reviewed/gated, Sonnet built. |

| S-21 | **A `new-skill` proposal NAMES the skill.** The analyst proposes a name; the human then keeps it, changes it, or routes the lesson to another surface. **Reverses** the prior pin ("a `new-skill` proposal never names the skill — the name is the human's call at route time"), which was **agent-authored, not ratified**. | User ruling 2026-07-27, verbatim: *"the analyst should be able to name the skill for presentation to the user. the user gets to decide if they'll keep the name, change the name, or route to another surface."* The reversed pin lived at `routing-doctrine.md:262-263` and `08-build-plan.md:469`, where it justified itself as "the confirmed §4 human call" — **§4 contains no such row** (verified with a positive control; `research/2026-07-27-routing-monoculture-and-pin-audit.md` §5). Its measured consequence: combined with a pane grammar that *requires* a name (`proposals.py:98-99`), a doctrine-compliant `new-skill` proposal was structurally un-approvable (`rc=1`, verified live), and `new-skill` was used **0 times in 28 routings**. Three of the four implementing parts already exist as dead code (`validate_proposal` accepts and round-trips `new_skill`; `models.py:1275-1287` renders it, with a test; the pane grammar matches it); the single load-bearing blocker is `verbs.py:510-517`, where `_Destination`'s qualifier slot is a hardcoded `None` beside `variant`/`rules_topic`/`rules_paths`, which all pass through. **Two sub-questions this ruling does NOT settle** and which route to the user before build: (a) is the model's name trusted like `rationale`, or CLI-regenerated like hook `script` bytes are at `ledger_ops.py:668-690`? (b) the "change the name" leg has **no UI** — the action bar carries a destination *cycle key*, not a text field. ***Both settled 2026-08-02*** (user, from the campaign §6 decision batch — see S-23's provenance note, which covers this same batch): **(a) the analyst proposes, the CLI validates, the human confirms.** The name is neither trusted raw nor regenerated: the analyst's name is the proposal, the CLI mechanically checks what must be mechanical (kebab-case, collision against existing plugins, marketplace entry), and a rejected name comes back **with its reason** — never silently rewritten, because a silent rewrite moves the naming decision away from the human this row just gave it to. Deterministic CLI regeneration was considered and rejected: reproducible, but a derived name is usually a bad one. Trusting the name as-is was rejected as the same misroute class this campaign is hardening — it would let an unreviewed model output mint a plugin directory. **(b) yes — the review surface gets a text field**, so the human can retype the name before approving. Without it the only moves are accept-or-reject, so a proposal right in every way except the name must be rejected and re-captured. Costed honestly at ruling time as genuine UI work (new input, validation feedback, re-render), not a one-liner. Together these unblock `U-name` in the r2 campaign. |
| S-22 | **Loosen the funnels the harness creates around its agents** — structurally and programmatically — while keeping the harness. A standing posture for routing/prompt/mechanics work, not a one-off task. | User ruling 2026-07-27, verbatim: *"we need to assess all of the prompting and the mechanics around said prompting and loosen some of the restrictions we place around the agents, both structurally and programatically. they need some freedom of movement in order to get this job done. that's not the same as saying just spawn `claude -p` instances and have them run around and do whatever they want. i like the harness we're building here, and the structure it provides. we just need to assess the funnels we're creating."* **Read the second half as binding as the first**: this is explicitly NOT authorisation to remove structure, drop the two-gate discipline, or widen agent tool surfaces — the pane's no-Bash/no-Task/no-web absence, `Edit`-not-`Write`, the secret scan, and the proposer≠approver split are containment, not funnels. A *funnel* is a constraint that silently removes an option the agent should have had: the audit's worked examples are the user-scope destination singleton (`models.py:101`), the `hook` serializer drop (`analyst.py`), and S-21's naming pin. Reopens if the user narrows the posture. |

| S-23 | **The cheap tier is PATHED, not DEMAND — at every scope.** Two halves, one tier model. **(1) The on-demand shelf (`reference`) survives and gets its pointer**, so the 14 records already routed there become reachable — but `paths:`-scoped rules become the **primary** cheap tier, and DEMAND shrinks to lessons that are genuinely *not* file-scoped. **(2) User scope gets a cheap surface, and it is pathed rules only** — explicitly NOT a user-level reference file. Consequences for the r2 campaign: `U-pointer` and `U-demand-user` unblock, and **`U-demand-user` is re-scoped** — "open the on-demand shelf at user scope" (campaign §2) is superseded, because the user-scope cheap surface is PATHED. `U-composer`'s doctrine rewrite must carry the tier reordering. | User ruling 2026-08-02, from the four-question decision batch the campaign playbook §6 requires be asked in one batch with a recommendation each (`forward/r2-routing-campaign.md`; the batch also settled S-21's two sub-questions). **Provenance note, stated because this corpus treats an absent quote as a signal:** the user ruled by *selecting* among options, so there is no verbatim user sentence to quote here — the framing and the recommendations were the orchestrator's, the decision was the user's, and all four selections took the recommended option. **Evidence the ruling rests on.** Retiring `reference` was rejected because it orphans 14 existing records; keeping DEMAND as the general cheap tier was rejected because the pointer's core assumption — that a session opens the file at the right moment — is **unmeasured**, and r2 §8 names it the design's soft spot. Pathed injection needs no such hope: it is automatic, and was **verified working on this host 2026-07-28** (canaries at project scope and at user scope through two fresh headless sessions at different working directories, then removed). The measured monoculture this breaks: user scope 7 always-loaded + 1 hook, project 5 + 1, and 14 unreachable `reference` routings all belonging to one skill. **The limit that shapes half (2):** user-level `paths:` globs resolve **relative to the session's working directory** and absolute globs never match — so a user-scope pathed rule structurally cannot be aimed at one repo. That is coherent for user scope ("everywhere") and means user-level pathed rules suit **cross-project file-type conventions**; repo-specific guidance belongs at project scope regardless. A user-level reference file was rejected on the same evidence that motivates the pointer at all: it inherits the unreachability problem with no `SKILL.md` to hang a pointer off. **A rider the doctrine must carry** (found in the 07-28 path-scoping test, not derivable from the tier model): a pathed rule fires on first *Read* of a matching file, so a lesson whose trigger fires *before* the work ("before choosing a fixture strategy, remember X") is served badly by PATHED even when it is nominally file-scoped. r2's T2 gate asks only "does it only matter for certain files?"; it must also ask **whether the trigger fires at or after first contact with those files.** Reopens if on-demand lookup is ever actually instrumented (campaign Checkpoint B) and measured to work. |
| S-24 | **ACCEPTED residual — a PATHED destination looks like delivery and, for a Grep/Glob-only workflow, silently is not.** Injection fires on `Read`. `Edit` is covered in practice because Claude Code requires a `Read` first, but a session that only ever `Grep`s or `Glob`s a matching file never has the rule injected at all — a lesson routed to PATHED is not delivered to a search-only workflow, and nothing in self-learn can observe that. Accepted as residual, not deferred: no code in `U-pathed` or any other unit can close it — injection belongs to Claude Code, not this codebase. Two consequences, both named rather than built: (1) doctrine — `U-composer`'s T2 gate must ask not just "does it only matter for certain files?" but **"will the work that trips this lesson actually open one of those files, or only search them?"**; a lesson about grepping conventions is exactly the case PATHED cannot serve. (2) No in-band warning is emitted — a YAML comment carrying the caveat was considered and rejected, since it would depend on Claude Code's frontmatter parser tolerating comments, which has not been measured on this host, and a rules file that fails to parse is worse than an unstated caveat. | `U-pathed` spec §7.1 (`docs/specs/self-learn/drafts/u-pathed-emission-spec.md`), landed in the same commit as the `paths:` emission build per the campaign's disposition rule (playbook §1: an undeclared residual is one a later agent re-opens as a bug). |
| S-25 | **ACCEPTED residual — user-scope pathed globs have no dead-glob guard.** Project scope refuses a zero-match glob at route time and re-asserts it in `selfcheck` (`selfcheck.py:374-399`). User scope has no canonical tree to check a glob against (S-23's own measurement 2: a user-level glob resolves against whatever repo the session happens to run in), so `_resolve_rules_target` runs only the proposal-schema shape check — and S-23 just made user scope a **primary** PATHED tier, so this gap now sits on the primary surface, not a side one. A misspelt user-scope glob therefore fires nowhere and reports nothing. Mechanical guards that DO apply, and are all `U-pathed` can offer: the proposal-schema shape check (`ledger_ops.py:613-623`) and the absolute/`~`-leading refusal (`U-pathed` §3.4(1)). Beyond that it is the human's read of the review card. This is a *new* gap, created by S-23's promotion of user scope — not a pre-existing one this unit merely declined to fix. | `U-pathed` spec §7.2, landed in the same commit as the build (same disposition-rule obligation as S-24). |
| S-26 | **The decision-trace fields (`gates:`/`flags:`/`recommendation:`, U-schema) flip from optional to MANDATORY at the earliest convenient moment — progress outranks capturing the pending backlog.** The flip is one flag; the binding constraint on "earliest convenient" is the producer, not the queue: flipping before the analyst emits traces by construction would make `write_proposal` refuse every analyst proposal and wedge the worker pipeline — so **the flip rides `U-composer`** (the unit that makes the analyst emit traces), landing with or immediately after its merge, and does NOT wait for the pending queue to drain. Pending records that predate the schema get conformed by a **migration workflow** rather than blocking the flip — the user explicitly anticipated this and sanctioned a multi-agent workflow for it. Two constraints the migration inherits: the ledger is agent-read-only, so migration mutates records only via CLI verbs under user supervision (mechanism to be settled in `U-composer`'s spec, which owns the flip); and migrated content must not fabricate traces — a record whose gates were never evaluated gets an honest absent-with-reason form, never invented values. | User ruling 2026-08-06, verbatim: *"its more important to me to make progress on this project than to capture all pending lessons. do it at our earliest convenience, but keep in mind that a multi-agent workflow to update the lessons to conform to the contemporary schema will likely be required."* Answers campaign §6 question 8 (asked with a recommendation per that section's rule; the user's own answer superseded the offered options by re-weighting the goal: progress over backlog preservation). Supersedes the "start optional so nothing in the current queue breaks" posture as a *permanent* state — it was always a bridge, this row gives it an end. |
| S-27 | **ACCEPTED residual — `t3a` is over-required at a mismatched scope.** When `gates.t3.answer == "yes"` and the record's scope is **not** `skill:<owner>`, Table-1 never reads `t3a` — the run falls through `L3`/`L4` — yet `_validate_gates` still requires it non-null. The analyst pays real cost (a judgment and a quote) for a block nobody will read. **Accepted, not deferred:** relaxing it would make the scoped path accept what the scope-free path refuses, and `worker.py`'s `_land_outputs` **deletes** proposals its positional `validate_proposal(data)` refuses — trading a wasted paragraph for a destroyed analysis. It closes for free the day the last positional producer site is scoped, which is `U-composer`'s obligation; until then it is the correct side of the trade. U-schema's own §6-D5 named this direction of its residual explicitly and handed it here; this row is the disposition, so a later agent does not reopen it as a bug. | `U-table` spec §7.2 (`docs/specs/self-learn/drafts/u-table-decision-table-spec.md`), landed in the same commit as the build per the campaign's disposition rule (playbook §1: an undeclared residual is one a later agent re-opens as a bug), following `U-pathed`'s S-24/S-25 precedent. Recorded 2026-08-07. |
| S-28 | **ACCEPTED residual — `alternates` cannot distinguish ALWAYS from PATHED.** R-HOOK requires `alternates` to contain the *destination* the load class renders to; `ALWAYS` and `PATHED` both render `claude-md`, so a HOOK proposal whose real alternative was `PATHED` satisfies the rule by naming `claude-md`, and so does one whose alternative was `ALWAYS`. Closing it would mean giving `alternates` a richer type than the shipped list from `PROPOSAL_DESTINATIONS` — a schema change to a field three surfaces already render, for a distinction the human can read off `gates.t2.answer` on the same card. **Accepted, with the note that the trace itself carries the missing bit**, so nothing is actually unauditable — only `alternates` alone is insufficient. | `U-table` spec §7.3, same commit as the build (same disposition-rule obligation as S-24/S-25). Recorded 2026-08-07. |
| S-29 | **Background review automation: the agent's autonomy is a user-configurable tiered policy, never a fixed boundary.** The review workflow is already a thin caller over CLI verbs (`commands/review.md`) and the M2 worker already produces the analysis (traced, post-S-26), so automation means one thing: delegating the per-card judgment tap to an agent under policy. Design consequence: a ledger-config autonomy policy (the S-10 `one_motion_route:` opt-in pattern) declaring per-resolution-class tiers — graduate-already-canon, defer, reject-stale, route by destination class — with everything above the configured tier escalating into a human digest instead of being resolved. Hard floors independent of any config: hook routes are never auto-applied (P9 — executable bytes get human eyes), and secret-scan blocks always escalate. The agent-read-only ledger posture stays the default; the configured reviewer role is its only sanctioned exception, and only through CLI verbs. **Sequencing, same ruling: the first post-flip drain is SUPERVISED** — Checkpoint A's measurement plus one interactive calibration batch — before any automation runs, because the before/after evidence is one-time. FW-76 is a prerequisite: unattended operation cannot sit on a worker whose failed Force run is indistinguishable from success. | User ruling 2026-08-08, verbatim: *"ultimately it should be configurable to the user's preference, which suggests a customizable tiered autonomy approach. there is no one size fits all solution."* — answering the autonomy-boundary question; the supervised-first option was selected as recommended for sequencing. Build tracked as FW-82. |
| S-30 | **ACCEPTED residual — a repair that clears one Set-C defect and leaves a sibling one is deleted, not re-repaired.** Measured (`U-repair` spec §1.2, §9-X2): 9 of 13 refused files in the replay corpus carry ≥ 2 independent Set-C violations, and the validator names only one at a time. The mitigation is the repair prompt's checklist (every line checked against the whole file, not just the refusal named) plus one round — not a second repair round. A file whose repair clears the named refusal and leaves a sibling violation is still deleted at `S8`; its record stays pending and the next window re-analyzes it from scratch. **Single-sample caveat, stated as part of the acceptance itself, not just the figure**: the 8/1/4 distribution is the replay corpus alone — the live run's 7 schema refusals were never censused on the same axis, so this is a one-round bet, not a proven-sufficient one. **Accepted, not deferred**: a second round doubles the model budget per run against an unmeasured population and is the kind of loop that becomes permanent by default. The honest instrument is the shipped `run: repair round: X of N refusals cleared` log line — if the first post-merge week shows X consistently far below N, the fix is a better checklist or exemplar, not a third invocation. | `U-repair` spec §7.2 (`docs/specs/self-learn/drafts/u-repair-unattended-elicitation-spec.md`), landed in the same commit as the build — same disposition-rule obligation as `U-pathed`'s S-24/S-25 and `U-table`'s S-27/S-28. |
| S-31 | **ACCEPTED residual (Rule-F) — the worker leaves a proposal it did not write, identified by a matching `record_sha`, entirely alone; full producer attribution is a separate unit's job.** `U-repair`'s Rule-F closes the *destructive* half of the FW-84 incident — an attended proposal already **validated** before the worker's landing ran now survives byte-for-byte, left in `result.foreign_left` and never staged, stamped or deleted. It does **not** close: an attended proposal written but not yet validated (indistinguishable from model output, no `record_sha` to match on); one edited after validation and caught mid-edit (F-b holds, F-a fails — deleted, but identically to today, so this is a non-regression, not a new loss); an attended proposal for a **batch** record that has **never** passed validation (unstamped, therefore repair-eligible — the repair round may rewrite it and a successful repair lands and commits it, pinned as the specified behaviour by `D8(iii)`, not rediscovered later as a bug); an uncommitted model-authored foreign file (accepted — committing it would publish a human's possible in-flight bytes); the analyst's Write tool overwriting an attended proposal before landing ever runs; and a genuinely foreign `destination: hook` proposal, which Rule-F carves out on principle (stamping is the only guard against model-authored executable bytes reaching a route, P9) and which the worker therefore stamps, counts and commits regardless of provenance. Of these residuals, four share one root (`_written_since` diffs the world instead of recording what the model actually wrote) and are **FW-84**'s to retire via real per-file provenance; the Write-tool overwrite needs a *different* FW-84 direction (create-only semantics, not provenance alone); the foreign-hook case has a different root entirely (script generation as a stamp-time side effect) and is **not** FW-84's; backoff-suppression visibility is **FW-82**'s. **Operational mitigation until FW-84 lands**: do not run an attended review session while a worker window is open (`cache_dir()/worker.window` holds the live pid), and if a card's proposal vanishes mid-session, route with an explicit `--dest`. | `U-repair` spec §7.3, same commit as the build (same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30). |
| S-32 | **Producer attribution is structural, not observational.** The analyst writes only to a stage (`worker.stage_dir()`) it exclusively owns — `Grant-1`'s batch invocation is granted the stage and nothing else — and the worker installs from the stage into the ledger under the commit lock (`Install-1`: absent destination, unstamped-and-unchanged destination, or a live install-journal entry whose digest matches). A ledger proposal that changed during the model window was, provably, not written by the model, and is never overwritten, stamped or deleted — attribution no longer depends on diffing the world before and after an invocation. `U-repair`'s `E-4` (batch membership) narrows to a litter rule over the stage; `E-5` (unstamped-and-unchanged) retires into `Install-1`'s own I-b leg; `Φ` (Rule-F's F-a ∧ F-b) leaves the repair partition entirely — every staged file is by construction the model's own output, so the V-set is simply every valid one — and is read only by pass 2's `Rule-Fp` over ledger paths the model never touched, independent of the hook carve-out (which still governs stamp-or-leave only, never partition membership). `SELF_LEARN_STAGE=0` reverts the whole namespace to today's shared, diff-observed write scope, for exactly the incident window a namespace regression would need one; `SELF_LEARN_ENFORCE_SCOPE=0` is a deliberately independent switch that drops only `defaultMode` enforcement, never the namespace itself. | `U-attrib` spec §7.5 (`docs/specs/self-learn/drafts/u-attrib-producer-attribution-spec.md`), landed in the same commit as the build (same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30/S-31). Recorded 2026-08-09. |
| S-33 | **Two ACCEPTED residuals, both closing FW-84's exclusive-namespace direction rather than deferring further.** (i) **The permanently-blocked destination** (§7.2): an abandoned, unstamped attended draft sitting at a batch record's destination is declined every window (`IN3`), so its record is re-analyzed and never lands until a human validates or deletes the draft — the alternative is either overwriting a human's work or changing queue semantics (`is_unanalyzed`/`proposal_info`), both out of scope here. Loud, not silent: one log line and one counter per run (`IN6(b)`), and the blast radius is wider than the blocked record alone — a blocked record can be a run's only outcome, which sets `status` to `failed`, increments the failure counter, and at `FOLLOWON_FAILURE_CAP` suppresses the follow-on window that drains `leftovers`, so one abandoned human draft can stall the backlog drain for unrelated records. Mitigation is unchanged (a human validates or deletes the draft); what changes is that the two existing log lines (`not_installed`, the suppression line) now let an operator see the second-order effect instead of discovering it. (ii) **A mid-window destination change loses one window** (§7.3): when a destination changes during the model's invocation, this run's staged proposal for that record is dropped rather than merged or queued; the record stays pending and the next window re-analyzes it. Accepted because one wasted analysis is strictly cheaper than one destroyed human review, and merging two producers' proposals is not a thing the worker may do (S-26). | `U-attrib` spec §7.5, same commit as the build (same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30/S-31). Recorded 2026-08-09. |
| S-34 | **The invocation seam exists: one package (`self_learn/invocation/`) behind which every `claude` process spawn in the CLI lives.** Four surfaces (`worker`, `worker-repair`, `miner-reader`, `analyst`), three call sites (`worker._invoke_claude`, `miner._invoke_reader`, `analyst.analyze`), two operations (`write_session` — the model writes files, stdout is never parsed; `text_session` — the analyst's, where stdout *is* the result). Containment is **data** (`Containment`), not a flag string: the same object renders to the `cli` backend's settings-file permission rules and argv flags, and to the `sdk` backend's `can_use_tool` charter, so the boundary cannot drift between transports. **Neither seam operation raises** — every failure becomes an `Outcome` — with exactly one deliberate exception, and it is on `text_session`: a bare `OSError` from a `CliBackend` on the **analyst** surface still escapes, preserved byte-for-byte by `U-seam` and owned by `FW-87`/`U-sdka`. **`write_session` has no such leg on any surface** — the analyst is the only operation that can leak an `OSError` and the analyst never calls `write_session` (`registry.py`'s own docstring: *"that surface never reaches `write_session`"*). `Containment` enforces nothing itself; it describes what the settings file and the charter enforce. | `U-seam` spec §3.2/§3.3/§3.10 and §7.5 (`docs/specs/self-learn/drafts/u-seam-invocation-seam-spec.md`), reserved there for this register and landed 2026-08-19 by `U-docs` rather than by `U-seam`'s own build — see `S-42`. |
| S-35 | **Backend selection is a five-rung precedence chain, per surface, fail-closed to `cli`.** In order, first hit wins: `SELF_LEARN_BACKEND_<SELECTOR>` (env) → `SELF_LEARN_BACKEND` (env) → `<ledger-home>/config.yaml`'s `invocation.backend_<surface>` → `invocation.backend` → the built-in default `"cli"`. Three selectors cover four surfaces (`WORKER` serves both `worker` and `worker-repair`; `MINER` serves `miner-reader`; `ANALYST` serves `analyst`), so the repair round is never independently configurable **by environment** — though it is by config key, whose suffix is the surface literal (`backend_worker-repair`). An empty or unset value at a rung is "no answer" and falls through **silently**; an *unknown* value is not — it warns once on stderr and resolves `cli`. The `config.yaml` in question is the same committed ledger file `S-10`'s `one_motion_route:` lives in: policy that changes which transport executes belongs in git history, synced and revocable by commit. Resolution reads that file because every surface passes the ledger home as its session `cwd`. | `U-seam` spec §3.7 and §7.5; shipped at `invocation/registry.py::backend_for` and `config.py::invocation_backend`. Operator-facing procedure: `17-invocation-runbook.md`. Landed 2026-08-19 by `U-docs` — see `S-42`. |
| S-36 | **A second, orthogonal switch — `provider` (`anthropic` \| `bedrock`) — exists as pure configuration and contract, install-wide (not per-surface), independent of `backend`.** `bedrock` requires `backend=sdk` to do anything at all: under `backend=cli` the provider configuration is silently inert by design (`R-1`, ruling `D-18`) — a legitimate intermediate state of the staged rollout (analyst → miner → worker), not a defect. Refusals (missing region, an Anthropic alias standing in for a Bedrock id) are gated to `provider=bedrock ∧ backend=sdk` and surface as `Outcome(failure="unavailable")`, never a new `FAILURE_KINDS` member. No live Bedrock call is made or verified by this unit — `self-learn doctor invocation` is the entire diagnostic surface for the failure class (IAM, model access, region enablement) this unit's suite structurally cannot reach. | `U-bedrock` spec §3 (`docs/specs/self-learn/drafts/u-bedrock-provider-spec.md`), landed in the same commit as the build (same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30/S-31/S-32/S-33). Recorded 2026-08-18. |
| S-37 | **No credential may enter `config.yaml`, ever — every credential check this codebase performs is presence-only, because the ledger `~/.self-learn` is a git repository the operator commits and pushes.** `provider.py`'s `PROVIDER_KEYS` is a closed, whitelisted set of seven non-secret keys (region, profile *name*, model ids) with no key for an access key, secret key, session token, bearer token, or credential file's contents; the doctor's credential probe (`Cred-1`) learns THAT a mechanism exists (env var present, file present, a `[profile]` section header present, an SSO-cache entry COUNT) and never reads or prints WHAT it contains — the one probe that touches a credential file's bytes is bounded to a boolean. A check that cannot distinguish "absent" from "invisible to it" (IMDS is never probed — that would be a network call) reports WARN, never FAIL. | `U-bedrock` spec §1.2/§3.8 (`SEC-1`), same commit as the build. Recorded 2026-08-18. |
| S-38 | **RATIFIED, not reverted: `U-bedrock`'s wiring of `IN3`/`Int-1` correctly rewrote two U-sdk criteria (`OP11`, `OP13`) outside its original sanctioned test-file scope, because the old versions pinned a real defect.** Before the wiring, `options.model` was sourced from whatever `--model` argv the surface's own `build_argv` emitted — `worker.build_argv`/`miner.build_reader_argv`/`analyst.build_argv` are not provider-aware and always emit the Anthropic alias (§1.1). Under `provider=anthropic` this coincided with `provider.model_for(...)`'s own answer by construction (both trace to `worker_model()`/`miner_model()`/`analyst._model()` — verified empirically: `provider.model_for("worker", home=home)` returns the same value `_worker_argv()`'s own default carries). Under `provider=bedrock`, they diverge: argv still carries the Anthropic alias regardless of configuration, while `model_for` correctly returns the configured Bedrock id — verified empirically in `test_in3_options_model_and_cli_path_match_provider` (`kwargs["model"] == BEDROCK_ID`, not the argv-injected alias). **Meaning a correctly-configured Bedrock install would have silently sent an Anthropic alias to Bedrock's endpoint on every surface flipped to `backend=sdk`**, on every session, forever — the wiring fix is a genuine bug fix, not a cosmetic rename. The code gate adjudicated this "correct on the merits; out of sanctioned scope" and ratified rather than reverted it: `SU2` is restated to five bounded paths, the fifth being `test_invocation_sdk.py` at exactly 33 insertions / 7 deletions (`OP11` rewritten to assert the new provider-sourced value and an explicit `!= ` divergence leg, `OP13`'s closed-argv-read-set literal narrowed from two flags to one, one new import). Also recorded: U-sdk's own spec (`u-sdk-backend-spec.md` §3.5, `A-4`, "`options.model` can never be `None`") is now VOID as a description of shipped behavior — `options.model` is never `None` under the new wiring either, but the reason changed (a `model_for` return, not an argv-flag-or-`None` read) — folded as an FW row (`14-forward-work-map.md`) so the next unit touching U-sdk's spec text sees it. | `U-bedrock` code gate, this build's continuation round; `docs/specs/self-learn/drafts/u-bedrock-provider-spec.md` §"Files this unit may touch" and its restated `SU2` (§4). Recorded 2026-08-19. |
| S-39 | **The model transport is a switchable backend, not a fixed property of the product** — the Agent SDK sits behind the same seam as the `claude -p` subprocess, and either may serve any surface. Two consequences bind future work: (1) **the default at every rung stays `cli` until a surface passes its burn-in** (`S-40`) — shipping the capability is not shipping the flip, and `U-sdk` explicitly declined to move any default; (2) **anything that describes invocation must describe it as backend-conditional**, which is the standing obligation `forward/platform-drift.md` §4 already imposed on engine swaps and which Waves 0–1 did not discharge (this unit's whole §3.1 inventory is the measured cost of that miss). *Amended 2026-08-25 (U-cleanup, S-49):* consequence (1) — the default-stays-`cli`-until-burn-in premise — has lapsed structurally; `KNOWN_BACKENDS == ("sdk",)` and there is no second backend left to default away from. | **User ruling 2026-08-09**, ruling 2 of four in the approved migration plan, verbatim: *"**Cutover:** switchable backend. The CLI subprocess path is retained verbatim behind a config switch; CLI stays default until burn-in passes; a final cleanup unit deletes it. Under `backend=cli`, behavior stays byte-identical."* The three rulings it shipped with, same date and same binding force: **sequencing** — the three paused campaign units land first, because `U-attrib` rewrites the very `worker.py` layers this migration replaces; **Bedrock** — *"no live testing possible. Ship the configuration surface, contract tests against a fake, and a preflight `doctor` that validates config/env/credential shape without any API call"* (which is exactly what `S-36`/`S-37` record as built); **parallelisation** — disjoint-surface units as concurrent two-gate pipelines under `S-18`'s model split. **Provenance note, stated because this corpus treats an absent quote as a signal** (the convention `S-23` set): the ruling record is **durable but out-of-repo**, at `~/.claude/plans/indexed-kindling-lightning.md` §"User rulings (binding, 2026-08-09)" — approved by the user in plan mode. Nothing inside this repository quotes it; every in-repo artifact refers to it only as *"the approved Agent-SDK migration"* (`U-seam`, `U-sdk`, `U-bedrock` and `U-fake` spec preambles; `14-forward-work-map.md`'s 2026-08-09 entry). This row is therefore the ruling's **first in-repo record**; the plan file is authority over it, and **the user's own wording outranks both.** |
| S-40 | **The flip order is analyst → miner → worker, one surface at a time, each gated by a per-surface burn-in.** The order is attended-first: the analyst runs synchronously in front of a human who typed `teach --route` and sees its output immediately; the miner runs unattended on a nightly timer; the worker runs unattended **and commits to the ledger**, so it is last and its burn-in is the only one that includes a repair round, a commit and a push. A mixed install — some surfaces `sdk`, some `cli` — is the **normal intermediate state** of this rollout and must never be reported as an error (`S-36`, `FW-89`); the doctor renders it as per-surface INFO and reserves FAIL for a *wholly* inert configuration. The burn-in gates themselves are recorded in `17-invocation-runbook.md` §5, which is their first home in this repository. | Order stated in `S-36`, `FW-89`, and `U-bedrock` spec §3.5 (*"The rollout flips surfaces one at a time in the order analyst → miner → worker; `provider` is install-wide (`D-2`)"*), with the rationale at that spec's **`D-2`** (§6, builder decisions) and **`Q-2`** (§10, values questions — *"Mixed backend flips already deliver attended-analyst-first, so per-surface provider buys nothing and doubles the precedence matrix"*). Flip order and its reason are the approved migration plan's, §"Rollout / burn-in / rollback": *"attended-first; the analyst flip is also the F3 security fix; worker last — it commits to the ledger."* Gate criteria from the same section — **an out-of-repo artifact**, which is why this row and the runbook exist. `U-sdk` spec §7.3 `R-6` names the owner it was waiting for: *"no burn-in evidence exists for any surface … Owner: the post-merge burn-in unit."* |
| S-41 | **Defects found on a surface that Wave 2 will rewrite are preserved, not fixed in place — the "wait for the flip" disposition — and each one names the flip that closes it.** The worked case is `analyst.analyze`'s missing bare-`OSError` leg: `U-seam`'s charter was byte-identity under `backend=cli`, so the not-caught behavior was pinned as existing behavior (`T-c`, `TR4`) rather than silently changed, and the obligation was handed forward — *"the SDK flip dissolves the subprocess layer but not the obligation — the error contract must catch into `AnalystError` on BOTH backends so capture-to-pending holds regardless of the switch"* (`FW-87`). **The disposition's headline case is a security hole, not an error leg.** The plan records it as finding **F3** and rules on it verbatim: *"**F3 (analyst containment hole)**: the teach-route session's tools fall through to the host's `bypassPermissions` default today. User ruling: **wait for the SDK flip** — no CLI-side carve-out; the analyst flips FIRST and its hardening (deny-list, deny-all-writes callback, strict MCP, isolation) rides that flip."* That is why the analyst is first in `S-40`'s order and not merely the easiest surface: **the first flip is also the security fix.** The plan's own scout table says the analyst's containment is the *"WEAKEST"* of the three — allow-list only, no settings file, no deny list, no `--strict-mcp-config`. The rule generalizes past both cases: a unit whose contract is *no observable change* may not fix what it finds, even when what it finds is a hole, and the correct move is a dated forward row naming the closing unit — never a silent fix, never a forgotten defect. **Closure:** `U-sdka` (Wave 2) owns both — `FW-87`'s error leg with a test on each backend, and F3's hardening riding the flip itself. | `~/.claude/plans/indexed-kindling-lightning.md` §"User rulings recorded during planning (2026-08-09)" and its §"Verified facts" scout table; `U-seam` spec §7.3 `R-1` and §"This unit changes no behavior" (*"A builder who finds themselves fixing a defect they discovered on the way has left this unit's mandate and must stop and report"*); `14-forward-work-map.md` `FW-87`. |
| S-42 | **A spec that reserves a register row owes the row at ITS OWN merge; when that does not happen, the row is written by whoever next notices — with the delay recorded.** `U-seam`'s files-may-touch table listed `03-decisions.md` with the footprint *"New rows `S-34`, `S-35` (§7.5), landing in the same commit as the build"*; the build merged (`e2e63a2`, 2026-08-09) and the rows did not land. **Measured** at `89f8ef7`: the register ran `S-33` → `S-36` for ten days, so the two decisions the entire migration rests on — the seam and the backend selector — were undocumented in the register while three further units built on them. The same class of gap accounts for four of the five `MISSING` rows in this unit's inventory. Standing consequence: the disposition-rule discipline this corpus already applies to accepted residuals (*"an undeclared residual is one a later agent re-opens as a bug"* — the campaign playbook §1, cited by `S-24`…`S-33`) **applies equally to reserved register rows**, and a round-close is not complete while one is outstanding. | `U-seam` spec §"Files this unit may touch" and §7.5, against the register's measured state at `89f8ef7`. Recorded 2026-08-19 by `U-docs`, which lands `S-34`/`S-35` late rather than leaving them unwritten. |
| S-43 | **`claude-agent-sdk` ships as an OPTIONAL EXTRA (`self-learn-cli[sdk]`), never a hard dependency.** The bundle is ~252 MB; a machine that never leaves `backend=cli` pays none of it. The cost of the choice is one failure mode, and it is made clean rather than removed: a surface resolved to `sdk` on an install without the extra raises `BackendUnavailable`, whose message **names the install command** — the refusal is a byte-pinned two-line string, not a traceback. Two consequences the corpus must carry: (1) `install.sh` does **not** install the extra, so "flip a surface" is a two-step operation on a default install and the runbook says so before it gives any flip instruction; (2) the extra's presence changes steady-state behavior — `--selftest` spawns a real `claude --version` per run once `claude_agent_sdk` is importable (`FW-94`). *Amended 2026-08-25 (U-cleanup, S-49):* the condition this row rests on — that a machine can stay on `backend=cli` — has lapsed. The SDK is a hard dependency. | `~/.claude/plans/indexed-kindling-lightning.md` §"User rulings recorded during planning (2026-08-09)", adopted designer recommendation, verbatim: *"`claude-agent-sdk` ships as an **optional extra** (`self-learn-cli[sdk]`), not a hard dep (252 MB bundle; cli-backend machines pay nothing; registry raises a clean `BackendUnavailable` naming the install command)."* Shipped at `plugins/self-learn/cli/pyproject.toml` (`sdk = ["claude-agent-sdk>=0.2.116,<0.3"]`) and `invocation/registry.py::_SDK_UNAVAILABLE_MESSAGE`. Recorded 2026-08-19 by `U-docs`. |
| S-44 | **Attribution is capture-now, consume-later: the SDK backend's `tool_events` and `denials` are captured from day one and deliberately have no consumer yet.** `SdkOutcome` carries both as frozen tuples, populated from the charter callback's own denials and from `ResultMessage.permission_denials`, and written to a per-run event log. **Nothing reads them.** That is the decision, not an oversight: the consumer is a post-burn-in unit (`U-corrob`), and building it now would mean re-editing `U-attrib`'s freshly-landed `_harvest` — the exact churn `S-32`'s structural-attribution design was written to stop. **The standing constraint on that future unit, stated here so it is not rediscovered as a question: the filesystem diff remains the authority.** Tool events are corroboration, never the primary record of what the model wrote — a model's self-report of its own tool calls is not evidence of provenance, which is the whole lesson `FW-84` cost. Operational consequence today: the worker burn-in's *0 out-of-scope writes* gate reads `denials` **and** the filesystem diff, and the two must agree (`S-40`). | `~/.claude/plans/indexed-kindling-lightning.md` §"User rulings recorded during planning (2026-08-09)", the **"Adopted designer recommendations"** bullet — the same bullet `S-43` quotes, whose second sentence is this one — verbatim: *"Attribution: SDK `tool_events`/`denials` **fields captured now**, consumer is post-burn-in U-corrob (avoids re-editing U-attrib's fresh `_harvest`)"*. The *filesystem-diff-stays-authority* half comes from a different section of the same file — §"Units, DAG, schedule", the **"Burn-in → Wave 3"** bullet (a bolded bullet label, not a heading): *"**U-corrob** (attribution consumer; filesystem diff stays authority)"*. Shipped at `invocation_sdk/backend.py::SdkOutcome` and `invocation_sdk/events.py` (`add_denial`, `add_sdk_permission_denial`, `add_tool_use`, `add_tool_result`, `write_event_log`). Recorded 2026-08-19 by `U-docs`. **Amended 2026-08-27 (`U-corrob`, `S-53`):** the consumer clause is closed. *"Nothing reads them"* becomes: **exactly one consumer reads them, in memory, off the `SdkOutcome` at the call site; no written event-log file is ever read back** — which is why `test_ev4_nothing_in_the_package_reads_a_tool_events_file` keeps its name and its bytes. The standing constraint is unchanged and was re-derived rather than assumed: the filesystem diff remains the authority, and `UN4` makes it a test (a corroboration mismatch leaves `RunResult` byte-identical). The operational consequence this row already named — the §5.4 burn-in gate reading `denials` **and** the filesystem diff — now has an automatic instrument instead of a manual procedure that `fixtures/trials.md` shows was never run. |
| S-45 | **The miner-reader's CLI argv carries `--strict-mcp-config`, and `Containment.strict_mcp` is `True` for that surface — the two are one decision, landed as a matched pair.** `miner.build_reader_argv` gains the flag as its last element (mirroring `worker.build_argv`'s own placement and rationale — the reader is allowed FEWER tools than the worker, so the case for it is at least as strong); `containment_for`'s `miner-reader` branch catches the record up to match. `test_invocation.py`'s `CN10` `iff` is what enforces the pairing mechanically: landing either half alone reddens it, in opposite directions. | `U-sdkr` spec §3.2 (`Fix-1`/`F-a`/`F-b`/`F-c`, `docs/specs/self-learn/drafts/u-sdkr-reader-contract-spec.md`), landed in the same commit as the build. Recorded 2026-08-19. |
| S-46 | **A surface's SDK containment is pinned from the `SessionSpec` its real call site builds, never from `containment_for`'s defaults.** `U-seam`'s `CN2` established this discipline for the CLI path (assert on what the call site actually sent, not on a hand-constructed containment); this unit extends the same discipline to `options_kwargs` on the `sdk` path — the reader's containment and its `options_kwargs(...)` mapping are both asserted from a captured, real `_invoke_reader(home, prompt)` call, never from a hand-built spec. | `U-sdkr` spec §3.3 (`Cont-1`/`C-a`, `docs/specs/self-learn/drafts/u-sdkr-reader-contract-spec.md`), landed in the same commit as the build. Recorded 2026-08-19. |
| S-47 | **Backend defaults are PER SURFACE, and the analyst is the first surface whose default is `sdk`.** `invocation/contract.py`'s new `DEFAULT_BACKEND_FOR_SURFACE` table gives rung 5 of the precedence chain a per-surface answer instead of the single hardcoded `"cli"` every surface previously fell through to — `worker`/`worker-repair`/`miner-reader` keep `cli` (their own wave has not arrived), `analyst` flips to `sdk` (attended, lowest volume, and the flip closes the F3 argv-exposure fix at the same time). `SELF_LEARN_BACKEND_ANALYST=cli` is the operator-facing rollback switch, recorded alongside `S-35`'s rung-precedence policy — using it re-opens the F3 exposure it was built to close (FW-111). | `U-sdka` spec §7.5 (`docs/specs/self-learn/drafts/u-sdka-analyst-flip-spec.md`), landed in the same commit as the build (same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30/S-31/S-32/S-33). Recorded 2026-08-19. |
| S-48 | **The invocation seam never raises, on any surface, for any member of `FAILURE_KINDS` — `U-seam`'s one documented exception (`T-c`, the analyst's bare `OSError`) is retired by FW-87.** Closing the last carve-out completes a contract `U-seam` stated but did not fully deliver: every `Backend.text_session`/`write_session` call now returns an `Outcome` whose `failure` member is always one of `FAILURE_KINDS`, never an uncaught exception escaping to the caller, on either the `cli` or `sdk` transport, on all four surfaces. Measured empirically by the build's own `M11` mutation self-check: reverting `TRANSPORT["analyst"].catches_os_error` to `False` while leaving `analyst.analyze`'s naive `except OSError` catch in place still leaves the sdk leg's hole open, because the sdk transport's failure mode is `claude_agent_sdk.CLIConnectionError`, not `OSError` — proving the contract had to close at the transport-table level, not the caller, for this row to actually hold. **N2 (2026-08-25, U-cleanup S-49):** this row's `M11` evidence cites `TRANSPORT["analyst"].catches_os_error`; U-cleanup trims `TRANSPORT` to a plain `dict[str, bool]`, so the same evidence now reads `TRANSPORT["analyst"]` — the symbol renamed, the row's substance unamended. | `U-sdka` spec §7.5, same commit as the build (same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30/S-31/S-32/S-33/S-47). Recorded 2026-08-19. |
| S-49 | **The `cli` invocation backend is RETIRED: every surface runs on the Agent SDK, and there is no second transport.** `CliBackend`, `invocation/cli.py`, the CLI-only fields of the `TRANSPORT` table (`kind`, `kills_process_group`, `prompt_via_argv`, `result_stdout` — the table itself is TRIMMED, not deleted), the three argv builders (`worker.build_argv`, `analyst.build_argv`, `miner.build_reader_argv`), the two settings writers (`worker.write_settings_file`, `miner.write_reader_settings`) and the two `SessionSpec` closures (`cli_argv_builder`, `cli_settings_writer`) are deleted; `SessionSpec` carries a first-class `doctrine` field in their place. `KNOWN_BACKENDS == ("sdk",)`, and a `cli` selection at any rung is a NAMED REFUSAL — `BackendUnavailable` carrying a retirement message, surfaced as `Outcome(failure="unavailable")` through the surface's own log template, never raised. **The precondition changed under this decision and the change is recorded, not hidden:** the plan gated retirement on "14 consecutive all-sdk days"; on 2026-08-24 the user waived the soak — *"as long as we have decent test coverage, then skip the soak; 2 weeks is crazy-town"* — and the bar became a measured coverage census (spec §3), which found the plan's own T3-armor figure wrong (142 bash-shim tests, not 88; 43 cross-backend contract pairs, not 16) and found ≈90% of the armor tests are worker/repair/attribution behaviour tests rather than transport tests, so they were migrated onto the SDK fake CLI, not deleted. One real loss was priced, after two misidentifications worth recording: the census first named `test_ou3_failure_legs_render_byte_identical_to_clibackend…` as the dying cross-backend oracle, but read line by line that test asserts no differential at all; `test_worker_contract.py::test_fl2_byte_identity_and_provenance[sdk]` is the real one — it drives a fresh CLI pass from inside the `sdk` leg and asserts the two backends' log lines are byte-identical on timeout, not-found and unavailable. That comparison is what the deletion costs; the test's provenance-and-shape assertions survive on the `sdk` leg alone, replaced by a byte-pin over every row of all three template sets, captured while `CliBackend` still existed. **`TRANSPORT` was NOT deleted:** its `catches_os_error` field is folded by `invocation_sdk` and is the table-level mutation point `S-48`'s recorded `M11` evidence depends on, so the table was trimmed to that one surviving field rather than removed. One production datapoint stands behind the flip and is not more than that: the first SDK miner run, 2026-08-24 19:20–19:24 PDT, "ok — 7 landed", 4 m 07 s, 586 MB peak, 0 orphans, volume in band. **The cost, stated plainly: rollback is no longer an environment variable. It is a revert** — `17-invocation-runbook.md` §6 says so in its own voice. | *Amends `S-43`* — `claude-agent-sdk` moves from optional extra to hard dependency; the extra is retained as an empty alias so `pip install 'self-learn-cli[sdk]'` keeps working. `S-43`'s premise was "a machine that never leaves `backend=cli` pays none of it"; there is no such machine after this row. *Amends `S-39`* — its first consequence ("the default at every rung stays `cli`") lapsed at U-flip (2026-08-23) and is now structurally impossible. `S-34`'s seam, `S-35`'s precedence chain, `S-47`'s per-surface defaults and `S-48`'s never-raises contract all survive unchanged — the chain now resolves one backend instead of two. `S-48` survives because `TRANSPORT["<surface>"]` survives (in trimmed form): the retired-backend refusal is raised inside `registry._resolve` and caught by `_dispatch`, so nothing escapes the seam, and the analyst's OSError split remains mutable at the table where `M11` measured it. `docs/specs/self-learn/drafts/u-cleanup-cli-path-removal-spec.md` §14, U-cleanup-B build. Recorded 2026-08-25. |
| S-50 | **The two SDK engines share a LIBRARY, not a monolith; the two charters stay two; and a long-lived host process becomes the library's third consumer.** Four framings were put to the user: (1) let the two engines diverge — they serve different clients, so two implementations is not obviously wrong; (2) force unification on cohesion grounds — one engine, one code path; (3) rebuild it the way it should have been built — discard both and write the thing once; (4) break the reusable MECHANISM out of each engine into a shared library — extensible, and eventually portable to other projects — rather than a monolith. **The user chose (4).** The two CHARTERS stay separate: their decision tables are disjoint (measured similarity of the two `build_can_use_tool` bodies is 0.333, and the 31 shared lines are callback scaffolding, not policy); merging them would put read-scope logic on the write-security path. The MECHANISM under them is where divergence is dangerous, and not because it has drifted — measured, it has not: the entire delta between the two `_log_abandoned_disconnect` copies is the intended log-sink/message-prefix difference, branch for branch, and the three differences between the two kill ladders are each stated and deliberate. **The danger is that the two copies are correct today and UNWATCHED**: nothing pins them to each other, and 18 of the 24 operator-visible messages they emit were asserted by no test at all — 10 of the 13 shutdown messages included. The `ResultMessage` error-detail chain (`errors` joined by `"; "`, else `result`, else `subtype`) is skeleton-identical at 1.000 across the two engines, differing only in two identifier names — the clearest single instance of one mechanism written twice. Scale: the library region measures 405 raw / 287 code lines of 2286 raw / 1286 code across the two engine trees — 22.3% of code lines. Refused, explicitly: option 2 (one engine) and option 3 (rebuild) — measured, the two engines share zero body-identical top-level functions or classes and 3 of 20 `ClaudeAgentOptions` keys; their session lifetimes are incompatible (the pane holds one multi-turn `ClaudeSDKClient` across `send()` calls; the seam does connect-query-drain-teardown once) and two of their shared option keys are semantically opposed (`system_prompt` replaces vs appends; `include_partial_messages` `True` vs `False`). **The long-lived host process is ADOPTED.** User ruling, 2026-08-26 22:01, verbatim: *"adopt the host process. amend the spec accordingly."* This REVERSES the r1 recommendation of this spec, which had ruled the daemon out — the reversal is recorded, not hidden. What r1 got right and still stands: the nightly miner-to-worker handoff never completing has a small cause (`self-learn-miner.service` is `Type=oneshot` with `KillMode=control-group`; `worker._spawn_window`'s `start_new_session=True` escapes the process group but not the cgroup) and three cheap remedies (`KillMode=process`, `systemd-run --user --scope`, or a chained `self-learn-worker.service`). What r1 under-weighted, and what decided it: portability — self-learn is going to other users' machines, and what they trip on first is not the SDK, it is three systemd units (`self-learn-miner.service`, `self-learn-miner.timer`, `self-learn-ui.service`) plus a covert any-verb miner watchdog (`miner.maybe_kick`, ticked by every CLI verb but `mine`/`init`) that silently `setsid`-spawns a real mining run once the last completed mine is over 24h old — invisible until it bites, and it bit during this campaign (the FW-116 rollback dry-run's first `doctor` call landed five real commits in a throwaway ledger copy before anyone noticed). A `self-learn serve` process under ANY supervisor is the only shape that ports cleanly; fixing the cgroup reap with a `KillMode` line fixes this machine and ports nowhere. Weighed pros: one thing to supervise instead of three units plus a hidden watchdog; portable to launchd or a bare terminal (the existing `self-learn-ui.service` already documents exactly that fallback); the miner-to-worker handoff dies by construction, since both jobs are scheduled by one live process; the watchdog stops being covert. Weighed against, and answered: daemon state is not crash-safe — answered by keeping every existing file-plus-flock as the source of truth, the daemon schedules, it does not remember; daemon-down means nothing runs — answered by `Restart=`, a heartbeat file, and a staleness alarm that lives OUTSIDE the daemon, so a dead daemon is LOUD; `test_lock_invariant.py` gains an entrypoint — true, real work, budgeted and named; portability for non-systemd users is the argument FOR, not against. **The invariant this must not break, and does not:** `13-hosting-and-separation.md` §5/§8 H-5 — the ledger repo needs no autosync watcher, ever: producers commit their own writes. `serve` is a SCHEDULER of producers, not a watcher — it starts `miner.run` and `worker.run` as jobs; each job still takes its own `commit_lock` and commits its own paths under its own pinned subject, exactly as today. | `U-engine` spec §3, in full (§3.1 the four-option choice, §3.2 the host-process adoption with its pros/cons), same disposition-rule obligation as S-24/S-25/S-27/S-28/S-30/S-31/S-32/S-33/S-47/S-48/S-49. User rulings 2026-08-26 21:10 (*"we deal with the engine unit first"*) and 22:01 (*"adopt the host process. amend the spec accordingly."*). Landed in the same commit as `U-engine` Phase 1. Build tracked as FW-118 (library, Phase 1) and FW-119 (host process, Phase 2). Recorded 2026-08-27. **Amended 2026-08-27 (gate r1 M-3) with the shipped `serve` surface:** `self-learn serve` (`cli/src/self_learn/serve.py`) — a plain synchronous scheduler loop, no event loop of its own; runs `miner.run`/`worker.run` as SERIAL jobs, one at a time (spec §5.2a), each still committing its own writes under its own lock and pinned subject (H-5 unaffected); the any-verb watchdog (`miner.maybe_kick`) is REDUCED, not retired — a fresh `serve` heartbeat turns a would-be detached spawn into a poke (spec §5.3); `doctor` gains a `serve` row with four verdicts (`SKIP`/`FAIL`/`PASS`/`FAIL`) read from a heartbeat file `serve` writes into `cache_dir()`, never the ledger, with the staleness alarm living OUTSIDE the daemon by construction; register item #11 (the miner-to-worker handoff never completing under a cgroup reap) is SUPERSEDED, not fixed — one live process starts both jobs, so there is no detached child to reap. Shipped as `systemd/self-learn-host.service`, linked but not enabled by `install.sh` — enabling it, and disabling `self-learn-miner.timer`, is a per-host operator decision, not a code default. |
| S-51 | **A canon host's version-control posture is a PROPERTY OF THE HOST, set once at registration: `git` (default; today's behaviour, byte-for-byte) or `plain` (no repo, no commit, no push).** Measured at `50fa815`: of 94 git call sites in the CLI, **38 are host-side, all in `verbs.py`, held by 11 functions**; of the eleven host-side operation classes, exactly two — the host commit and the host push — deliver anything, and the other nine are gates and plumbing that exist because those two do. Three of nine registered hosts are git repos ONLY because self-learn requires it, two of them carrying hand-written deny-by-default `.gitignore` files, and **three of nine have no remote at all**. **The user's own framing is adopted as the mechanism:** integrity moves to a ledger-side **compile record** — per target, the sha256 of the region the ledger says must be there, plus the hash OBSERVED ON DISK immediately before this write (never a stale prior entry's own expectation — REC13/M47) so an unlanded apply is distinguishable from a hand edit — written **inside the resolution's own ledger commit**, so it opens no new failure window. A route against pre-existing marker-bounded content with no record yet self-adopts — writing the missing record entry and proceeding, with one printed notice — only when that content is byte-identical to what the compiler would currently render from the ledger; anything else still refuses and names `recompile --adopt` as the consent step. It is *stricter* than `git status` for the region self-learn owns, because it sees a **committed** in-marker hand edit that git is structurally blind to, while not refusing over out-of-marker edits the compiler provably preserves. Its promise is scoped honestly: the ledger holds self-learn's region, **15.8%** of the bytes of the 16 files carrying one (44 654 of 283 204; **18.4%** across the 13 that are routable targets rather than worktree checkouts), and never the human's remainder — so `git` stays available and stays the default. **The overload this retires at the root:** `TargetSpec.host_repo is None` already meant "chezmoi user scope" in 17 shipped sites, five of which would have misrouted a plain project host into the chezmoi branch — so the field is renamed `host_path`, a `mode` field carries the posture, and **user scope becomes a first-class plain host** (`~/.claude`, measured: no VCS, and the largest managed section on the machine) whose write goes through the same plain path as every other plain host — and `chezmoi.py` is deleted wholesale, CLI and UI adopt surfaces included, chezmoi having been retired 2026-07-24 with zero `chezmoi-adopt` uses in 380 ledger commits. **Refused, so they are not rediscovered:** a global-only switch (this machine needs both values at once); auto-detection by `.git` presence (behaviour changing without a decision is what `S-10` was written against, and it is irreconcilable with Y-17's disclosed-consent contract); try/except at each of the 42 host-side sites (it destroys the exit-6-vs-7 distinction dispatch depends on); a second ledger commit for the record (its failure is post-mutation and would return exit 6 asserting, falsely, that nothing was written). **The one thing plain mode must not lose:** the `not a git repo` refusal is currently the only guard **on the skills-root legs** stopping a hand-edited hosts.yaml typo from getting canon written into it (a typo'd *projects* entry is caught first by `_project_host_or_refuse`'s registration match), so plain mode ships a replacement predicate — a `.self-learn-host` marker written by `host add --mode plain` — not an omission. No amendment to any existing row is owed: `S-6`, `S-10`, `S-12`, `S-17`, `S-23`, `S-50` all survive verbatim. `S-17`'s D3 (*"pushes are MANUAL"*) is reinforced: a plain host has nothing to push. | `U-hostmode` spec §3, in full. Provenance: this spec; user ruling 2026-08-26 16:20 and the same day's framing question; the concrete instance is `~/repos/3d-printing`, git-inited `d4f7c17` 2026-08-25 solely to satisfy the requirement. Both phases (Phase 1: the mode carrier, the compile record, plain-mode user scope; Phase 2: `chezmoi.py` deletion, UI adopt surface) landed under this unit — see `FW-122`. |
| S-52 | **Ancestor canon is INHERITED, read-side only; the already-canon scan reads whole canon files, not a marker window.** Measured 2026-08-27 on Claude Code `2.1.250`. **Instrument: the `InstructionsLoaded` hook** — a project-scope hook in a throwaway tree outside every registered host, appending each event's `{file_path, memory_type, load_reason}` to a file. It reports that Claude Code loads `CLAUDE.md`, `.claude/CLAUDE.md` and `CLAUDE.local.md` from the cwd **and every ancestor directory**, each as one `load_reason: session_start` event (`memory_type` `Project`, or `Local` for `CLAUDE.local.md`), with `~/.claude/CLAUDE.md` arriving separately as `User`; **the git root is not a stop** (markers two levels above it fired). **Descendant** files load only on demand, as `load_reason: nested_traversal`, once their directory is read; **sibling** subtrees never load. That descendant/sibling result is a real negative, not instrument blindness: the **positive control** is a second run that reads a file inside the descendant directory and makes its `CLAUDE.md` fire with `nested_traversal`, while the sibling stays silent in both runs. **Two things were NOT measured and are doc-sourced only: the concatenation ORDER of the loaded files** (the hook's emission order differed between two runs) **and the walk's terminus** (`/` vs `$HOME`; the tree lived outside `$HOME` and every ancestor still loaded, so the walk is at least not `$HOME`-gated — and both live ancestor pairs are one level apart under `$HOME`, so no terminus can matter). `--debug`/`--debug-file` were tried first and expose **no** loaded-file list on this build; an earlier draft claimed the session transcript was the instrument, which is **false** — 2.1.250 does not write loaded instruction files into the transcript, and the marker tokens appear there exactly once each, in the assistant's own reply. This corroborates deferred record `lrn-b21d1969` (doc-sourced 2026-08-25) for everything but its ordering clause; its `--add-dir` clause remains untested. **Consequences, all read-side:** (1) a registered ancestor host's `CLAUDE.md` is canon that already loads for every child session, so `gates.g0.canon` may fire on it, and the analyst is handed it as a labelled *inherited* block; (2) `selfcheck._loaded_surface` gains the registered ancestors' `CLAUDE.md` as members for project scope — the "Model B remap" its own docstring anticipated; (3) ancestry is **derived** from `hosts.yaml` by resolved-path prefix arithmetic at read time, **never persisted** and never a bucket key (bucket identity stays `(scope, name)`); (4) an **unregistered** ancestor stays a fact told to the human (routing-doctrine §3) — its path is reported, its bytes are never read; (5) a host with a registered ancestor or descendant gets a base-naming sentence in its compiled pointer block, because all three live blocks are byte-identical today and two of them load in one session. **The already-canon scan is widened in the same unit and for the same reason:** `canon_excerpt` returned the marker window ±20 for files ≥200 lines, which is **11.5%** of the k1c host's `CLAUDE.md` (2,923 of 25,499 B); `lrn-bcf6b3e7` had 4 of its own 5 `t3.scan_terms` in that file and 0 of 5 in the excerpt, and the covering hand-written rule sat **188 lines above the excerpt window** (208 above the marker) — the human graduated it by hand. The analyst now reads the **whole** own-host file plus `<host>/references/**`, capped and byte-reported (BR-1/BR-3: 32 KB/file, 64 KB/record, depth 2), and **nothing outside `canon_read_roots`' project family** — the whole-host-tree read, priced at **43.3 MB / ~10.8 M tokens** across the 9 hosts, stays refused and user-ratifiable per G-3's 2026-07-17 note; `<host>/CLAUDE.local.md` also stays out, because it is git-excluded by design and not in `canon_read_roots`. **This row SUPERSEDES `u-marker-excerpt-case-spec.md` §3 criterion A's A3 leg ONLY** *(CORRECTED-r3, gate r2-N2: r2 said "criterion A", which reads as retiring A1/A2 — i.e. as dropping the requirement that the compiled section reach the analyst at all, the opposite of what this unit does)* — A3's exact-window equality (`excerpt == lines[begin-20:end+21]`) is obsolete once the whole file is read, while **A0 (the ≥200-line / begin-index>60 fixture guard), A1 (both markers present) and A2 (`entry_line(R)` present) are PRESERVED and re-asserted against the whole-file contract**. **Criterion B is re-homed, not retired**: under the byte cap the compiler-written managed region is always retained, and it is located by the imported markers **case-sensitively**, so a case-variant the compiler never wrote cannot capture the retained window. Four shipped tests in `cli/tests/test_worker.py` pin criterion A by exact list equality and are rewritten in the same commit; that file is armor (`test_worker_contract.py::_ARMOR_SHAS`) and its sha is re-pinned with this row as the reason. **A hand-written mention is not a rule:** a `g0.canon.target` outside a managed section requires the `already_kept` card section and the `canon-hand-written` flag, no verb auto-resolves on it, and a hit found only in a `references/` file may never set `already_canon` at all — references are the DEMAND shelf, reached by a pointer, not loaded. **Refused here, by name:** umbrella-as-destination (FW-125), any new verb or `--dest` grammar (U-verbs owns it), and widening the worker's title-overlap recurrence basis across the ancestor edge (FW-127). | Loading rule sourced from `code.claude.com/docs/en/memory.md` + `large-codebases.md` + `agent-sdk/claude-code-features.md` (stopping point and ancestor `.claude/CLAUDE.md` both **docs silent**), then measured with the `InstructionsLoaded` hook and a `nested_traversal` positive control. Census, retro-measure and budget figures in `drafts/u-ancestry-ancestor-canon-spec.md` §2–§3, all reproducible command outputs; instruments in that unit's `misc/u-ancestry-measurements/`. |
| S-53 | **The consumer of `tool_events`/`denials` is an IN-MEMORY corroborator that emits COUNTS, and nothing else — no file is ever read back, nothing reaches the ledger or the UI.** `S-44` reserved the consumer for this unit and fixed its constraint: the filesystem diff remains the authority. Measured at `50fa815` before deciding: the whole retained corpus is **42 files / 128 lines / 70 tool events / 16 denials**; **every denial in it is on the `analyst`, is `Bash`, and falls in one 13-minute window on 2026-08-21** (8 of 8 consecutive runs, each recorded three times — charter, `sdk-result`, and the paired `tool_result`); `worker`, `worker-repair` and `miner-reader` have recorded **zero denials ever**, and capture is uniform across surfaces (`backend.py:213`), so that zero means the charter never denied there — subject to two caveats, that 16 of 21 worker files are fake sessions that never request a permission and that `SELF_LEARN_ENFORCE_SCOPE=0` returns ALLOW before any deny path (`charter.py:211`); `FW-107`'s line — the product's only shipped consumer — **has never printed** (`grep -c` 0 against 168 readable `worker.log` lines, with its own positive control); and the one real worker run carrying tool events **agreed exactly** with the filesystem census (8 staged files, 8 distinct accepted paths, 0 outside the stage). **What decided the unit was not that evidence but its absence elsewhere:** `17-invocation-runbook.md` §5.4's *"0 out-of-scope write attempts (`denials` empty AND filesystem diff agrees)"* gate is prose with no instrument, and `fixtures/trials.md` records **no burn-in entry for any SDK surface** — so the check became automatic and per-run instead of manual and never-executed. **Four consequences are permanent.** **(1)** A mismatch is a LOG LINE, never a status: `RunResult` is byte-identical on a mismatching run (`UN4`) — corroboration may report, never adjudicate. **(2)** Three counting rules, each adopted because the obvious alternative **fires on a correct run** — the class that would have made the instrument worthless. Accepted writes are counted as **distinct resolved paths**, never as events (a `Write`-then-`Edit` on one staged file is two events for one file in a flat `staged_paths()` listing). The reader's filesystem side is **`after − before`** over two recursive snapshots taken around the session, never a flat listing (an existing passing test plants two spool strays before the run, and the `:771-772` early return leaves residue the corpus has twice produced). And an outcome whose `tool_events` attribute exists but is **empty** yields a NO-EVIDENCE line rather than a mismatch, while an outcome with **no such attribute** yields nothing at all — two flags, because one could not tell those apart. The fourth false-alarm source was a **fixture bug**, not a design choice, and was fixed at the root: `_scenario_shim_script` performed every write but announced only the first, so a correct two-write run reported one. **(3)** Surfacing events into `report`, the UI, or a record is REFUSED, not deferred (`FW-129`): the product's only scrubber returns **0 hits over the entire corpus while 65 occurrences of the user's home path sit inside it**, so reusing it at a boundary would be a green light on exactly what `FW-106` named; and reading a written log back would require re-pinning an armor-sha'd test file. **(4)** An event-derived field in the decision trace is refused permanently — the trace is published, and writing the model's self-report of its own tool calls into the durable provenance record is precisely what `FW-84` cost; `S-32` already proves the same thing structurally. **Placement is part of the decision:** the module is `src/self_learn/corroborate.py`, at top level, because it consumes OUTCOMES and is not part of the invocation seam — and because `invocation_sdk/` is pinned to exactly six modules by an armor-sha'd test (`test_pl1_…`), so putting a consumer inside the seam would have cost the very armor motion consequence (3) prices as prohibitive. | `U-corrob` spec (`docs/specs/self-learn/drafts/u-corrob-tool-events-consumer-spec.md`), landed in the same commit as the build (same disposition-rule obligation as S-24/…/S-50). Recorded 2026-08-27. |
| S-54 | **The ledger's verb surface covers its own state machine: one guard vocabulary, one filing-move operation reaching every scope, one batch executor, one preview, one flush epilogue, and real retirement for references.** Measured before the build (code at U-hostmode Phase 1 `b206800`, ledger at `4444be7`): **10 of 12 record-verbs** already route their status precondition through `ledger_ops.require_status`, and `rehome`/`rescope` are the two holdouts (`verbs.py:4281`, `:4467`) carrying FW-51's exact shape — so the move work also removes the last two hand-rolled checks. **The move matrix is completed, not extended by a third verb:** `rehome_record` and `rescope_record` were two half-complete implementations of one operation (one rewrites `scope:` and never stamps `meta.yaml`, the other the reverse), replaced by one `move_record` that writes `scope:` unconditionally from the landing bucket and stamps `meta.yaml` iff the target is a project bucket. Both verb names are retained over one widened `--to` grammar; u-rescope §3 rationale 1's collision hazard is **measured away** (12 of 12 live slugs match `^-.+-[0-9a-f]{8}$`, so `user` and `skill:` are unreachable as slugs). `skill:<a> → skill:<b>` is ruled **IN**, closing **FW-114**; **FW-115** closes with the ownership ruling it asked for — the widening, not a third verb. **`batch` is the one executor the review skill calls** — 42 verb calls across two hand-written scripts re-derived the same scaffolding twice — and **the eight-integer exit contract gains exactly ONE integer, `EXIT_BATCH_PARTIAL = 8`** ("batch completed; N applied, M refused; the ledger DID change"), under a decision procedure in which `1` is emitted only when nothing landed. **Idempotence is defined and TOTAL** for all 18 permitted verbs by a state read, with a sheet-line key on `note --append`; a second run of an applied sheet applies 0 items and exits 0. **`route --dry-run` renders the bytes the compiler would write**, computed from U-hostmode's own `_expected_*_region` helpers. **One frontmatter key is added, not two:** `history`, append-only, closed `event` set `{resolution, routing}`, with `clear_resolution_note` refusing unless the note is already in it — so the write-once field can be displaced but never destroyed. **The 11 §4.2 flush rule moves to ONE place:** measured, it is hand-copied across **six** call sites in `cli.py`, so `cli._mutating_epilogue` becomes the single seam and `batch` joins it. **`reference` retirement becomes real** — exposure **27 of 92 routings across 7 files**, live instances **0**, which is why this row is also its ratification. **`host remove` refuses** while routed records target the host, with `--gate-only` as the explicit override and **no `--retire`**, because `graduate` and `supersede` are both false statements about a deregistered host's records. **Producers get the resolution verbs' contract, not new integers:** `--json` outcome envelopes, exit codes byte-unchanged. | `U-verbs` spec (`docs/specs/self-learn/drafts/u-verbs-ledger-verb-completion-spec.md`), spec landed 2026-08-28. Four blind spec gates (r1 0/9/10/6 → r2 0/4/6/0 → r3 0/3/3/0 → r4 0/1/3/0, all folded, the fourth explicitly "no further round"); 80 criteria across two phases (56 [A], 24 [B]), 80 mutations, every number a quoted command output. Reserves `FW-133`-`FW-138`; `FW-85`/`FW-114`/`FW-115` take their dated dispositions at build time — `FW-114`/`FW-115` close, `FW-85` names a different exit-code surface this spec never touches. **Phase 1 (T2) landed** `9c7ebdd` (batch, move, show, dry-run, reopen/undefer/note, the epilogue, producer envelopes) — all 56 [A] criteria. **Phase 2 (T2) build complete** (2026-08-29, uncommitted — worktree rule, not a commit reference; pending its own blind code gate): all 24 [B] criteria — `reroute` (RER1-7, the shared retirement path now covers reference-routed records for `graduate`/`supersede` too), `host remove`/`bucket prune` (HOST1-5), `followup add`/`reclassify` (META1-5), and the UI's G10 parity + keymap/notice generalization (UIP1-5) — each with its own named discriminating test (`cli/tests/test_u_verbs_p2.py`, `ui/tests/test_u_verbs.py`) and its own mutation (M41-M62) run RED then restored, sha256-verified. |
| S-55 | **A test-file guard pins an INVARIANT, not a snapshot: every armor literal is measured on the ANCHOR side, fixtures are protected whole-file with a dated re-pin door, behaviour files are protected by an anchor-side census of EVERY top-level AST node compared by normalized dump, and advancing the anchor is the landing chain's motion, never a builder's.** Measured before the build (code at `3b8e037`): the shipped `_ARMOR_SHAS` hashes the WORKING TREE, so **6 of its 7 pins are exempt from the base-diff half entirely** and assert only "unchanged since the last unit re-pinned it" — producing **41 pin writes across 15 commits and 9 units in 30 days**, of which **28 writes across 12 commits fell in the last 7 days**. The toll per unit is measured: **FW-117** added **166 lines to three armor files** to delete one dead function whose own production diff was 22 lines, and **U-hostmode Phase 1** added **178**. `_AR1_SANCTIONED_PIN_LINES` is a **396-entry literal holding a verbatim copy of every line ever added to `conftest.py`** — 15.5% of `test_u_sdka.py`. The pin set was never derived: `support.py` (**62 importers**, the most depended-on module in the CLI test tree) has **no armor**, while `conftest.py` (2) and `backends.py` (3) are whole-file pinned. And the bookkeeping has decided product questions three times, each recorded in the tree: a shipped `report.py` docstring naming the armor as the reason for an implementation choice; FW-129 listing a re-pin among a deferral's costs; and FW-131's analyst fix *"reverted rather than shipped"* against a zero-headroom `hy5` ceiling, landed only after a coordinator ruling that *"the cap … is armor BOOKKEEPING, not a design constraint"*. **The decision:** one module (`cli/tests/test_armor.py`), one table (`ARMOR`), one anchor (the first-parent PARENT of the latest first-parent merge). **Fixtures** — derived by import graph, including `support.py` — are byte-identical to the anchor, with a single `repinned` door carrying a sha and a dated reason; an earlier draft let them grow additively instead, and that was **measured** to admit an appended module-level rebinding on all three. **Behaviour files** are protected node-wide: **627 top-level nodes** across eight files — 366 tests, 93 non-test defs, 40 module constants, 8 classes, 120 imports — each keyed by name and compared by normalized AST dump, because a census of test functions alone was **measured** to miss a mutated constant read by 10 tests and a gutted helper called at 22 sites. A unit may ADD nodes freely and may not DELETE or EDIT one without an entry in `missing` / `edited` / `edited_exports` whose reason cites a spec section and carries a date, and which cannot outlive its subject. **Deletion stays legal; it stops being silent.** | `U-armor` spec §2 (the census), §3.2 (the recommendation), §4.7 (the disposition of all 17 existing mechanisms), §3.5/§3.6 (two blind spec gates, 2 blockers and 20 majors folded). Reopens if a fourth armor mechanism is proposed outside `ARMOR`, or if a measured evasion passes all of `F1`-`F3` / `B1`-`B7`. Reserves `FW-140`/`FW-141`; `FW-139` is deliberately skipped (a tombstone in the `U-verbs` spec). Five blind spec gates (r1 1/10/5 → r2 1/10/7 → r3 1/6/3 → r4 0/2/4 → r5 delta **SOUND** 0/0/2, all folded); 42 criteria, 59 mutations, every number a quoted command output. Recorded 2026-08-28. |
| S-56 | **Landing is a program in the repository, not a shell chain retyped per unit.** Every merge to `master` runs `plugins/self-learn/cli/scripts/land`: preconditions (main checkout, clean trees, `git fetch origin master`) → `merge-tree` preview → `merge --no-ff --no-commit` → a **named** resolver, never an inferred one → landing checks and the armor re-measure **inside** the merge commit → suites → a sanitize scan of the outgoing diff's **added lines** → `push origin master` → prune. Each stage is fail-closed with its own exit code (2-7), and each refusal path has a test observed to fire. Corollaries: **(a)** conflict resolvers are named on the command line and unit-tested on synthetic conflicts built by a real `git merge`; **(b)** the gate verdict is an **argument**, never an inference — the runner asserts its shape, not its truth; **(c)** the runner has no `--force` capability at all; **(d)** `land` is operator tooling under `scripts/`, not a product surface under the CLI; **(e)** **there are no path exemptions in the sanitize gate, ever** — a standing per-path skip is the hole the 2026-08-27 incident lived in; adjudication is per-line, content-bound and auditable (`--sanitize-ack '<file>:<line>:<sha256>=<reason>'`), and this unit's own landing is the mechanism's first worked example; **(f)** a docs-only landing (every changed path under `docs/`, detected with `--no-renames`) runs the landing checks, the sanitize gate and a derived doc-reading test set instead of the full suites; **(g)** every mutation labelled MEASURED carries a runnable command in the spec's ledger, re-run at build start, and the build refuses on any that does not reproduce. | Three landing-chain defects on 2026-08-28, each caught only after the fact and each fixed forward — so each still in a public history: a newline-chained script pushed an unfolded spec carrying **6** absolute home paths (`8d716ff`; fix-forward `104f6db`); an armor pin re-derived **after** the merge commit shipped stale (`801c746`); and `grep -c` exiting 1 on a zero count broke an `&&` chain **between the commit and the push** (measured: value 0/rc 1 on zero, value 1/rc 0 on one). All three are one class — a check whose output is identical whether it passed or could not see its target — and a program can be tested where a chain typed from memory cannot. The docs-only lane is **KEEP by measurement**: 29 s / 354 tests against a 473 s full lane, a **94 %** saving, giving up **9.2 %** of non-skipped tests, and it runs **zero** UI tests. Six blind spec gates (r1 1/11/8 → r2 1/5/6 → r3 0/3/5 → r4 0/3/3 → r5 0/1/2 → r6 **SOUND** 0/0/3, all folded); 59 criteria, 85 mutations, every number a quoted command output. Recorded 2026-08-28. |

## Open

| # | Question | Lean |
|---|---|---|
| O-1 | ~~Resident review TUI — when?~~ | **→ settled** (2026-07-12): after M2 — the destination is recorded in `07-review-ui.md`, and **G-3 owns the build gate** (M2 shipped, worker proven). The slash-command review is v1 scaffolding; record-per-file + CLI verbs are the API, so no migration either way. |
| O-2 | ~~Auto-memory importer in v1.0 or v1.1?~~ | **→ settled as S-14** (2026-07-12): v1.0. The other-projects memory sweep remains this row's open extension, gated with G-2 (see the v1 territory statement, `01-architecture.md` §2). |
| ~~O-3~~ *(settled 2026-07-15 — doc 12 ratified)* | **SessionEnd candidate appender — ship at all?** | Original answer: v1.1 at the earliest, only if a month of supply-mix data shows lessons escaping. **Resolved early by user directive** ("build now without a shadow of a doubt"; same-day anecdote: 2 of the day's 3 lessons entered only because a session happened to offer). Shape amended: not a SessionEnd appender (E-11 stands — best-effort, crash-lossy) but the **nightly batch transcript miner of `12-transcript-miner.md`**, landing `source: session` records via verb-gated, capped, scanned ingest. Never load-bearing survives as invariant M-3; mined records never auto-route (M-1). |
| ~~O-4~~ *(settled — built, M1/T10 2026-07-13)* | **`/teach` slash wrapper in v1.0?** | Yes — and more than a thin forwarder: it is the **primary capture UX**. In-session Claude (holding the transcript that contains the failure) composes trigger/instruction/evidence and calls the CLI with structured flags; the CLI's small-model well-forming is the bare-terminal fallback only. The CLI path remains the contract. |
| O-5 | ~~Prune-on-route for imported auto-memory entries?~~ | **→ settled as S-13** (user, 2026-07-12): yes to both prune-on-route and prune-on-reject, as a **post-decision, post-processing sweep** — never in-flight, never inline with adjudication. |
| O-6 | ~~Model-prompted teach offers — ship in v1.0?~~ | **→ settled as S-15** (2026-07-12): yes, quality-gated — the gate is quality, not count (gen-1's ≤2-interruptions budget superseded as artificial; several serious corrections in one session all deserve offers, un-rationed). Still explicit, human-confirmed capture — S-3's rationale intact. |
| O-7 | **ha-note's fate after the backlog import?** | ***Parked* 2026-07-12 (user):** ha-note is independent of self-learn until the library is fully developed — the focus is the standalone learning tool, not claude-skills internals. No unification work before the library matures; the backlog import stays honestly one-shot; ha-note keeps working as-is. (Prior lean — thin alias for `teach --skill home-assistant --route` — preserved for when this unparks.) |
| O-8 | **Go port of the CLI/UI?** | ***Parked* 2026-07-17 (user):** the fleet sketch exists (`research/2026-07-17-go-port-fleet-sketch.md`) and stays a sketch. Not reopened unprompted — the packaging phase (G-2 note below) pursues a slim Python distribution instead; a port only re-enters if packaging hits a wall the sketch would solve. |
| O-9 | **Design round 4 — composition/aesthetics pass?** | ***Parked but governing* 2026-07-18 (user):** the four principles (compose don't stack; frame the page; progressive disclosure of chrome; one rhythm — `feedback/2026-07-18-ui-feedback-04-design.md`) **bind all new UI work now**; the full retrofit pass waits for the user to unpark it. Build shape when it unparks: design spec → build → user walkthrough as the DoD. |
| O-10 | **The CLAUDE.md→rules-file restructure** (campaign §6 question 7; blocks `U-modelb`) — routed lessons currently append prose into CLAUDE.md's managed section; the restructure would cut them over to self-learn-owned files under `.claude/rules/`, leaving CLAUDE.md to the human. | ***Deferred with a concrete trigger* 2026-08-06 (user, selecting the recommended option):** revisit at **Checkpoint A** (campaign §4, after `U-composer` lands), with its routing measurements in hand. Not to be re-asked before the trigger fires. The lean at deferral time: the cutover aligns with S-23's promotion of pathed rules to the primary cheap tier, but the campaign was deliberately built indifferent to it, and the checkpoint produces the evidence a ratification should rest on. `U-modelb` stays blocked until then. |

## V2-gated (designed, not built — triggers are observable facts)

| # | Capability | Gen-1 spec to reuse | Activation trigger |
|---|---|---|---|
| G-1 | Statistical layer: corroboration counts, reputation/outcome signals, decay clocks, quarantine machine | decisions doc §0c/§C8/§E2.2 (archived); **evaluate microsoft/SkillOpt-Sleep first** — transcript-harvest → replay-gated skill edits, Claude Code backend, shipping since 2026-07 (SOTA survey) — before building anything bespoke | A second regular human user, **or** a measured recurrence (same lesson captured ≥2× after routing), **or** >50 active routed lessons in one host |
| G-2 | Portability: manifest + capability probe + adapter tiers | archived A1/A3 + example TOMLs (the two-repo proof) *(Dated note 2026-07-18: the **packaging phase** — the roadmap's next major work — is this row's groundwork done early: D2's product-repo extraction is complete, and the SDK bundle-exclusion probe verified a slim standalone distribution is feasible (PATH-`claude` fallback works; `research/2026-07-18-sdk-bundle-exclusion.md`). The trigger below is unchanged — packaging makes self-learn installable; G-2 proper fires when a second host actually wants it.)* | A second host repo with a real user wanting self-learn |
| G-3 | Review surface (embedded agent adjudication pane) | **Re-planned 2026-07-12** *(dated update — the same-day TUI plan's terminal verdict was voided post-gate: its engine ground rested on a falsified auth claim and its trade study never priced non-TUI platforms; failure record: `reviews/2026-07-12-tui-phased-gates.md` POST-GATE CORRECTION + README ground rule 4)*. Re-derived from a holistic problem-space map (`research/2026-07-12-adjudication-surface-problem-space.md`) with the user's values routed first and **binding**: platform = **localhost server-rendered web app** (FastAPI/Jinja/vendored htmx, systemd --user, 127.0.0.1, dedicated chromeless app window); pane engine = **Agent SDK default** (original directive restored; `canUseTool` charter enforcement, empirically probed — `research/2026-07-12-sdk-pane-probes.md`); cli engine + Textual TUI = recorded alternatives (view-layer swap only). Docs renamed: **`09-surface-spec.md`** (design authority) → **`10-surface-build-plan.md`** (execution authority); all TUI-era substrate amendments (validate verb, already_canon, `--no-push`, --json hardening) carry unchanged. *(Dated note 2026-07-17: the trigger FIRED — M2 shipped + worker proven 2026-07-15, M3 complete + v1.1 tagged 2026-07-17; pre-build re-ground landed as 09 §11 (Y-1…Y-12) + 10 amendments, gate-zero blind review run NOT CLEAN → folded. One posture decision rode the fold, recorded here per P10: the pane agent's canon read scope over registered hosts is pinned to **canon surfaces only** (one CLI-owned `canon_read_roots()` enumeration), NOT whole host roots — the review judged whole-root reads a prospective loosening (`host add` consents to compilers writing canon, not a model session reading a whole tree, untracked files included). Conservative default, **user-ratifiable**: the user may widen to whole-root reads by a dated edit here.)* *(SHIPPED 2026-07-17: built by an orchestrated multi-agent run (Sonnet builders U0–U10 in per-agent worktrees per 10 §8; Opus reviews), merged to master and deployed live — the `self-learn-ui` systemd --user service serves 127.0.0.1:7357 against the real ledger. ui 481 tests / cli 894·3, pyright clean. Review discipline held: gate-zero (spec) → interim adversarial + delta → U11 live trials T-A/B/C/D/E + browser pass → final assembled-branch review + delta, all folded CLEAN; six real defects caught that green suites could not see (2 spec blockers, 1 interim join-miss blocker, 2 live-trial defects — charter double-prefixed record_id, chromium-Wayland --class quirk — and 1 final-review dead-focus MAJOR). Trials logged in `fixtures/ui-trials.md`. Residual UX backlog: pane Esc-interrupt uses the 5 s kill backstop on subscription auth; push-fail-on-successful-route not surfaced in-UI; cross-record deep-links open a new window (09 §5 degradation). O-3/O-7 metrics revisit due ≥2026-08-17.)* *(Dated note 2026-07-18: seven extension units shipped on top — U12 chat panes (Y-13), U13 idle lifecycle (Y-14), feedback rounds 1–3 (Y-11, Y-15, Y-16–Y-18), UX round 1 (Y-19–Y-21) — every unit two-gate reviewed; records under `reviews/2026-07-17-*` and `reviews/2026-07-18-*`. Counts at master d0efd44: **CLI 970/3 skipped, UI 722**. Open user rulings on this row's surface: (a) the canon-read-scope widening above remains user-ratifiable and unexercised; (b) terminal `host add` WITHOUT `--init` on a path inside a parent work tree still passes the old is-inside-work-tree validation and would commit canon into the parent repo — tighten to the Y-17 repo-root predicate? Flagged in the Y-17 fold as recorded-not-pinned; needs the user's yes/no.)* | **SHIPPED 2026-07-17** |
| G-4 | Forensic analysis layer (flag-and-store scorer → LLM drain) | archived §C2 | G-1 active **and** transcript volume worth mining |
| G-5 | znote store backend (vector dedup, DB queries) | archived B2 | Lesson volume where lexical clustering visibly misses merges |
| G-6 | Staleness revalidation: compiler/`--selftest` checks each routed lesson's referenced target (file, path, device, behavior) still exists, flagging stale canon for supersession | Copilot Memory's citation-revalidation + expiry pattern (E-21) — gate-the-read as a *complement* to our gate-the-write, not a replacement | First routed lesson observed stale in live canon, **or** a shared-repo deployment (`06-horizon.md` Stage 2 — team velocity makes canon rot faster than one person notices) |

*Horizon note (2026-07-12):* the planning horizon is **team scale** — a
shared artifact repo with ~5–6 regular users (`06-horizon.md`). That makes
G-1 and G-2 *destinations with unfired triggers*, not speculative options.
The triggers themselves are unchanged: the horizon informs design headroom,
it does not pre-fire gates.

## The register's own rule

When any settled decision's stated inputs change — a platform feature ships,
a gate fires, a review lands — the decision reopens *in this file* with a
dated note. Nothing in this corpus is shielded by its status; that failure
mode has a name now (E-4) and this table is where we watch for it.
