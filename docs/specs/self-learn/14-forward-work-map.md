# 14 — Forward work map: the potential-work register (FW)

*Authored 2026-07-18 by the orchestrator at the user's direction
("project into the future… write up substantial documentation that can
provide a deep mapping of the potential required work"). Status:
**GATED SOUND 2026-07-18** — blind Opus review NOT SOUND (F1 BLOCKER:
an early draft scheduled an automatic ledger fetch, a D3 weakening
this map has no authority to make; F2 MAJOR: a G-6 scope misquote) →
all findings folded → delta re-check SOUND. Record:
`reviews/2026-07-18-forward-work-map.md`.*

*What this is: a **map of likely required work**, with triggers,
dependencies, decision points, and done-shapes — written before any of
it is scheduled. What this is not: a build plan (no task is authorized
by appearing here), a spec (it pins nothing — items graduate into 09/10
or a new numbered doc when they become real), or a promise (items exist
to be *ready*, several exist to be *watched*, and some should die
unbuilt if their trigger never fires). Authority relationships: 03's
register wins on decision state; 09/10 win on surface truth; the README
header wins on status. When an FW item becomes real work, it gets a
spec + its own gates per the standing two-gate discipline, and this
file gets a dated disposition note on the item.*

## 0. Reading map

| Doc | Theme |
|---|---|
| `forward/supply-quality.md` | **A — Supply quality**: the miner becomes the main character (FW-1…FW-5) |
| `forward/canon-lifecycle.md` | **B — Canon lifecycle back-half**: first-firing readiness for graduation, staleness, recurrence, supersession (FW-6…FW-9) |
| `forward/packaging.md` | **C — Packaging & distribution**: the next major phase, mapped (FW-10…FW-15) |
| `forward/ui-ux.md` | **D — UI/UX**: round 4, the JS harness, backlog burn-down, composition pressure, settings surface (FW-16…FW-19, FW-30, FW-37…FW-39) |
| `forward/sync-and-fleet.md` | **E — Sync & multi-machine**: push-state visibility and the divergence playbook (FW-20…FW-22) |
| `forward/platform-drift.md` | **F — Platform drift**: the risks we don't control, and the watch protocol for each (FW-23…FW-25) |
| `forward/process-and-horizon.md` | **G — Process debt & horizon discipline**: orchestration runbook, records index, suite budget, team-scale guard rails (FW-26…FW-29) |
| `forward/worker-ecology.md` | **H — Worker ecology**: the four-worker community, its channels (field reports, briefs, doctrine drafts), the (c)-ish domain boundary, and the user's 2026-07-18 feature ranking (FW-31…FW-36) |

## 1. Where the system stands (one paragraph, pointers only)

M1–M3 shipped, v1.1 tagged; the G-3 surface is live and has absorbed
seven extension units (U12–U18, carrying Y-11 and Y-13…Y-21); the capture-and-route half
of the loop is mature and in daily solo use (README header + revision
log; 03 G-3 row for counts). The structural observation driving this
map: **every shipped round improved adjudication; almost nothing has
yet tested supply quality, the post-routing lifecycle, or distribution
— and those are exactly where the next phases live.**

## 2. The FW register

One line per item; depth lives in the theme docs. **Type** column:
BUILD (work to do), DRILL (exercise/verify something already built),
DECIDE (a user ruling is the deliverable), WATCH (monitor a trigger;
building early is the failure mode).

| # | Item | Type | Trigger / when |
|---|---|---|---|
| FW-1 | Episode-brief live verification (open U18 DoD leg) | DRILL | Next real miner cycle |
| FW-2 | Miner precision/recall observation window → tuning pass | DRILL→BUILD | ~2 weeks of journaled cycles |
| FW-3 | Rejected-proposal digest loop validation | DRILL | First rejections with notes accumulate |
| FW-4 | DP-2 live recall test (the deliberately-uncaptured lesson) | DRILL | Standing; scored when the miner meets it |
| FW-5 | Supply-mix + queue-health accounting | DRILL | **2026-08-17** (O-3/O-7 revisit) |
| FW-6 | Over-cap graduation pressure: first-firing readiness | DRILL→BUILD | First 02 §4 over-cap WARNING |
| FW-7 | G-6 staleness revalidation | BUILD (gated) | First routed lesson observed stale (03 G-6) |
| FW-8 | Recurrence resolution flows: first-firing readiness | DRILL | First confirmed recurrence |
| FW-9 | Supersession end-to-end drill (correct a routed lesson) | DRILL | First genuinely wrong routed lesson |
| FW-10 | Distribution shape | **DECIDE** | Opens the packaging phase |
| FW-11 | Versioning + release discipline | BUILD | With FW-10 |
| FW-12 | Ledger schema migration machinery | BUILD | Before any installed base exists |
| FW-13 | External-facing docs (quickstart; corpus stays internal) | BUILD | Packaging phase |
| FW-14 | Install/upgrade story + environment preflights | BUILD | Packaging phase |
| FW-15 | SDK pin drift management (verify-at-build as release gate) | BUILD | With FW-11 |
| FW-16 | Round 4: the composition/IA pass | BUILD (parked) | User unparks (O-9) |
| FW-17 | JS DOM harness | BUILD | Next; cheapest before further UI rounds |
| FW-18 | UI backlog burn-down (5 carried items) | BUILD | Opportunistic; batch with FW-17 |
| FW-19 | Detail-page information-architecture pressure | WATCH | Region count grows again |
| FW-20 | Push-state surfacing in UI (fail + ahead/behind) | BUILD | Before a second capturing host |
| FW-21 | Ledger divergence/conflict playbook | BUILD (doc-first) | Before a second capturing host |
| FW-22 | D3 posture review at fleet scale | **DECIDE** | Second host captures regularly |
| FW-23 | SDK upgrade cadence + verify-at-build standing gate | WATCH→BUILD | Any SDK bump |
| FW-24 | Native per-skill memory watch (S-1 reopen protocol) | WATCH | Anthropic ships it (E-9) |
| FW-25 | Plugin/skill format drift watch | WATCH | Claude Code release notes |
| FW-26 | Orchestration runbook, in-repo | BUILD | Soon; knowledge currently session-fragile |
| FW-27 | Review-record + research index | BUILD | Cheap; with FW-26 |
| FW-28 | Suite runtime budget | WATCH | CI wall-clock exceeds ~3 min |
| FW-29 | Team-scale guard rails (what NOT to build early) | WATCH | Standing; see 06-horizon triggers |
| FW-30 | Settings surface in the web UI (models-per-role, miner cadence, doctrine/rubric editing) | BUILD | User-requested 2026-07-18; dated addition per §7 — see `forward/ui-ux.md` §5 |
| FW-31 | Proposal-time lint: trigger recognizability + why-clause (analyst rider) | BUILD | Upranked by user 2026-07-18; supersedes cold-read at entry level |
| FW-32 | Destination-bounded contradiction check (analyst rider) | BUILD | Upranked 2026-07-18; canon-wide detection stays G-5-gated |
| FW-33 | Portfolio auditor: receipts digest + worker briefs + one-time why-audit | BUILD (deferred) | Upranked 2026-07-18; builds only when the digest is spec'd — the (c)-ish ruling |
| FW-34 | Miner near-miss visibility (+ canary recall checks) | BUILD | Upranked 2026-07-18; mostly rendering over the existing run journal |
| FW-35 | Review fast lane, stakes-tiered by destination | BUILD | Upranked 2026-07-18; hooks/user-scope never qualify — invariant, not default |
| FW-36 | Worker ecology channels: miner field reports + pane doctrine drafts | BUILD | The ecology's two new information products; constitution in `forward/worker-ecology.md` §4 |
| FW-37 | Measure per-verb latency across the remaining 39 htmx verbs, then decide indicators on data | DRILL→BUILD | Unit 2's natural first job (R-1's deferral) |
| FW-38 | In-flight state-resync envelope (`sse.py` + new route) — closes the post-reconnect silent window | BUILD | After FW-37; needs the same SSE plumbing exercised there |
| FW-39 | Reusable, project-agnostic perceptual (ARIA-snapshot) test harness, own repo — self-learn as target #1 | BUILD | After FW-37/38 stabilize the pattern in-repo |
| FW-40 | **`reference` routes but does not deliver** — add a "target reachable from a loaded surface" selftest, re-deliver the stranded records | BUILD | **DECIDE half ANSWERED 2026-08-02 (S-23 (1))** — `reference` survives and gets its pointer. The selftest half **SHIPPED** with `U-reach` (`17aa06c`): `selfcheck._check_reach`, positive control 14 of 14 unreachable. Remaining: emit the pointer (`U-pointer`) and re-deliver. No longer gates FW-35 |
| FW-41 | `analyst.analyze()` can never return `hook` — its fixed key set drops the `hook:`/`examples:` the validator then demands | BUILD | **Now.** A bug against a *quoted* user ruling (S-10 scope ruling: the knob opens "fully… Considered and rejected: splitting the knob per authorship"). Fix, do not adjudicate |
| FW-42 | User-scope destination availability — `_SCOPE_DESTINATIONS["user"]` is a one-element tuple | BUILD | **DECIDED 2026-08-02 (S-23 (2))**: user scope's cheap surface is PATHED. So the menu gains the pathed-rules destination, **not** `reference` — and the `verbs.py:950-955` refusal of `reference` at user scope **stays** (correct its stale chezmoi reason to cite S-23). Unblocked; campaign unit `U-demand-user`. See S-22 |
| FW-43 | Stale premises in the analyst's system prompt — the chezmoi ground (retired 2026-07-24) at `routing-doctrine.md:124` + `verbs.py:950-955` + `fast-lane-spec.md:39`, and the autosync claim at `:129` (contradicted by user-ratified D3) | BUILD | Now; cheap text, and it currently mis-teaches every user-scope call. **Keep the no-secrets rule** — restate it on its own merits |
| FW-44 | ✅ **FIXED 2026-08-02** (`c7b1f14`, merge `bb89e43`) — `worker.py` now imports the real `BEGIN_MARKER`/`END_MARKER` from `compilers.py` instead of re-declaring them. Row left saying BUILD while its same-day siblings FW-48/49/52 were marked fixed; caught 2026-08-03 by the citation-repair pass. The UI half is FW-48, also fixed. Managed-section excerpt marker case mismatch — `worker.py` searches `SELF-LEARN:BEGIN`, `compilers.py` writes `self-learn:begin` | BUILD | Now. Live today for any target ≥200 lines (`~/.config/CLAUDE.md`, 703) — the analyst gets `lines[:60]` and the contradiction check is blind *(2026-08-27 — **superseded in part by S-52**: U-ancestry's `SCAN1` replaces the excerpt window with the whole file under a byte cap, so u-marker §3 criterion **A3**'s exact-window equality no longer describes any code — **A0/A1/A2 survive** and are re-asserted against the whole-file contract. **Criterion B is re-homed, not retired** — it becomes `SCAN8`: over the byte cap the managed region is reserved first and located by the imported markers case-sensitively, so a case-variant the compiler never wrote cannot capture the retained window. §3.1's mutations follow their criteria: `M1`/`M2`/`M3`/`M5` are carried to `SCAN8` (as its `M35`/`M36`/`M37`/`M30`), `M4` is retired with A3. The marker-import rule this row fixed is unchanged and still binding, and `SCAN8` is now its only observable consequence.)* |
| FW-45 | Resolution observability — no telemetry event kind exists for route/reject/defer/graduate, and `routing.by` is a hardcoded `"human"` constant | BUILD | Before any autonomy-ladder measurement (12 §L1–L3): the evidence substrate those gates read does not exist |
| FW-46 | Implement S-21 — the analyst names the skill | BUILD | **UNBLOCKED 2026-08-02** — both sub-questions answered (§4): analyst proposes, CLI validates, human confirms; and the review surface gets a text field. Campaign unit `U-name`. Scope is wider than the one positional at `verbs.py:512`: **`routing-doctrine.md:262-263` still states the REVERSED pin** ("a `new-skill` proposal never names the skill") and is injected into the analyst's system prompt every run (`analyst.py:14`, `worker.py:621`), and `08-build-plan.md:469` still repeats its debunked "confirmed §4 human call" justification. Until that text changes, the model is told the opposite of the ruling on every invocation |
| FW-47 | Test-isolation residuals from the cache unit — `HOME`/hardcoded `~/.claude/CLAUDE.md` (F-4) and the module-scoped bring-up leak (F-5) | BUILD | Opportunistic. F-5's naive fix hides Chromium and silently skips 77 browser tests — pair with `PLAYWRIGHT_BROWSERS_PATH` |
| FW-48 | ✅ **FIXED 2026-08-02** (`f8d8433`) — and fixed at the root: rather than re-syncing the copied marker, `worker.canon_excerpt` became a shared public function and `pane.py`'s hand-copy was deleted. Both live victims verified rendering correctly against the real target file. A whole-repo sweep confirms zero remaining literal consumers of the legacy needle. **FW-44's defect is cloned in the UI and a green test pins it as the contract.** `ui/pane.py` re-declares the wrong marker inside a hand-copied `_canon_excerpt`, and `ui/tests/test_pane.py` hand-writes that same wrong marker into its fixture — so the test passes *because* the code is broken. This is the excerpt the **human** reads in the review pane | BUILD | With or immediately after FW-44. Fixing `worker.py` alone leaves the human adjudicating from a head-of-file truncation, which is the surface that matters most. Found 2026-08-02 while spec'ing FW-44; tracked as campaign unit `U-marker-ui`. **Added 2026-08-02 after `U-marker` merged:** `pane.py:254`'s comment — "Mirrors `self_learn.worker._canon_excerpt` EXACTLY" — was true that morning and is false as of `bb89e43`; `worker.py:53` now imports the real constants while `pane.py:266-267` still hardcodes `_BEGIN_MARKER = "SELF-LEARN:BEGIN"`. Fix the comment with the code, or it becomes an invariant claim that actively misleads. Same shape at `models.py:98-99`, which still gives the retired chezmoi premise as its reason for excluding `reference` at user scope (the real reason is now S-23). **Escalated 2026-08-02 by the post-wave code audit — this now has LIVE VICTIMS and a NEW hazard U-marker created.** Measured against a section written by the real compiler into a 284-line file: `pane.target_canon_excerpt` returns a 61-line head-of-file truncation with no markers where the fixed worker returns the correct 43-line window. Two records are `pane_truncated=True` on the live ledger right now — `lrn-b197d06b` and `lrn-be6dca06`, both project scope, both targeting a 720-line `CLAUDE.md` with one lowercase marker pair. **The new hazard:** before U-marker shipped, both halves were wrong *consistently*; now the analyst reads the managed section while the human adjudicating its proposal reads the head of the file, **with no indication they differ**. Fixing `worker.py` alone made the divergence possible. A whole-repo sweep confirms `ui/pane.py` and `ui/tests/test_pane.py` are the only remaining code consumers of the legacy needle; `test_pane.py:702` writes `"<!-- SELF-LEARN:BEGIN -->"` — a string no compiler emits — so `test_over_threshold_excerpts_around_markers` passes *because* the code is broken |
| FW-53 | ✅ **FIXED 2026-08-03** (`5fa3fbb`) — **and it was FIVE crash sites, not one.** Each was found by fixing the confirmed one and re-running the reproduction, which exposed the next: `_ledger_index`/`_canon_index` (the reported crash), `import_common.existing_origins` (unreachable until site 1 was fixed), `telemetry.read_events` and `telemetry.flush` (both on the hot path of EVERY productive run), plus `_find_record` fixed proactively because its own comment already asserted the invariant. The telemetry fixes decode **per line**, so one torn line no longer discards the whole event history. Decision: skip-and-report, not fail-run — the nightly producer degrades rather than stopping — but the skip is carried in `MineResult.corrupt_records`, every journal entry from detection onward, `miner.log`, AND the human-readable `mine status` line, never `--json` only. **Residual, declined with reason:** `flush()`'s heal-a-torn-trailing-line read of the *tracked* target still crashes if that file is itself corrupt — a different corruption source, and fixing it needs a decision about appending to a target you cannot read. Own follow-up, not folded in **A record file with invalid UTF-8 wedges a mine run — pre-existing on master, isolated 2026-08-02.** The crash is `_compose_prompt` → `_ledger_index` → `Record.from_path` → `read_text`, which runs **before** `_reconcile_and_land`, so `run()`'s outer handler turns the run into `status: failed`. Confirmed pre-existing by neutering `U-recur`'s backfill and reproducing it anyway; that unit adds no new exposure because the backfill is never reached | BUILD | Opportunistic. Same permanent-wedge class as the guard `U-recur` closes, through a door no unit currently owns. Found at `U-recur`'s code gate while hunting the unresolvable-record ground; recorded so it is not later mis-attributed as a `U-recur` regression |
| FW-52 | ✅ **FIXED 2026-08-02** (`fcffbf8`) — two half-blind fixtures added ALONGSIDE the shipped one (never replacing it), each proven to redden when its own half-fold is reintroduced while the original stays green. The both-uppercased fixture remains the sole valid positive control against the original bug, and both docstrings say so. **FW-44's fixture catches case-blindness only when it is applied to BOTH markers.** The code gate modelled five implementations against three fixture shapes and measured it: a build that folds case on the *begin* needle only, or the *end* needle only, survives the entire suite. Mechanism — the shipped fixture uppercases both markers, so a half-blind build finds one and misses the other, falls into the `begin is None or end is None` branch, and lands on the same head truncation the criterion asserts. Closure is two more fixtures (begin-uppercased-only, end-uppercased-only) **added alongside** the shipped one, never replacing it: neither is red pre-fix, so only the both-uppercased fixture is a valid positive control | BUILD | Opportunistic, low. Disclosed by the gate as a coverage fact rather than a build defect — the spec pre-declared this outcome for one of its own mutations ("worth seeing"), so it is a known accepted limitation, not a regression. Recorded so a later reader does not mistake the green suite for full coverage of the unit's own subject. Found 2026-08-02 at U-marker's code gate |
| FW-49 | ✅ **FIXED 2026-08-02** (`fcffbf8`) — **and the row's premise was half wrong, which is the finding.** `_recurrence_suspects` is neither uncalled nor output-dropped: it runs on every `worker.run()` and its output does reach tracked telemetry. The defect is in the predicate, and its two bases are not equally dead. **`origin-match` is provably, permanently unreachable** — `import_common.existing_origins()` enforces global uniqueness of `evidence.origin` before any mined candidate lands, so a fresh pending record's origins are always disjoint from every routed record's; and independently, `teach`-authored records never write an `origin` key at all. That branch is removed. **`title-token-overlap` is NOT dead** — the pre-existing unit test proves it live; it is *starved*, because the miner folds genuine duplicates before they reach pending (the campaign's own §5 diagnosis). Measured against a full copy of the live ledger (35 pending × 31 routed): 0 hits before and after. Broadening it to full-section-body tokens was tried and measured WORSE (the one near-miss pair drops 0.571 → 0.33 Jaccard — longer text dilutes overlap), and that pair is a deliberate `--supersedes` refinement rather than a recurrence, so the basis was left alone rather than recalibrated on weak evidence. Adds ZERO new suspects on top of the miner's 4. Double-emission is impossible by construction: the two producers' `origin` value spaces are disjoint (pending-record-id vs transcript-ref). **"Zero `recurrence-suspect` events" has TWO dead producers, not one.** The channel-split diagnosis (campaign §7) names the miner; `worker._recurrence_suspects` (`worker.py:969-1016`) also spools `recurrence-suspect` and has likewise emitted zero. The campaign's `U-recur` fixes only the miner | BUILD | After `U-recur` lands, since it shares `worker.py` with FW-44. Until then the suspect surface stays under-reporting even once the miner crosses over. Found 2026-08-02 while spec'ing `U-recur` |
| FW-51 | ✅ **CLOSED 2026-08-27** (unit `u-verbguards`, T1 gate-characterized fix; gate-pending, worktree uncommitted) — root cause closed at `ledger_ops.resolve_record`/`ledger_ops.supersede_record`, which now consult ONE shared precondition, `ledger_ops.require_status` (terminal-state, "status never mere existence", 02 §2), plus a dedicated `ledger_ops.supersede_cycle_check` graph walk for the supersession-cycle case. **Measured before → after** (2026-08-26 verb assessment's five cases, re-run against the fix): `reject A` then `graduate A` — rc 0, `superseded_by: canon` → refused, exit 1, `record lrn-… is 'rejected' — graduate needs status pending/deferred/routed (02 §2)`, nothing written · `supersede` on a canon-graduated record — rc 0 → refused the same way (source status `'superseded'`) · `supersede C D` then `supersede D C` (two-record cycle) — rc 0 → the second call refused (liveness check first since C is now terminal; a longer, liveness-independent chain is refused by the dedicated cycle walk, pinned by its own test) · `supersede B C` with B graduated and C already superseded — rc 0 → refused on BOTH sides independently · `route`/`reject`/`defer` on a resolved record — exit 64 "not found" (a lie: the record exists) → exit 1, names the actual status, never 64 for an existing record. Refusals fire BEFORE any lock/mutation, verified with file bytes + git HEAD unchanged for EVERY involved record across EVERY refusal test in `test_verbs.py` (a code-gate r1 fold: 3 of the original 10 refusal tests asserted bytes only, 1 HEAD only, the two cycle tests neither, and `test_refuses_terminal_target` compared only the OLD side — all now assert both, for every record the refusal touches). UI POST half (`action_arm`/`action_confirm`) was never a separate hole — it shells out to this same CLI via `VerbRunner`/`RealRunner`, so the fix propagates automatically; `test_routes.py` pins that a CLI refusal renders as a visible error, never a success redirect, with a positive control (the real guard exercised in-process, not a hand-typed stderr string) — gate r1 found the HTML-side assertions (`data-armed="false"`, no `HX-Redirect`) inert (survived the gate's mutation) and replaced them with the exact refusal message text (markupsafe-escaped to match the template) plus a byte-level disk-unchanged check. Gate r1 (0 B/3 M/10 N/D) found and this fold closed: **M-1** `route_direct`'s own `--supersedes` guard (`verbs.py:3153`) had ZERO coverage and the CLI suite stayed green when deleted — the gap is load-bearing: without it, a rejected OLD supersedes-completion at `teach --route` time raises an unconverted `LedgerOpsError` from INSIDE `_ledger_write`, AFTER the new record was already written to `resolved/` and staged (exit 2 at the `teach --route` surface — measured by the gate: `teach` catches the escaping error, and the orphan already in `resolved/` then makes its own fallback capture fail on "already exists", so the capture is LOST) — now pinned by `test_route_direct_supersedes_refuses_terminal_old`. **M-2** every refusal test now asserts bytes + HEAD for every involved record (above). **M-3** `followup-done` is now gated on status too (`{routed}`) — measured: route with a follow_up, then graduate (status → superseded, follow_up SURVIVES, graduate only warns), then `followup-done` used to still succeed and commit, clearing a follow-up `open_followups()` had already stopped calling open; the criterion for needing this precondition is whether a verb's correctness is GATED ON status, not whether it MUTATES status (02 §2 now says so explicitly). Root-cause extras folded too: `require_status` gained a `reason` kwarg, and `confirm-held` / `confirm-recurrence` / `dismiss-suspect` now route through it with their pre-existing message text passed as `reason` — one implementation of the rule, six verbs total, every emitted byte unchanged (`test_dismiss_suspect.py`'s pinned assertion still passes); `supersede_record(home, X, X)` (a direct self-cycle) is now refused at the ledger_ops layer too — one line, `test_supersede_record_refuses_direct_self_cycle`; `self-learn host commit-drift`'s unrelated 64 "not found" is a different, host-family verb (64-mapped by design) and stays out of this pin's scope — noted, not touched. Tests: `test_ledger_ops.py` (the helper + cycle-walk + self-cycle guard, isolated from the verb layer) and `test_verbs.py`/`test_lifecycle_cli.py` (every resolution verb, the three route/reject/defer-on-resolved cases, `route`'s AND `route_direct`'s own use of `supersede_record`, and `followup-done`'s new status gate). Original defect description follows, kept for the record. **`graduate` on a REJECTED record succeeds, silently rewriting a denied lesson to `superseded_by: canon`.** Unguarded in the CLI — measured in a throwaway sandbox 2026-08-02, not inferred. `graduate` on an already-graduated record fails safe (nothing to commit); on a rejected one it goes through and inverts the human's decision | BUILD | Independent of the campaign; `verbs.py` is contended, so sequence it after. **Correction 2026-08-02 (audit):** this row previously said "not reachable through `U-grad-ui`'s surface (which gates the action to `routed`), so this is a CLI-side hole only." **That containment claim is false.** The status gate exists on the GET render only (`detail_resolved.html:87`); the POST half has none — `routes.py:1283` (`action_arm`) and `:1417` (`action_confirm`) validate only `if verb not in _KNOWN_VERBS:`. A hand-crafted POST with `kind="resolved"` against a graduated or rejected record still dispatches `graduate`. The shipped code says so itself at `ui/templates/partials/action_bar.html:152-159` (code-gate MAJOR 1), and `u-grad-ui-resolved-surface-spec.md:107` agrees while `:720` repeats the overstatement. Treat this as reachable from the UI, not CLI-only. Found 2026-08-02 while spec'ing `U-grad-ui`; containment corrected the same day |
| FW-68 | **An ARMED verb is lost the same way a cycled destination was — bigger blast radius, same mechanism.** The Iterate control is reachable by mouse even while the action bar is armed (nothing gates it), and `pane_close` fires a full-page `HX-Redirect` that rebuilds the entire Detail page from the record's own defaults. So arming a verb and then opening/closing Iterate discards **the armed verb itself**, not merely a cycled destination. Found 2026-08-03 while fixing the destination-loss defect (`f74c249`); **not reproduced and not fixed** — flagged because the fix for the smaller case does not cover it | BUILD | With FW-69, which shares the root cause. Two candidate shapes, and they are not equivalent: gate the Iterate control while armed (cheap, removes the path) or make the armed state survive a full-page rebuild (general, but the codebase's "no state that isn't a file" posture constrains where it could live — read `routes.py`'s own comments before choosing) |
| FW-69 | **Unarmed action-bar state has no protection from a broadcast refresh.** `app.js`'s `reloadDeferred()` defers a reload for an ARMED bar, an error strip, and a few offer states — but not for an in-progress destination cycle. An SSE broadcast or the 10s poll fallback arriving mid-cycle wipes the human's pending choice, with no Iterate detour needed. Found 2026-08-03 alongside FW-68; the destination fix (`f74c249`) closes the Iterate path only | BUILD | Same root cause as FW-68 — a full rebuild discarding unarmed UI state. Note the reload-defer machinery already exists and already enumerates what it protects; this is one more leg on a list that was built for exactly this purpose, which is why it is cheap and why its absence reads as an oversight rather than a design choice |
| FW-70 | **`telemetry.flush()`'s heal-a-torn-trailing-line read of the TRACKED file still crashes on a corrupt target.** FW-53 hardened the spool side and made `read_events`/`flush` decode per-line, but this one read is of the tracked telemetry file itself. Different corruption source, and closing it needs a decision about how to safely append to a target whose existing content cannot be read — declined deliberately by the FW-53 builder rather than half-fixed | BUILD | Opportunistic. Same "corruption never blocks the rest" posture as FW-53; the open question is the append semantics, not the detection |
| FW-62 | ✅ **FIXED 2026-08-02** (`81cb694`) — `record_text=` now passed, matching the exact text form `write_proposal` uses so the two paths cannot disagree again. Verified a no-op on the live ledger (none of the 20 real pending proposals carries a `gates:` trace), so no transition was needed. **`self-learn proposal validate` reports VALID — and STAMPS — a proposal that `write_proposal` refuses.** `selfcheck.py:154` calls `validate_proposal(read_proposal(...))` positionally with no `record_text=`, so U-schema's quote containment never runs on the one surface built to ask "is this honest?". Measured on identical bytes in a scratch ledger: `write_proposal` → *REFUSED — gates.t1.field_shaped.evidence is not contained in the record it claims to quote*; `proposal validate` → *"valid — record_sha stamped in place"*, rc=0. `SKILL.md:52` documents this verb as **REQUIRED after any direct edit of a pending record outside CLI verbs** — so the fail-open sits exactly where a hand-edited record gets its only check. The fix is already in hand at the call site: `selfcheck.py:153` does `Record.from_path(record_path)` and **discards the result on the line above**, so it is `record_text=Record.from_path(record_path).to_text()` | BUILD | **Top of the audit rows with FW-57.** A validator whose stricter surface is the machine path and whose lenient surface is the human path has its permissions inverted. CONFIRMED by execution 2026-08-02 |
| FW-63 | ✅ **FIXED 2026-08-02** (`05f8a5b`) — `key=repr` on both sites, AND on the pre-existing clone in `_validate_hook_extension`, because fixing the instance and not the class is how this recurs. **S6 — "raises only `ProposalError`, on every input" — is false, and `self-learn list` tracebacks for everyone.** `ledger_ops.py:798`, inside `_validate_gates`: `sorted(gate_keys - set(TRACE_GATE_KEYS))`. A `gates:` mapping with **two or more unknown keys of mutually incomparable types** (YAML permits non-string keys — `{1: x, zzz: y}`; also reproduced with `None`, `True`, tuple) raises `TypeError`, which is neither `ProposalError` nor `LedgerOpsError`. `proposal_info` catches only `ProposalError`; `queue()`/`list_items` catch nothing. Measured end to end: `cli.main(["list"])` → `TypeError: '<' not supported between instances of 'str' and 'int'`. **A malformed trace on somebody else's record breaks `list` for everyone** — verbatim the symptom the unit's own `re.error` backstop was added to close, one branch earlier in the same function. One-token fix: `sorted(..., key=repr)`; pair it at `:803`. **Why no test caught it:** the A7/M22 mutation catalog models one mechanism, *index-before-type-check*, which `_mapping` closes thoroughly — this is a second mechanism *above* any `_mapping` call, since the key-set diff runs on the raw mapping before any leaf is touched. **Honest scoping:** the identical construct pre-exists at `ledger_ops.py:505` (`_validate_hook_extension`) and raises the same way, so the shape is repo-wide; what is new is declaring S6 *unconditionally* while cloning the escape into it. 12 other hostile `gates` shapes fuzzed: 0 further escapes | BUILD | CONFIRMED by execution 2026-08-02. Together with FW-57 these are the two escapes from a guarantee the merge commit stated without qualification |
| FW-64 | ✅ **FIXED 2026-08-03** (`4f8817a`) — the `dest is None` proxy is replaced by an explicit `by`, validated against a closed `{human, analyst, agent}`. **A third value was added deliberately**: the SDK pane agent is a real third chooser and was previously misrepresented as one of the other two; checked and confirmed this does NOT touch `SCHEMA_VERSION` (a value inside an existing kind, not a new kind — important, since that constant is already double-booked per FW-65). The UI distinguishes approve-as-proposed from an override via `dest_touched` — **a positive fact (did the human use the cycle control), not a comparison against the proposal, which was rejected because the proposal can go stale between render and confirm.** Mutation testing found THREE defects in the builder's own first-pass tests, all closed before delivery — notably a hardcoded `False` in the commit-drift retry context that left all 18 pre-existing commit-drift tests green. Historical records deliberately not rewritten. **FW-45 may now be closed** **`routing.by` is still dishonest on BOTH live routing surfaces — FW-45 is on track to be closed while the capability it names is dead.** U-reach's Part C rests on the premise (`verbs.py:2052-2054` comment, `test_route_observability.py:15` docstring) that *"the review UI's approve-as-proposed argv omits `--dest` entirely."* Driving the shipped app end to end disproves it: the detail page renders `<input type="hidden" name="dest" value="skill-md">` (fed by `RecordRow.destination_default`, the analyst's own scope-corrected destination), and confirm dispatches `['route', '<id>', '--dest', 'skill-md', '--json']`. So `by = "human" if dest is not None else "analyst"` yields **`human` for every UI approval, including approve-as-proposed, where the ANALYST chose.** The second surface is the already-known `teach.py:697` (no `by=`, so `resolve_record`'s `"human"` default applies) — **which no FW row records**, existing only in a commit message and a docstring. A third, same root cause: `ui/proposals.py:141`, the SDK pane agent, records `analyst` when it omits dest and `human` when it supplies one — wrong in *both* branches, since the chooser was neither. `dest is None` is a proxy that cannot distinguish three choosers. Nothing pins the seam: `ui/tests/support.py:199` hardcodes `"by": "human"` and no UI test asserts on `by`; the CLI tests DO discriminate (mutating `verbs.py:2058` reddens criteria 16/21/24), so the gap is purely at the package boundary where no test looks | BUILD | **Do not close FW-45 until this is fixed.** The whole point of `routing.by` is measuring how much routing the analyst actually does; recording `human` for analyst-chosen routes makes the autonomy-ladder evidence substrate report the opposite of the truth. CONFIRMED by driving the app 2026-08-02 |
| FW-65 | ✅ **FIXED 2026-08-02** (`92454a7`) — F6 now asserts the INCREMENT and tells the builder to read the current value first. **`SCHEMA_VERSION = 2` is double-booked, and a future acceptance criterion will pass vacuously.** `telemetry.py:68` is now `2` for U-reach's `route` kind. `drafts/16-ecology-spec.md:902` pins **`SCHEMA_VERSION = 2`** as *its* bump for adding `contradiction-suspect`, instructing "bump the literal, currently `1`", with acceptance criterion **F6** reading "`SCHEMA_VERSION = 2` pinned". When 16-ecology Stage A lands, **F6 passes with nobody having bumped anything** — a criterion satisfied by a different unit's work — while two different closed kind-sets both label themselves v2, which the constant's own contract forbids. Same file: `:904` describes `miner._event_seen`'s pre-U-recur signature (U-recur changed it from `set` to `tuple[set, list]`), and its `telemetry.py` line refs are stale | BUILD | Cheap now, confusing later. Bump 16-ecology's target to 3 and re-word F6 to assert the *increment*, not a literal. Documentary, not executable. Found 2026-08-02 |
| FW-66 | ✅ **FIXED 2026-08-02** (`81cb694`) — and it was worse than recorded: the crash fired even earlier, in `_section_targets`, so `--selftest` printed ZERO of its seven rows. Fixed across six sites (`_check_reach`, `_section_targets`, `_check_drift`, `_check_compiler`, `_check_markers`, `_check_hooks`); an undecodable surface now FAILS loud naming the file rather than passing silently. **`_check_reach` raises `UnicodeDecodeError` on two inputs it appears to guard.** `selfcheck.py:336` (`surface.read_text(encoding="utf-8")`) and `:383` (`Record.from_path`, whose `except RecordError: continue` at `:384` does not catch a decode error). One non-UTF-8 byte in a loaded surface or a resolved record → `--selftest` tracebacks instead of printing its seven PASS/FAIL rows. Both reproduced. **Honest qualifier:** identical shape pre-exists in `_check_drift` (`:438`), so this is a clone, not a regression — but the blast radius is new. Drift reads compiler-owned targets; reach reads the **loaded surface** (`SKILL.md`, a project `CLAUDE.md`, `~/.claude/CLAUDE.md`) — files self-learn does not own and cannot constrain. Same class as FW-53, which explicitly certifies *U-recur* added no new exposure; nobody ran that check against U-reach | BUILD | With FW-53 — one decode-hardening pass over every `read_text` on a file self-learn does not own. CONFIRMED 2026-08-02 |
| FW-67 | ✅ **FIXED 2026-08-02** (`358c9c1`) — rewritten as the general RULE (containment iff `record_text=`) pointing at §2's S2 census as the single authoritative list, rather than a second enumeration that would drift again. All seven sites re-verified against current code first. **U-schema's containment disclosure names two of seven positional call sites.** §3.7 item 9 (`u-schema-decision-trace-spec.md:641-655`) is the unit's stated "here is exactly how far containment reaches" paragraph; it names `worker.py:909` and `verbs.py:509`. Seven sites call `validate_proposal(data)` positionally (containment OFF): those two plus `worker.py:1237`, `verbs.py:1098`, `verbs.py:1152`, **`analyst.py:235`** and **`selfcheck.py:154`**. Only `ledger_ops.py:1342` (`write_proposal`) and `:1745` (`proposal_info`) pass `record_text=`. Items 3–5 are arguably covered by "the worker and route call sites"; **6 and 7 are not covered by any reading** — `analyst.py:235` is the PRODUCER, where a fabricated quote first arrives from the model, and `selfcheck.py:154` is FW-62. The spec's S2 list at `:131` does enumerate all seven, so the information is in the document — the defect is a second, narrower enumeration in the paragraph that exists to be the authoritative one, in a unit whose §0 forbids exactly that | BUILD | With FW-62. Also fold in the prose drift the same pass found: `analyst.py:167-170`'s docstring still enumerates the old fixed key set *on the very function whose shipped defect was enumerating a fixed key set*; `miner.py:1008-1009` still says the journal's outcome vocabulary is "UNCHANGED (12 A1)" after U-recur added `recurrence-from-fire` and edited the next line of the same block (`12-transcript-miner.md:406-409` likewise); and `skills/self-learn/SKILL.md:53`'s user-facing `--selftest` list names neither `drift` nor `reach` | 
| FW-57 | ✅ **FIXED 2026-08-02** (`05f8a5b`) — consecutive `**` now collapse before translation, as the oracle does. Pre-fix measurement reproduced at 40.4s for twelve; `_compile_glob_pattern` was also split so failures are cached too (`lru_cache` never caches exceptions). **`self-learn list` can HANG — not raise — on a `rules_paths` pattern with consecutive `**`.** `ledger_ops.py:667` emits a separate `(?:[^/]+/)*` per `**` segment, and adjacent groups nest exponentially; `glob.translate` — the oracle the docstring names — collapses consecutive `**` into one `(?:.+/)?`. Measured against a 24-segment non-matching path: 8 repeats → 0.24s, 10 → 3.36s, **12 → 33.5s**, versus 2µs through the oracle. A proposal with `gates.t2.answer: yes`, a long `match_path` and `rules_paths: ["**/**/**/…/x.py"]` wedges the listing. **This is the one failure mode `U-schema`'s S6 guarantee structurally cannot cover** — S6 catches `ProposalError`, and non-termination never reaches an `except`. It sits on the eligibility hot path. Two related prose claims that do not hold: `_compile_glob_pattern`'s *"Memoized: this runs on the eligibility hot path"* memoizes **compilation only** (three identical `_glob_match` calls: 0.516 / 0.495 / 0.491s), and `lru_cache` never caches exceptions, so an untranslatable pattern is re-translated for every record on every listing (`cache_info()` after five identical calls: `hits=0, misses=5, currsize=0`) | BUILD | **Highest of the audit rows.** Honestly scoped: the *intra-segment* blowup (`src/*a*a*a…*.py`) is NOT a divergence — the oracle is equally slow — so only consecutive `**` regresses against the named oracle. Fix is to collapse consecutive `**` the way the oracle does. CONFIRMED by execution 2026-08-02, post-wave code audit |
| FW-58 | ✅ **FIXED 2026-08-03** (`cf9a700`) — all five confirmed STILL uncovered first (including the `&`/`~`/`|` escape, whose enclosing function was rewritten hours earlier by FW-57/63 without touching that line). Test-only; no production code changed. One honest limitation recorded: escaped and unescaped `&`/`~`/`|` compile and match IDENTICALLY under this interpreter, so that check can only be pinned on the translated pattern's SHAPE, not its behaviour — stated rather than papered over with a behavioural assertion that could not fail **Five more `U-schema` production checks that no test can see.** Each, neutralized individually, leaves `test_decision_trace.py` at **73 passed** with the full-suite failure set byte-identical: `ledger_ops.py:822` (`g0.reject/defer.evidence` required-when-`yes`), `:912` (`t2.evidence` `required=True` — the "required BOTH ways" rule), `:896` (`t1.cost_bearing.evidence` required-when-`yes`), `:815` (`g0.<leg>.answer` yes/no enum), and `:630` (the `&`/`~`/`\|` class-body escape — *the unit's own false-refusal fix*). No fixture ever sets `gates.g0.reject/defer.answer: "yes"` or `t2.evidence: None`. The merge commit folded in "eight production checks no test could see"; the same sweep found eight and left at least these five | BUILD | Test-only. CONFIRMED by mutation 2026-08-02. Note the shape: the gate's sweep was thorough within the shapes it modelled and blind to the ones it did not — the same lesson `U-pathed`'s A17 taught at a different level |
| FW-59 | ✅ **FIXED 2026-08-03** (`cf9a700`) — **and the mechanism is worth naming: one check was silently BACKSTOPPED by another.** With `rules_paths=[]`, deleting the target clause did not make the call succeed — a downstream glob check raised `ProposalError` anyway, for an unrelated reason, so the assertion stayed green while the thing it guarded was gone. Fixed by pinning the exact message on both legs so a swap between them is visible. The second vacuity was cruder: `match=key` was satisfied for EVERY key because every message echoes the full key list — proved by hardcoding the production list to `["g0"]` and watching the old assertion stay green for **8 of 9** deleted keys **A criterion whose docstring says it "must never be vacuous" is vacuous.** `test_decision_trace.py:1095` — deleting the `or not rules_paths` clause at `ledger_ops.py:929` leaves both halves green: with `rules_paths=[]`, `any(...)` over an empty list is `False` and the next check raises with a **byte-identical** message, so the empty-list case never discriminated the clause it targets. No behavioural bug (both refuse). Same file, `test_missing_gate_key_refused:434`: its `match=key` is satisfied by every message, because they all echo `list(TRACE_GATE_KEYS)` | BUILD | Low, test-only. CONFIRMED 2026-08-02. Worth fixing precisely because the docstring asserts the opposite — a comment claiming non-vacuity on a vacuous assertion is the strongest form of this project's signature defect |
| FW-60 | ✅ **FIXED 2026-08-02** (`358c9c1`) — §3.4a treated as normative and narrowed to name the one measured false-refusal exception, with both measurements stated; also disclosed that it is NOT proven unreachable, since nothing in Schema-1 forbids an absolute `match_path`. **Two `_glob_match` divergences from the named oracle, both benign but one contradicting a spec sentence.** 6000 fuzzed patterns × 60 paths: **564 mismatches, all one shape** — `_glob_match("", "*")` is `True`, oracle `False`; zero other semantic mismatches and zero non-`ProposalError` exceptions. Separately `_glob_match("/src/a.py", "**/*.py")` is `False` where the oracle is `True` — **a false refusal, the direction `u-schema-decision-trace-spec.md` §3.4a says cannot happen**, though §8-O6 claims the absolute-path behaviour deliberately. The docstring's "0 mismatches on a real tree" is literally true only because a real tree cannot hold an empty filename | BUILD | Low. Reconcile §3.4a with §8-O6 rather than changing behaviour. Also record here, disclosed-not-defect: `_glob_match(".claude/rules/ts.md", "**/*.md")` is `True` while `verbs._validate_project_globs` (`verbs.py:698`, `glob.glob(recursive=True)` with `include_hidden` defaulted **False**) refuses the same tree with "matches nothing" — §8-O6 rules `verbs.py` is the side that moves, but note the divergence lands exactly on `.claude/rules/`, this project's own rules-variant destination. CONFIRMED 2026-08-02 |
| FW-61 | ✅ **FIXED 2026-08-02** (`92454a7`) — folded into the campaign playbook's mutation-verification section, along with the wrong-tree and editable-install variants and the survivals-are-suspect asymmetry. **Mutation sweeps in this repo need `PYTHONDONTWRITEBYTECODE=1` and a cache clear, or they report survivors that never ran.** Python reused stale `__pycache__` when a source file was rewritten inside the same second, so the audit's first mutation run produced garbage — mutations recorded as "survived" that were never actually executed. That is itself a check passing for the wrong reason, at the level of the tooling the gates depend on | BUILD | Process, not code: fold into the campaign playbook's mutation-verification instructions so every future code gate carries it. Found 2026-08-02; the audit's own findings were re-run under the corrected setup |
| FW-54 | **`U-reach`'s shipped code and tests state a reason S-23 closed the other way.** `selfcheck.py:284-290` says the `user` row is "**dead code end-to-end until `U-demand-user`**", repeated at `test_selftest.py:511-514` and `:533-537` and four times in `u-reach-reachability-selftest-spec.md` (`:303-304`, `:526-531`, `:532-533`, `:627-632` — the last describing a "seam into this unit's file" for when B7 opens). Under S-23 (2) user-scope `reference` is dead **permanently**, not pending: the branch is correct, its stated reason is not, and the promised seam is moot. Note the spec was *partially* updated for S-23 (it cites it at `:573`), so half the file was reconciled and half was not | BUILD | Comment/docstring/test-name accuracy only — no behaviour changes. Cheap, and worth doing before someone builds the seam that S-23 removed the need for. Found 2026-08-02 by the post-wave bookkeeping audit |
| FW-55 | ✅ **FULLY FIXED — 2026-08-06 (row corrected; both halves independently closed earlier).** The U-schema half closed 2026-08-02 (`358c9c1`): sanitiser and `re.error` backstop described, §9 gains the missing code-gate round. The U-analyst half — recorded just below as "still open" — actually closed 2026-08-03 (`f641178`), one day after this row was written; this row was never updated to say so until today's pass (dated note appended at the end of this cell). **Two shipped specs described mechanisms their own build had replaced; both are now corrected.** (1) `u-schema-decision-trace-spec.md:417-420` pins the glob recipe as "`[...]` → a passed-through character class"; shipped `ledger_ops.py:629-630` **sanitises** it instead (`\\` doubled, `&~\|` escaped), and the spec's §9 revision history stops at r3 with no entry for the code-gate round that changed it. The `re.error` backstop at `ledger_ops.py:675-680` and its two tests appear nowhere in the spec, whose final step (`:435-436`) ends at `re.compile`; and §3.4a's unconditional "the divergence direction is false accept, never false refusal" (`:480-482`) now holds *because of* the escaping the spec omits. (2) `u-analyst-proposal-fidelity-spec.md:61-66` states as measured that a proposal carrying `gates:`/`flags:`/`recommendation:` "validates clean today" — U-schema merged after and closed half of it (`ledger_ops.py:776-779`, `:784-789` now refuse out-of-set `flags:`/`recommendation:`; measured on master: `banana` → CLEAN, `flags:['banana']` → REFUSED) **Amended 2026-08-06 (routine bookkeeping pass, docs-only):** the U-analyst half just above, described as "still open," was actually closed the day after this row was written — `f641178` (2026-08-03), the same commit that repaired FW-56's citations, rewrote `u-analyst-proposal-fidelity-spec.md` §1.1 to state the current behaviour, dated, quoting the real refusal messages. This row simply never recorded it. Re-verified today, empirically, in a scratch env (`SELF_LEARN_HOME`/`XDG_CACHE_HOME`/`XDG_RUNTIME_DIR` redirected to a temp dir) against current `validate_proposal`: `banana` → CLEAN (no general unknown-key rejection exists, confirming that half of r2 §6 row B2 is still false for the general case); `flags: ['not-a-real-flag']` → REFUSED (`"flag 'not-a-real-flag' is outside the closed set [...]"`); an out-of-set `recommendation` → REFUSED; a malformed `gates: {'nonsense': 1}` → REFUSED (`"gates has unknown key(s) ['nonsense'] — allowed: [...]"`) — byte-identical in shape to what the spec's own 2026-08-03 correction already states. **This row's own citations two sentences above are themselves now stale — a second, smaller instance of the exact citation-drift defect FW-56 fixed at scale:** `ledger_ops.py:776-779`/`:784-789` no longer hold the flags/recommendation refusal; those lines now fall inside `_check_evidence` (§3.1's per-leaf evidence checks — the quote-length floor and the RECORD-quote containment check), an unrelated function that shifted into this range as the file grew. Current locations, all inside `_validate_gates` (`ledger_ops.py:819-1251`, confirmed against the `def` before and after): the flags closed-set refusal is `:837-840`, the recommendation closed-set refusal is `:846-850`, the gates unknown-key refusal is `:867-871`. Item (1) — the U-schema half — was re-checked the same pass and remains accurately closed, no edit needed: `u-schema-decision-trace-spec.md` §3.4a (`:393-629`) and §9's "Code gate" entry (`:1528-1557`, the section's final bullet, running to EOF) already describe both the sanitiser and the `re.error` backstop in full. | BUILD | Doc-accuracy only. (1) is the exact failure the campaign playbook §5 warns about — a spec pinning an algorithm in prose that the build then had to correct — so it is worth fixing as the worked example of that rule. (2) is ordinary cross-unit staleness from concurrent merges. Found 2026-08-02 by the post-wave bookkeeping audit. **Status corrected 2026-08-06 — both halves confirmed closed; see the amended note above** |
| FW-56 | ✅ **FIXED 2026-08-03** (`f641178`) — and the scale was far past this row's estimate: **~301 citations checked, ~208 stale, 0 unresolvable.** These specs cite the same construct repeatedly and nearly every unit had shipped, so sibling merges drifted almost everything. **Most were converted to SYMBOL names rather than re-pinned to new line numbers** — a symbol survives the next insert; re-pinning ~200 line numbers would only reset the clock. All four named examples confirmed and fixed. Where a citation pointed at code its own fix had REPLACED (not merely moved), the citation was corrected and a dated note added, without rewriting the historical narrative around it. **Citation drift across the wave's specs — roughly 80 `file:LINE` anchors no longer resolve.** Almost all were exact at each unit's own base commit and drifted from the units' own inserts and same-day sibling merges, so this is honest drift rather than fabrication. Four matter because they sit in load-bearing text: the campaign playbook's completion checklist (`r2-routing-campaign.md:542`) cites `miner.py:1174-1185` for the recurrence-suspect spool (now `:1236-1251`; `:1174` is a docstring tail) and `verbs.py:3147` for `confirm-recurrence` (now `:3204`; `:3147` is unrelated code), and `u-grad-ui-resolved-surface-spec.md:93-94` cites `verbs.graduate` at `verbs.py:2913` (now inside **`rehome`**'s refusal; `def graduate(` is `:2969`) and `:772` cites `ledger_ops.list_items:1108` (now mid-`validate_proposal`; `def list_items(` is `:1797`) | BUILD | Low individually, systemic in aggregate: a citation that resolves to *unrelated but plausible* code is worse than one that resolves to nothing, because a reader checking it may believe they have verified the claim. Consider whether load-bearing citations should name a symbol rather than a line. Found 2026-08-02 by the post-wave bookkeeping audit |
| FW-50 | **Quote containment closes the record-sourced legs only — the S-21 *flavour* stays open.** `U-schema`'s validator checks a cited quote against the record, which covers the load-bearing judgment gates; it does **not** check a quote against a canon **target file**, which is precisely the shape S-21's fabricated citation took (a pin quoting a document section that contained no such text). Deliberately out of scope there: it needs hot-path I/O, it reads mutable canon (an unrelated edit would silently re-queue a record through a swallowed exception), and it needs `verbs.py::_resolve_target`, which is contended | BUILD | After the campaign's `verbs.py` units settle. Recorded so a later reader does not read "quote containment shipped" as "fabrication is closed" — it is closed against records, open against documents. Found 2026-08-02 while spec'ing `U-schema` |
| FW-71 | **UI terminology/affordance gaps, from the 2026-08-03 cold-open walk** — a source-blind walker with no prior exposure to the app or corpus. Vocabulary: `"arm"` appears user-facing exactly once, in the help overlay only; `"canon"` is never defined outside a success banner; roughly two dozen terms are left undefined for a cold user. Affordances: a `"Close (q)"` control does not close; `"Force run"` gives no perceptible feedback; keyboard `b` does nothing visible | BUILD | ✅ **FIXED 2026-08-06** (merge `f8a7523`) — all four defect groups closed, each with a diagnosed root cause the fix build verified empirically rather than assumed: "Close (q)" was the bucket pane's synthetic session key (`bucket:<scope>/<name>`, colon included) flowing into `hx-include`'s CSS id selector, making htmx's `querySelectorAll` THROW (`SyntaxError`) and abort the POST — fixed by gating the attribute off for bucket panes on both the page-load and `/pane/panel` swap paths; "Force run" now publishes through the existing S-20 applying-strip machinery on both `worker_kick` and `mine_run`; keyboard `b` was never dead — its effect landed off-screen (scroll-position bug), fixed with `scrollIntoView` for every keyboard-dispatched action; "arm" defined ahead of the help-overlay keymap list and "canon" + 9 further terms glossed at 11 render sites. 19 new tests + 3 more from the code-gate fold round (the gate found two of the new tests were themselves vacuous/coverage-gapped — the house defect class — both closed by mutation-verified fixes). Blind code gate CLEAN; suite 1170 passed / zero skips on the merged tree. Residuals split out honestly: **FW-76** (a failed Force run still looks like success) and **FW-77** (glosses are hover-only, keyboard-unreachable); terms deliberately left unglossed are listed in the build report (suggested-survivor, scan-blocked/schema-invalid badges, commit-drift, chezmoi-adopt offer, freshness badge, row-level deferred badge) |
| FW-72 | **The front page's bucket table was observed disagreeing with itself across two consecutive loads.** Single occurrence, 2026-08-03, source-blind walker; the detail-prefetch cache was checked and ruled out as the mechanism. Not reproduced since. **No diagnosis exists — this row records the observation only, and deliberately does not speculate about a cause.** **Amended 2026-08-06 — bounded reproduction attempt completed, NOT reproduced.** 91 HTTP loads, 124 CLI reads, 25 browser paints against a frozen copy of the real ledger, across sequential / 24-way-concurrent / cold-start / CPU-pressure / live-SSE / two-tab conditions, produced exactly one bucket-table state throughout — negative at three layers (CLI JSON read, server render, passive browser assembly) for this corpus/configuration. **This does not close the row.** Explicitly NOT ruled out: a load spanning a real ledger write; the SSE-driven reload race and the 10s poll fallback (neither fired during the attempt — see below); sort-key ties under different dirent/emission order (`models.py`'s bucket-ordering key, `:962` — `key=lambda b: (b.oldest_days is None, -(b.oldest_days or 0))` — is not a total order; confirmed ties keep `status --json`'s own emission order, since `buckets_raw` at `:905` reads straight off `status_data["buckets"]` before the sort at `:949-963`); `oldest_days` day-boundary rollover; and walker actions between the two loads. **Candidate mechanism (1), measured, not confirmed as cause:** a front-page load can spawn a detached ledger WRITER. `routes.front` (`self_learn_ui/routes.py:462-480`) shells four `self-learn … --json` subprocesses — `ledger.list_items`/`status`/`report`/`mine_status` (`ledger.py:141-175`, each via `_invoke_json` → `subprocess.run`) — and `cli.py`'s dispatcher ticks the miner watchdog on every command except `mine`/`init` (`:1829-1848`, `miner.maybe_kick` at `:1842`), which (`miner.py:1671-1701`) spawns a detached run once the last completed run is >24h stale and the last attempt is >2h old. Under a deliberately staled miner state, one `GET /` was observed to grow the miner journal and spawn a run — so two consecutive front-page loads are not guaranteed the same ledger state, since the first can start a process that writes it. In the sandbox the spawned run landed nothing and the table did not move. **(2), measured:** an SSE refresh broadcast reached an open front page and did not trigger a reload within 20s; the poll fallback (`app.js`'s `POLL_FALLBACK_MS`, `:412`) never armed. Checked as asked whether a refresh-broadcast handler exists on the front page: **it does** — the SSE `onmessage`'s `case "refresh": if (inScope(envelope.scope)) reload();` (`:662-664`) — so "no handler" is not the explanation; `inScope` (`:440-445`) matches every scope from the front page (`:443`, "the Front page matches every scope"), so scope-filtering isn't either. The poll fallback arms only inside the `EventSource`'s `onerror` handler (`:641-652`, `startPoll()` called at `:651` — not the `:621-628` first guessed, which is `connectEventSource`'s opening plus the `startPoll`/`stopPoll` helper *definitions*, not the error handler), consistent with the connection never having errored during the attempt. `reload()` itself (`:527-533`) silently DEFERS, never drops, while any of six documented legs holds (`reloadDeferred()`, `:517-525`: a `[data-verb-error]`/`[data-verb-success]` element, a verb-confirm POST in flight, an armed bar, or a contradicts/adopt-offer element) — a held leg would produce exactly the observed symptom with no handler missing or broken. **Unverified against what was actually on-page during the attempt — a candidate mechanism, not a diagnosis** | DRILL | A bounded reproduction attempt completed 2026-08-06 — NOT reproduced (see amendment). Next: either exercise the two candidate mechanisms above deliberately (a real ledger write mid-load; a forced SSE-defer leg), or wait for the next live occurrence |
| FW-73 | **The miner's per-run cap silently loses candidates it drops — the session cursor advances past them anyway.** MEASURED: the 2026-07-30 run (`e22fdb66`) journal entry (`~/.cache/self-learn/home-0f24de4d/miner/journal.jsonl:18`, read-only) records a candidate — a nix `accept-flake-config` lesson — with `"outcome":"dropped-cap"` and `"promotable":true`, at `"cap":15,"landed":15`. **Mechanism confirmed against current code, matching the diagnosis exactly.** The per-candidate cap check (`miner.py:1328-1343`, inside `_reconcile_and_land`, `:1212`) only `continue`s past the one candidate; it does not touch `processed` (`:1815-1834`), which is built purely from which SESSION SLICES fit the digest budget, entirely BEFORE `cap` is even computed (`cap_for(len(digests))`, `:1895` — well after `processed` is finalized). `_advance_cursors(processed)` (`:1934`) then runs unconditionally on the success path, after `_reconcile_and_land` (called `:1907`) returns — so the capped candidate's originating session cursor moves past it regardless of whether it landed. Contrast with the flood gate (`:1849-1860`), whose own comment states the opposite design — *"don't advance cursors — nothing is missed"* (`:1849`) — and which `return`s at `:1860`, before `_reconcile_and_land` or `_advance_cursors` are ever reached, holding EVERY session's cursor when the queue is full. The cap has no equivalent hold | BUILD | ✅ **Spec gated r3 CLEARED FOR BUILD (2026-08-09); build complete** as `U-cursorhold` — Rule-H (`miner.py`): the digest loop's session map (`digested: dict[str, bool]`, session id → that slice's halt flag, populated only where a digest was actually read), `_reconcile_and_land`'s new `digested` parameter classifying every cap drop into exactly one of `held` / `advanced-halted` / `advanced-unmatched` (journaled as the new `cursor` outcome field), `MineResult.held_sessions`, and a path-plus-halt filter at the `_advance_cursors` call site (`_advance_cursors` itself, `processed` itself, the cap values, and the flood gate are all untouched, per the spec's own footprint table). M-5 wins unconditionally: a halted slice always advances and always persists its halt, even when its cap drop would otherwise classify `held` — the aliasing corner (two same-stem transcripts under different project directories) is why H-3 filters on path-plus-halt rather than session id alone. 17/17 acceptance criteria pass; the mutation sweep's 20 code mutations (M1–M15, M17–M21) all REDDENED on the targeted edit with a clean exact-inverse revert verified after each, including the two "and only" negative controls the spec calls out by name — M4 reddens A4 alone (A1/A3/A7 stay green) and M20 reddens A9 alone (A1/A3/A5/A7/A10 stay green); M16 (the doc-only mutation) verified by grep-detectability instead of pytest. One SURVIVED case, analyzed: M9's outcome rename doesn't redden the new UI unit test B3(ii) (a static-dict test of `cursor`-key inertness that never calls the renamed `_outcome()` site) — M9 is still caught at the criterion level by B3(i) and by the pre-existing shipped `test_cap_refused_journals_snippet`, both of which look up the outcome by its literal name. Suite 1535 passed / 5 skipped / 0 failed (1520 baseline + 15 new miner tests + 1 UI test for B3(ii)); pyright 50/50 (baseline unchanged, one new dict-value-type note fixed with an explicit annotation). Canon amended in `12-transcript-miner.md` §2 Phase 0, the Phase 4 flood-control paragraph, and §8 A1's journal contract. Pending code gate |
| FW-74 | **Test-suite runs left ~31,231 `home-*` namespace directories in the REAL user cache, `~/.cache/self-learn/`.** MEASURED 2026-08-06 (nightly diagnosis); re-verified independently today, read-only: `command ls ~/.cache/self-learn \| grep -c '^home-'` → 31231, equal to the directory's total entry count (`ls \| wc -l` → 31231) — every entry is one of these. **The task's INFERRED cause does not hold as stated for either suite's fixtures today.** Both already redirect `XDG_CACHE_HOME` for every test, autouse: `cli/tests/conftest.py::_worker_test_defaults` (`:13-24`, `XDG_CACHE_HOME` at `:24`; landed 2026-07-14, `7a53fab`) and `ui/tests/conftest.py::_redirect_env_defaults` (`:11-55`, `XDG_CACHE_HOME` at `:44`; landed 2026-07-27, `6bd30eb`) — the UI fixture's own docstring documents this exact defect class already found and fixed once ("measured leak: exactly 176 stray directories per full UI suite run before this fixture existed", `ui-test-cache-isolation-spec.md` r2 §3.1). **Re-verified, read-only, against real dir mtimes:** of the 31,231, one (`home-0f24de4d`) is the real production ledger namespace, not a leak; roughly 31,213 predate the 2026-07-27 fix (legacy debris, consistent with pre-fix suite runs); the remaining 17 postdate the exact fix commit (2026-07-27 16:57:28), clustered 2026-08-02 16:21 – 2026-08-03 00:12, none since — a small, unidentified residual leak source outside both conftest fixtures survived both fixes | BUILD | The ~31,213-directory legacy majority needs a one-time sweep — user-run housekeeping, not agent work; do not delete anything here. The 17-directory residual is the open BUILD item: source not yet found |
| FW-75 | **The front page's column sort is client-only and silently reverts.** `static/app.js`'s `sortValue`/`sortBucketTable` (`:368-398`, wired via the document click listener at `:400-403`) reorders the actual `tbody` rows in the DOM (`tbody.appendChild(row)`, `:396`) and persists nothing — no querystring, no `localStorage` anywhere in the function or its listener. Any reload (including the front page's own SSE-driven/poll reloads, FW-72) restores server order with no warning. MEASURED 2026-08-06: a header click changed row order; the next reload reverted it, silently. This is a real, user-action-driven same-page path a human can hit — adjacent to the FW-68/FW-69 family (UI state discarded by a full-page rebuild) but distinct: this state was never intended to survive a reload in the first place, so it is an unannounced transience, not a lost-state bug in the FW-68/69 sense | BUILD | Small. Either persist the sort choice (querystring or `localStorage`) or mark the sorted state as visibly transient in the UI |
| FW-76 | **A FAILED Force run is indistinguishable from a successful one.** The FW-71 fix (`f8a7523`) publishes `"error"` terminal states from `worker_kick`/`mine_run`, but `app.js`'s applying handler treats every non-`"start"` state identically (`inflight.delete(key)` and re-render) — there is no error rendering on the strip, so on failure the strip simply disappears, exactly as on success. MEASURED live by the 2026-08-06 blind code gate with a deliberately failing runner: `strip seen → hidden_after=True; error/failed text on page after? []`. Contrast the verb-confirm routes, which on `not result.ok` re-render the bar with `ctx["error"]`. Pre-existing (Force run was silent on failure before the fix too), and the gate ruled fixing it out of scope for an extend-S-20-don't-touch-the-render-path build — recorded rather than smuggled in | BUILD | ✅ **FIXED 2026-08-09** (spec `drafts/u-forcefail-error-leg-spec.md`, U-forcefail; code gate CLEAN — 0 BLOCKER / 0 MAJOR / 5 NOTE, all 13 named mutations independently reddened, an end-to-end real-`.click()` probe reproduced the original 2026-08-06 failure shape pre-fix and passed post-fix; pending commit/merge) — `app.js`'s `inflight` Map gains a fourth rule: `error` now SETS a distinct `{badge:"failed", failed:true}` entry (replacing an applying one, or creating one when the key is absent) instead of folding into `done`'s bare delete, with a Map-scoped `data-verb-error` marker, a render-scoped `role="alert"`, and a `scrollIntoView`/`releaseReload` transition pair on that marker (§2.1); `worker_kick`/`mine_run` stop erasing the failure on `not result.ok` — no forced refresh, no `HX-Redirect` (§2.2). Three residuals recorded on the row rather than fixed here, per spec §6: **(a)** the CLI's own fail-open exit codes (`worker kick`/`mine run` both return `EXIT_OK` for outcomes a human would call a non-fire, e.g. `disabled`/`held-gate`) are a separate unit — see **FW-85**; **(b)** `mine run`'s `landed-uncommitted` (`gitops.EXIT_HALF_WRITTEN`) correctly renders `failed`, but the strip doesn't carry the recovery (`self-learn reconcile`), and this is the one failure outcome in scope that *writes*, so no tab's view updates until it navigates; **(h)** the blast radius is app-wide by construction — `_publish_applying`'s three OTHER call sites (`action_confirm`, the route retry, the proposal-bar verb) now also render a persistent failed strip in every connected tab on failure, accepted because the broadcast there is deferred-never-dropped (unlike Force run's own fix) and every hold has a user-reachable release (navigation) |
| FW-77 | **The FW-71 terminology glosses are hover-only — a keyboard user never sees them.** 9 of the 11 gloss sites shipped in `f8a7523` are `title=` attributes on spans/buttons/headings; `title` fires on mouse hover and does NOT surface on keyboard focus. This app is explicitly keyboard-first (`s`/`d` walk, arm-then-confirm, `?` overlay), and the cold-open walker persona that motivated FW-71 drives by keyboard — for that user only the help-overlay "arm" definition is reachable. The choice matches existing house style (the status strip's `title=` glosses on master have the same property) and adds zero always-visible chrome (O-9 principle 3), so it is a gap in the *convention*, not a defect in the change — which is why the code gate ROUTED it as a values question instead of deciding | ROUTED (user) | Making definitions keyboard-reachable (focus-triggered tooltips, an expanded help-overlay glossary, or visible-on-focus hints) is design work under the parked-but-governing O-9 retrofit (`03-decisions.md` O-9). Belongs to the user's design-round call; until then, keyboard users have the `?` overlay only |
| FW-78 | **S-22 funnel — a plainly-proposed user lesson cannot reach the pathed tier from the surface.** `U-demand-user` widens the `o`-cycle to offer a record's OWN already-proposed rules topic, but a proposal with no `rules_topic` at all still renders a singleton cycle, and nothing on the bar says Iterate can supply one. `partials/action_bar.html:285-287` (the `_rules_topic` decode / Destination button) is the natural site; the edit is scope-conditional, not a one-string change — rules are unavailable at skill scope (P-A13, `_SCOPE_DESTINATIONS`, `models.py:108-112`), so the affordance can only ever appear for user/project records. Wording is `U-composer`'s doctrine question, since it is the unit that would make Iterate emit a rules topic in the first place. Source: `U-demand-user` §7.2 / §9.3 | BUILD | Needs `U-composer` landed first (the analyst-side half that would populate `rules_topic` on a fresh proposal) before the UI affordance has anything to invite the human toward. Recorded so the gap does not read as forgotten. |
| FW-79 | **Bucket-page grouping files a pathed rule under the plain claude-md heading.** `_group_key_for` (`models.py:1320-1326`) keys on the bare destination enum (`item.get("destination")`, always the unqualified `"claude-md"` string per `list --json`'s own shape, `ledger_ops.py:1807`) — it never sees the qualified `claude-md:rules:<topic>` string `U-demand-user` introduced, so a pathed-rules row and a plain-claude-md row land in the SAME group under `_GROUP_LABELS`/`_GROUP_ORDER` (`models.py:494-511`), both enum-keyed by P-A11. A rules topic is a scope PARAMETERIZATION of `claude-md`, not a sixth destination (R-2, `ledger_ops.py:1325-1329` — the same doctrine this unit's own `rules_dest`/`destination_cycle_for` honor), so the fix needs a group-key vocabulary distinct from the destination enum — one no unit currently owns. Source: `U-demand-user` §7.3 / §9.4 | BUILD | Needs a group-key design (e.g. keying on `(destination, rules_topic)` or a synthetic per-topic group) that `U-composer` or a follow-on unit would own; recorded rather than folded into this unit's file set, which the spec's §3.6 scopes to seven specific files. |
| FW-80 | **The card's `flags` vocabulary is display-only and unvalidated.** `U-demand-user` §8A renders `recommendation`/`flags` (`WhyRegion.recommendation`/`.flags`, `models.py`'s `_build_why`) and keys behaviour on `recommendation == "defer"` alone (`proposed_destination`) — every flag (`no-cheap-surface` etc., `TRACE_FLAGS`, `cli/src/self_learn/ledger_ops.py:95-107`) is treated as inert display text, never read by any UI logic. If `U-table` or `U-composer` later needs a specific flag to CHANGE behaviour (e.g. gate a destination the way `recommendation` does today), that coupling has no owner and no agreed vocabulary for which flags are behavioural vs. cosmetic. Trigger: the first flag whose meaning is behavioural, not merely descriptive. Source: `U-demand-user` §8A.3 | BUILD | No action needed until the trigger fires — recorded as a declared residual, not a defect, per §8A.3's own posture. Whichever unit first needs behavioural flag semantics should design the vocabulary split (behavioural vs. cosmetic) explicitly rather than overloading the existing display-only set. |
| FW-81 | **14 UI Playwright tests fail deterministically on this host in an environmental state that began 2026-08-07 between 03:15 and 04:00 — cause unpinned; the repo is exonerated.** The 14: `TestReloadDeferLegF::test_verb_success_defers_then_fires_on_navigation`, `TestSuccessFooterNeverAdvertisesADeadKey::test_route_confirm_footer_advertises_only_keys_that_exist`, `test_js_dom_pane_persistence.py::test_view_swaps_same_id_to_readonly_transcript_and_back`, `::test_close_pane_click_on_bucket_pane_actually_closes`, `TestQuestionMarkSwallowedByTextInputShowsHint` ×2, `TestInFlightDisabling::test_14`–`test_21`. Failure shape: `Locator.click: Timeout 30000ms` — the element is FOUND but never becomes "visible, enabled and stable"; notably the keyboard-path sibling (`…presses_q…`, same element, no `.click()`) passes. MEASURED chronology: a full UI run on master `cbbfd5b` at ~03:15 was green (1212 passed / 1 tolerated / 0 skips); from ~04:00 onward the same bytes fail the same 14 in every configuration tried — full-suite, isolation (14/14, ~30s each), real vs scratch `XDG_RUNTIME_DIR`, and solo runs hours later. RULED OUT by measurement: branch-caused regression (a controlled A/B on the U-composer branch vs master `cbbfd5b`, same machine/hour/env with `realpath` import verification, produced test-for-test IDENTICAL failure lists — that adjudication cleared the milestone merge); a Playwright browser update (zero writes under `~/.cache/ms-playwright` after 03:00); raw host load (steady ~4.3 on a many-core desktop, and the failures are deterministic solo on that same host state). NOT ruled out: a compositor/graphics/session-state interaction with headless Chromium actionability checks, or any other host-side change in that 45-minute window. This row exists so the next session neither re-blames the repo nor trusts a red UI suite as a code signal on this host until the state clears or is explained — check whether a reboot/relogin clears it before drilling | DRILL | First move: re-run one of the 14 after the next reboot/relogin. If green, record the host-state dependence and close as environmental; if still red, bisect the host axis (headed vs headless, a different `PLAYWRIGHT_BROWSERS_PATH` build, a fresh user session) — never the repo axis, which is measured clean. The push discipline meanwhile judges UI suites on NO NEW FAILURES vs this row's list, not on absolute green. **✅ RESOLVED 2026-08-09 ~01:00**: all 14 ran and PASSED, in two independent full UI-suite runs on this same host with no reboot between them (the U-forcefail builder's own run, then the code gate's independent re-run) — the environmental state cleared on its own; cause stays unpinned. The NO-NEW-FAILURES judging standard this row established is RETIRED as of this measurement — ordinary green-suite judging resumes. If the state recurs, this row's bisect-the-host-axis instruction (never the repo axis) still stands. **RECURRED 2026-08-27 (self-cleared again, cause still unpinned).** `test_js_dom.py` and `test_js_dom_pane_persistence.py` were excluded from the UI baseline at 03:50 after failing identically at base and at a build (58 run / 2 failed / hung at #59 — `close_pane` never clickable, the same actionability-timeout shape as the 2026-08-07/08-09 episode). A diagnoser re-ran both files at `50fa815` (no code change) at 22:45 the same day: 98/98 twice, and the full UI suite 1267 passed / 1 failed (the one known pre-existing failure, `test_service_unit.py::test_both_units_document_manual_registration_via_symlink`) — `playwright==1.61.0`/chromium-1228 with no version skew, unique ephemeral ports (`_free_port()`), autokicks already off in conftest. The state cleared on its own again; cause stays unpinned. **New observation, untested as a cause:** the host carried orphaned `playwright-mcp --headless --isolated` processes — leftover from earlier UI-walk runs (`walk4`/`walk5`) — during the 03:50 failure window. **Standing rule, now twice-confirmed:** on recurrence, bisect the HOST axis only — never the repo axis, cleared by A/B twice now (2026-08-09 and this episode) — and judge push-readiness on NO NEW FAILURES against whatever this row's list is at the time, not on absolute green |
| FW-82 | **Background review automation — an agent drains the review queue under a user-configurable tiered autonomy policy (S-29).** What already exists (~90% of the plumbing): the thin-caller review workflow whose every mechanic is a CLI verb with a documented exit-code contract (`commands/review.md`), the M2 worker producing traced proposals by construction (S-26), the card-section registry, the proposal secret scan, and the miner's systemd unit/timer pattern (`systemd/self-learn-miner.timer`) to copy for scheduling. What's missing: (1) the autonomy-policy config in ledger `config.yaml` (S-10 opt-in pattern) declaring per-resolution-class tiers; (2) the autonomous reviewer prompt — an escalation-digest variant of the review command where cards above the configured tier accumulate into a digest instead of `AskUserQuestion` taps; (3) a runner + third systemd unit/timer sequenced AFTER a worker run; (4) **FW-85 as a prerequisite** (re-pointed 2026-08-09 from FW-76, per that spec's own merge obligation) — the CLI's Force-run exit codes are fail-open (`worker kick`/`mine run` both return `EXIT_OK` for outcomes a human would call a non-fire), a fail-open signal unattended operation cannot sit on. FW-76 (FIXED 2026-08-09) gives the UI's *human* a truthful Force-run signal but explicitly does not give *automation* a machine-readable one — the exit code itself is what doesn't distinguish the outcomes, and FW-85 is the unit that would fix that. Hard floors regardless of config: hook routes and secret-scan blocks always escalate to the human (S-29). Blocked behind the supervised first drain — Checkpoint A's one-time before/after evidence plus one interactive calibration batch (S-29 sequencing ruling) | BUILD | After Checkpoint A's supervised drain: policy spec → two-gate pipeline → supervised maiden run of the reviewer → timer enabled. Reviewer model per S-18's judge tier |
| FW-83 | **The unattended analyst cannot reliably emit valid S-26 traces — measured batch yield 13–30% valid; the retry chain is blind and unthrottled.** MEASURED 2026-08-08 on the flip's maiden run (`research/2026-08-08-worker-maiden-run-elicitation-failure.md` — read it before touching this): first run timed out at `INVOKE_TIMEOUT_SECS` 900 s with zero writes; the follow-on finished at ~857 s with 3 valid / 7 schema-deleted; an UNBOUNDED replay of the same batch prompt on a scratch ledger clone took 745 s and yielded **2 valid of 15** under `_validate_written` — so time pressure is exonerated, and every refusal is a CONDITIONAL schema requirement (`t4.depth_behind_rule.target/evidence` on yes, `t4.fs.verdict` enum, `t1.field_shaped.answer`, `t3.scan_terms` null-on-yes, one containment refusal of a paraphrased quote). Ruled out by probe: Edit-family Write scoping (intact on CLI 2.1.226, 8 s), MCP/startup hang. Structural half: deleted output produces NO feedback to the next attempt and the follow-on chain has NO failure backoff (`worker.py:2101`) — a persistent failure burns a full analyst call every ~25 min indefinitely (the 01:09Z window was killed by user decision). The attended path already embodies the fix (`proposal validate` reports → session repairs → re-validates, and it drained records the unattended path failed on the same evening). Candidate scope, evidence-ranked in the research doc §"Candidate fix scope": elicitation contract conditionals + worked-example yes-branches; ONE bounded repair round fed the validator's refusal lines; timeout/batch-cap headroom (745–857 s vs 900 s is a coin flip); follow-on backoff; `--strict-mcp-config` hygiene. **Prerequisite to FW-82** (automation needs valid proposals) and to any further unattended drain of the backlog | BUILD | Spec gated r4 CLEARED FOR BUILD (2026-08-08); **build complete** as `U-repair` (worker.py exemplar-driven elicitation rewrite, one bounded repair round guided by a checklist rather than a second refusal line, `INVOKE_TIMEOUT_SECS` 900→1800, a leftovers-conditional follow-on backoff, `--strict-mcp-config` hygiene, Rule-F foreign-proposal detection) — code gate returned **0 BLOCKER** (3 all-test MAJORs folded + re-verified; the gate empirically vindicated the pure `_check_proposal_file` divergence by demonstrating the spec's own `apply` design breaks the lock invariant), merged 2026-08-08 night. Two accepted residuals recorded as `S-30` (a repair that clears one Set-C defect and leaves a sibling is deleted, not re-repaired — one round, not iterated, until the post-merge `repair round: X of N cleared` series says otherwise) and `S-31` (Rule-F leaves a proposal it did not write, matched by `record_sha`, entirely alone — full attribution handed to **FW-84**, added below). S-26's no-fabrication policy binds the repair round: deletion stays the backstop for what a repair cannot honestly fix |
| FW-84 | **Full producer attribution for `worker.py`'s Rule-F foreign-proposal detection — `_written_since` diffs the world instead of recording what the model actually wrote, so four of `U-repair`'s seven declared Rule-F residuals collapse into one root and a fifth needs a different fix entirely.** THE MEASURED INCIDENT, 2026-08-08, both seats at once: ledger commit `efd5ebd` ("worker 3 proposals", 18:09 PDT) landed mid-attended-review-session, DELETED two freshly-validated attended proposals and rewrote two more over the attended session's versions (records still pending; the operator recovered by routing with explicit `--dest`). `U-repair` (§3.8/§7.3) closes only the *destructive* half of the incident it measured: an attended proposal already validated before the worker's landing ran now survives byte-for-byte instead of being staged/stamped/deleted. Four residuals share ONE root and are retirable by a single real per-file provenance signal — parsing `--output-format stream-json` tool-use events, a per-record lease, or an equivalent, none built here because each is a unit's worth of design against a version-fragile CLI surface: an attended proposal written but not yet validated during the model window (no `record_sha` to match, indistinguishable from model output); one edited after validation and caught mid-edit (F-b holds, F-a fails — deleted today, identically, so `U-repair`'s fix is a non-regression here, not a new loss); an attended proposal for a **batch** record that has never passed validation (unstamped, therefore repair-eligible by design, pinned as specified behaviour by `D8(iii)`); and an uncommitted model-authored foreign file (accepted, not fixed — committing it would publish a human's possible in-flight bytes). A provenance fix retires `E-4`/`E-5` (§3.4) along with these four, since those exist only because the signal doesn't. A **fifth** residual needs a *different* direction: the analyst's Write tool can overwrite an attended proposal at the same path before landing ever runs — no landing-time provenance check helps, because the bytes are already gone; the fix is create-only semantics for analyst writes. **Two of the seven are explicitly NOT this unit's**: a genuinely foreign `destination: hook` proposal being stamped, counted and committed regardless of provenance (root: script generation as a stamp-time side effect, `ledger_ops.py:1700-1701`; owner: `U-repair` itself, §3.8, whose un-adopted alternative — regenerate and compare — is named there); and backoff-suppression visibility, which today shows only in `worker.log` (root: operability, §3.10/BD8; owner: **FW-82**). One candidate direction is refused on record and should not be re-proposed: narrowing the *batch* invocation's write scope to the batch's own proposal paths — it doesn't address the measured incident (the attended session was working on records already *in* the batch) and a wrong rule blocks the analyst from writing at all (`U-repair` §7.1). Sequenced **with or immediately after `U-repair`**; until it lands, the operational mitigation stays: never run an attended review session while a worker window is open (`cache_dir()/worker.window` holds the live pid), and route with an explicit `--dest` if a card's proposal vanishes mid-session. Source: `U-repair` spec §7.3 ("seven bullets, three roots") and §8's handoff paragraph | BUILD | Spec gated r4 (mechanically bijective: 49 criteria / 50 mutations); **build complete** as `U-attrib` (an EXCLUSIVE stage the analyst writes to and nothing else can — `Grant-1`'s batch invocation is granted the stage alone; `Install-1` moves a validated staged file into the ledger under the commit lock, atomically, with an install-journal recovery path for a crash between the copy and the stamp; `SELF_LEARN_STAGE=0` reverts the whole namespace for an incident window, independent of `SELF_LEARN_ENFORCE_SCOPE=0`). This closes four of `U-repair`'s five named residuals via the create-only direction (the ACTUAL adopted fix — not stream-json parsing or a lease, both refused on a design argument: two producers legitimately write the same path, so a record of the model's writes alone cannot answer whose bytes these are) and retires `E-4`/`E-5` as designed. All 50 mutations REDDENED (none survived unresolved — every SURVIVED case along the way traced to a test-construction gap, fixed at the test, then re-verified REDDENED). Two accepted residuals recorded as `S-32` (attribution is now structural, not observational) and `S-33` (the permanently-blocked destination, and a mid-window destination change losing one window — both closing the exclusive-namespace direction rather than deferring further). Merged to `master` 2026-08-09 |
| FW-85 | **`worker kick`/`mine run`'s CLI exit codes are fail-open — a no-op and a genuine failure both reach any caller as `EXIT_OK`.** `self-learn worker kick` returns `EXIT_OK` unconditionally (`cli.py:747-750`) across all four outcomes — `spawned \| absorbed-window \| absorbed-race \| disabled` — so a kick that did nothing because autokick is disabled is indistinguishable, by exit code, from one that actually spawned. `mine run` is the same shape for `held-gate` (`EXIT_OK if result.status != "failed"`, `cli.py:666`). **FW-76** (this row's source, FIXED 2026-08-09) gives the UI's *human* a truthful Force-run signal by rendering the CLI's existing `result.ok`; it explicitly does NOT give *automation* a machine-readable one, because the exit code itself doesn't carry the distinction — reading it back out of `--json`/human stdout would violate the ratified "never parse human-formatted stdout" contract (`runner.py`'s own docstring, `07 §4`). This is the row **FW-82**'s autonomy prerequisite (item 4) actually needs, re-pointed here 2026-08-09. Source: `drafts/u-forcefail-error-leg-spec.md` §5 residual (a) | BUILD | Small-to-medium: a real exit-code contract change (new codes, or a `--json` envelope carrying the outcome rather than just ok/fail) at `cli.py:747-750`/`:666` — blast radius is the systemd units, the miner's callers, and their tests. Sequence before FW-82 can unblock |
| FW-86 | **CN9's one-hop taint scan is blind to module-level containment construction — measured evasion, currently unreachable.** The U-seam code gate probed the twin-witness tautology guard (`test_invocation.py`'s CN9, `_one_hop_taint_violations`) beyond the spec's enumerated mutations and measured two residual evasions: two-hop variable indirection (sanctioned by the spec's `D-25` — one hop, deliberately) and a `Containment(...)` collapse at MODULE level, which the scan never visits because it walks `FunctionDef`/`AsyncFunctionDef` bodies only. Attribute-qualified calls (`module.write_permission_rules(...)`) ARE caught — gate probe, not inference. Reachability verified mechanically at the delta round: all four shipped `containment_for`/`Containment` call sites (worker.py 3193/3301, miner.py 779, analyst.py 249) sit inside functions, so the residual is unreachable from shipped code today. Recorded in-spec as §7.3 `R-3` (fold F7). Source: U-seam code gate NOTE-3 + delta obligation 5 | WATCH | Trigger: the first module-level `Containment` construction anywhere under `cli/src/`. Then widen `_one_hop_taint_violations` to walk `Module`-body statements as one more scope (small, test-only change) rather than trusting the residual stays unreachable |
| FW-87 | **`analyst.analyze` loses a routed lesson to a raw traceback on a bare `OSError`.** `subprocess.run` failures other than `FileNotFoundError`/`TimeoutExpired` — `EACCES` spawning the CLI, fd exhaustion, a vanished cwd — propagate uncaught out of `analyze`, so `teach` dies with a traceback instead of the `AnalystError` → capture-to-pending → exit-4 path, and the lesson text the user typed is lost with it. Found by the U-seam spec author (spec `R-1`) and deliberately PRESERVED by U-seam: that unit's charter was byte-identity under `backend=cli`, so the not-caught behavior is pinned as existing behavior (`T-c`) rather than silently changed. The SDK flip dissolves the subprocess layer but not the obligation — the error contract must catch into `AnalystError` on BOTH backends so capture-to-pending holds regardless of the switch. Source: U-seam spec `R-1` | **DONE** | ✅ **FIXED** (`U-sdka`, the analyst-flip build — `invocation/contract.py`'s transport table: `TRANSPORT["analyst"].catches_os_error` `False`→`True`, `_ANALYST_TEMPLATES.os_error` `None`→`"analyst invocation failed ({exc})"`; `analyst.analyze` gains an `outcome.failure == "os-error"` leg rendered through the same template, on both backends). Three things this row did not anticipate, each measured by the build: (1) the fix lands as a **transport-table data change**, not a `try/except` wrapped around `analyze`'s own call (`E-b`) — `CliBackend` and `SdkBackend` both consult the same `catches_os_error` cell, so one edit closes both legs; (2) the sdk leg carries **the same defect under a different exception class** (`claude_agent_sdk`'s `CLIConnectionError`, not `OSError` — `B-7`) — this row's literal "add the `OSError` catch" prescription would have left the sdk leg's hole open, caught by the build's own `M11` mutation self-check (HD2 is the guard that reddens if the sdk leg's catch is ever weakened or merged into the cli leg's); (3) it closes `U-seam` residual `R-1` at the same time (this unit's spec §7.3, folded into this same disposition rather than a separate row) |
| FW-88 | **On hosts with strict global Claude settings, the sdk backend's opened `SELF_LEARN_ENFORCE_SCOPE=0` hatch is WIDER than the cli path's.** On the cli path, `ENFORCE_SCOPE=0` omits `defaultMode` from the written settings file and the HOST'S OWN global settings then decide what follows — on this machine `bypassPermissions` (measured), so U-sdk's charter mirroring the hatch as unconditional-approve is an exact behavioral mirror HERE. But on a host whose global settings are stricter, the cli path would still refuse some of what the sdk hatch approves — the one place in U-sdk where the sdk path can be wider than the cli path, reachable only when the operator has deliberately opened the incident escape hatch. Narrowing it would mean loading the settings source the isolation design (`setting_sources=[]`, F-C) exists to keep out. Recorded by the U-sdk spec as its `R-2` residual (fold r3, 2026-08-09); shipped with U-sdk (merged 2026-08-10). **Amended 2026-08-25 (U-cleanup-B, `03-decisions.md` `S-49`):** the comparison this row names — sdk hatch vs. cli hatch — no longer has a cli side to be wider than; `CliBackend` is deleted. The underlying breadth (unconditional-approve for every non-deny-listed tool once the hatch is open) is unchanged and stays a real, live trap on the one remaining backend — `17-invocation-runbook.md` §7 trap 6 already documents it without the retired comparison. This row's premise is CLOSED by the deletion; the trap it points at is not. | WATCH | Trigger: self-learn deployed to a second host with a stricter global permission posture AND an incident window there. Until then the divergence is unreachable-in-practice; if it fires, the candidate directions are a host-posture probe at hatch-open time (one settings read, done outside the session) or documenting the hatch as sdk-semantics-only in the runbook |
| FW-89 | **Under `backend=cli`, `provider: bedrock` is silently inert, BY DESIGN — the CLI leg assembles no environment, so a bedrock config does nothing for that surface and the child inherits whatever the operator's shell already sets.** Ruled `D-18`: this is the staged rollout's normal intermediate state (analyst → miner → worker never flip together), not a defect, and a runtime warning on the combination would fire on every invocation of every correct rollout. What catches the forgot-to-flip trap instead: the doctor's `rollout` row FAILs a wholly-inert config (`Doc-f`, `DC3`) and renders every other mixed state as per-surface INFO (`DC12`). Recorded by the U-bedrock spec as its `R-1` residual. **Amended 2026-08-25 (U-cleanup-B, `S-49`):** the row's whole subject — Bedrock silently inert under `backend=cli` — is CLOSED by the deletion; no surface can be on `cli` any more, so there is no "silently inert" state left for `provider: bedrock` to fall into via that mechanism. The docs line this row asked for already shipped (`17-invocation-runbook.md` §3, unchanged by this rewrite) and stays correct advice regardless. | BUILD | ✅ CLOSED (U-cleanup-B, subject retired — not a commit reference, this worktree is uncommitted per its own operating rules) |
| FW-90 | **The IMDS / EC2 instance-profile credential mechanism is not probed (`Cred-1`), so a doctor run on an instance with an instance role reports no mechanism while credentials in fact resolve — this is why the credentials row's verdict is WARN and never FAIL (`Doc-g`).** Deliberate: probing IMDS is a network call, and this unit makes none. A check that cannot distinguish "absent" from "invisible to it" must not FAIL. Recorded by the U-bedrock spec as its `R-4` residual. | WATCH | Trigger: an operator running self-learn from an EC2 instance relying on an instance role sees a WARN they cannot silence. Candidate direction then: an opt-in IMDS presence probe (bounded timeout, off by default) — not attempted here because it would be this unit's only network call |
| FW-91 | **`test_selftest_reports_seven_checks_criterion_12`'s NAME goes stale as of this build** — the selftest's two "seven"/"seven checks" strings became "eight" (`B-5`, U-bedrock's 8th `invocation` row), but `SU2` forbade renaming the test in the same unit that changed the count (a rename would be a third changed line beyond the two sanctioned edits). Recorded by the U-bedrock spec as its `R-5` residual. | BUILD | Owner: an FW row (this one) until a later unit touching `test_selftest.py` renames the function to match; trivial, test-only, no behavior change |
| FW-92 | **The failure class this unit's suite structurally cannot reach: IAM denials, model-access-not-granted, region-not-enabled, inference-profile-not-found, and throttling all surface only against a live Bedrock endpoint — no criterion in U-bedrock's §4 covers any of them, because no live call is ever made.** The doctor's handoff block (`Doc-d`) — provider/backend/region/profile/model/sdk-version/cli-version/credential-mechanisms, all pure and non-network — is the entire mitigation available before a live run. Recorded by the U-bedrock spec as its `R-7` residual. | WATCH | Trigger: the first operator with real AWS credentials runs self-learn against Bedrock. That first live run is itself an experiment; its result (which failure class it hits, if any, and whether the handoff block predicted it) belongs in the ledger, not just this row |
| FW-93 | **U-sdk's own spec text is now VOID on one point: `u-sdk-backend-spec.md` §3.5's `A-4` states "`options.model` can never be `None`", describing a shipped mechanism (an argv `--model` flag always present or defaulted) that `U-bedrock`'s `IN3`/`Int-1` wiring replaced.** `options.model` is still never `None` post-wiring, but for a DIFFERENT reason (`provider.model_for(...)`'s own total rule, not an argv-flag-or-`None` read) — the conclusion survived, the stated mechanism did not. Ratified alongside the `OP11`/`OP13` rewrite this fold required (`S-38`). | BUILD | Owner: the next unit that touches U-sdk's spec text — correct `A-4`'s stated mechanism to cite `model_for`'s totality instead of the argv read. Small, docs-only; found at this fold's `S-38` |
| FW-94 | **New per-run operational behavior, sanctioned but previously absent: on any install with the `[sdk]` extra, `self-learn --selftest` now spawns a real `claude --version` child process on every run.** Before U-sdk shipped `SdkBackend` and this unit wired `preflight` into both `doctor invocation` and the selftest's `invocation` row (`DC11`), no selftest run spawned any subprocess for this purpose. Now, whenever `claude_agent_sdk` is importable, `_sdk_row` reaches `Doc-a`'s one permitted spawn (`[<resolved claude>, "--version"]`) on every invocation — the zero-spawn path only applies to installs WITHOUT the `[sdk]` extra. The spawn itself is sanctioned (`Doc-a`, byte-pinned, `timeout=10`, three-exception SKIP) — this row exists so the new steady-state cost (one child process per selftest run, for sdk-extra installs) is a documented decision rather than a surprise an operator discovers from process-table noise. | WATCH | Trigger: an operator on an sdk-extra install notices the per-run spawn (process monitoring, sandboxed/offline CI, or a slow/hung `claude` binary making selftest itself slow) and asks why. Candidate mitigation then: cache the host CLI version check across a short TTL, or make the spawn opt-out — neither built here, since it would be optimizing an unmeasured cost |
| FW-95 | **The `cost ≤ 1.5× cli baseline` burn-in gate has no instrument on the `cli` side of the comparison.** `Outcome` (`invocation/contract.py`) carries `ok, rc, stdout, detail, failure, exc` and no cost or turn field; only `SdkOutcome` (`invocation_sdk/backend.py`) adds `cost_usd`, `turns`, `session_id`, populated from `ResultMessage`'s `total_cost_usd`/`num_turns`. **`CliBackend` never returns an `SdkOutcome` and has no code path that would populate either field**, so the `cli` baseline the cross-cutting gate compares against cannot come from this product. Consequence: as written, the gate is half-measurable — the `sdk` side self-reports, the `cli` side does not — and the "isolation should be CHEAPER" heuristic (`setting_sources=[]` + `settings=None` mean the sdk session loads no settings file and no CLAUDE.md, so its input token count should be *lower* than the cli child's, which inherits the host's) inherits the same gap. The runbook (`17` §5.5) states the two substitutes available today — an external control run of `claude -p --output-format json` capturing `total_cost_usd`, or the provider's own usage reporting for the window — and states plainly that neither is built. Found by `U-docs` while writing the burn-in section; not fixable by a docs unit. **Resolution direction, ruled 2026-08-19: the denominator comes from the operator's own billing surface (the API console's usage for the window), NOT from the product** — so the gate is executable today by an operator, and no code is owed for the ratio itself. What remains open is the *method*: console-window comparison is coarse (it cannot attribute cost to a surface), and a per-run figure would need either `CliBackend` reading `--output-format json`'s `total_cost_usd` or an external control run. | BUILD | ✅ CLOSED (U-cleanup-B, `S-49`, 2026-08-25 — uncommitted, worktree rule, not a commit reference): the burn-in's 14-day soak precondition was waived (2026-08-24) rather than completed, and `cli` is now retired — there is no `cli` baseline left to compare a cost ratio against, ever, and the "external control run of `claude -p --output-format json`" substitute this row names is now impossible (`CliBackend` deleted). **Action superseded, not performed.** *Amended 2026-08-25:* pick the method and write it into `17` §5.5 **before the first burn-in closes** — a ratio whose measurement was decided afterwards is a ratio nobody trusts. Owner: the post-burn-in recalibration unit `U-sdk` §7.3 `R-7` already names, if a code-side figure is wanted; otherwise the first burn-in itself closes this row by recording what it actually measured. |
| FW-96 | **`12-transcript-miner.md` contradicts itself on the miner's scheduler.** §3 says *"**Trigger: nightly `cron-claude` timer** (the house scheduler)"*; §6 Q5 says *"the schedule is a plain systemd user timer executing `self-learn mine run` — not literally a cron-claude job"*; the shipped artifact is `systemd/self-learn-miner.timer` + `self-learn-miner.service`, so §6 Q5 is right and §3 is stale. Found by `U-docs`'s inventory sweep (`I-26`) and left uncorrected **because it is out of that unit's mandate** — a scheduling claim, not an invocation claim — and correcting it silently under an invocation-layer scope fence is exactly the smuggling the two-gate discipline exists to prevent. | BUILD | *Action:* one-clause substitution in §3 with a dated marker, by whichever unit next has doc 12 in its may-touch table. Trivial, docs-only, no behavior change. |
| FW-97 | **`docs/specs/self-learn/README.md`'s revision log is ~26 days and roughly two dozen shipped units stale.** Its last entry before this unit's is **2026-07-24**; between then and `89f8ef7` the repo shipped (at least) `U-repair`, `U-attrib`, `U-pointer`, `U-cursorhold`, `U-forcefail`, `U-seam`, `U-sdk`, `U-bedrock` and `U-fake`, none of them logged. `15-orchestration-runbook.md` §1 step 7 makes the entry part of round-close, so the gap is a **process** miss, not a docs backlog: the log is the corpus's index into its own history, and an unlogged unit is one a future reader cannot find from the front door. `U-docs` appended its own entry (`Sub-18`) and **deliberately did not back-fill the others** — reconstructing nine round-closes from git history and spec drafts would produce a plausible narrative rather than a record, which is the opposite of what the log is for. | WATCH | *Action:* the honest fix is one entry per unit written by whoever still remembers the round, or an explicit dated note in the log declaring the gap and pointing at `records-index.md` and the git log instead. Decide which; do not silently reconstruct. |
| FW-98 | **`test_invocation_sdk.py::_miner_argv()` is now a stale mirror of `miner.build_reader_argv` — it omits `--strict-mcp-config`, which the product now carries.** After `U-sdkr`'s `Fix-1`, the reader's real argv ends in `--strict-mcp-config`; this test-local literal (used only to build hand-constructed `SessionSpec`s for `backend.py`'s pure-function `OP`-group tests) does not, and nothing asserts on the difference — `backend.py` cannot read the flag at all (`B-4`: `test_op13_argv_read_set_is_closed` closes the SDK's argv read set at exactly `--append-system-prompt`). Deliberately not fixed here (`D-4`): touching U-sdk's frozen-by-convention test file for a cosmetic mirror spends footprint this unit does not need. Source: U-sdkr spec `R-1`/`D-4`. | BUILD | Owner: a later unit that already touches `test_invocation_sdk.py` for its own reasons — fold the fix in then rather than reopening the file solely for this |
| FW-99 | **The analyst's argv has the identical strict-MCP gap `U-sdkr` closed on the reader, and it is deliberately NOT fixed — ever, on the CLI path.** `analyst.build_argv` omits `--strict-mcp-config` exactly as `miner.build_reader_argv` did before this unit's `Fix-1`; the analyst carries no settings file and no write scope at all (`U-seam` `C-d`), so U-seam's original faithful recording of a genuinely weaker boundary stands. **Ruled at this unit's `Q-2`, per the user's own `F3` ruling (2026-08-09):** *"wait for the SDK flip — no CLI-side carve-out; the analyst flips FIRST and its hardening rides that flip."* `U-sdka` performs that flip this wave, after which the analyst's CLI path becomes rollback-only; the no-carve-out stance covers the rollback path too. Source: U-sdkr spec `R-2`/`Q-2`. | WATCH | Owner: **nobody, deliberately** — not deferred, accepted. Revisit only if the analyst ever un-flips DURABLY; a transient rollback is not a trigger |
| FW-100 | **The reader's 15-minute timeout has no env override, and under `sdk` it is no longer the only bound — `max_turns=60` can end a session first, and the two have never been reconciled.** `worker.invoke_timeout_secs()` reads `SELF_LEARN_INVOKE_TIMEOUT_SECS`; `miner.INVOKE_TIMEOUT_SECS` is a bare module constant with no equivalent. Whether a turns-starved reader ever actually surfaces is unmeasured (`VB-3`). Source: U-sdkr spec `R-3`/`Q-4`. | WATCH | Owner: **the miner-flip decision** (not this unit, which does not flip). The observable is the plan's miner burn-in gate — candidate volume watched against ±1σ is exactly the signal a turns-starved reader would move; a runbook miner-flip section (pending — see FW-102) is the eventual home for this row's watch instructions. **Disposition 2026-08-26 (U-fw100):** the env-override half is CLOSED — `miner.reader_timeout_secs()` now reads `SELF_LEARN_READER_TIMEOUT_SECS`, sharing `worker._timeout_secs` with `worker.invoke_timeout_secs()`'s exact parsing/validation/fallback semantics (no import cycle — `miner.py` already imports the `worker` module). Both call sites (`timeout=`, `timeout_display=`) now route through it. The `max_turns=60` reconciliation half is UNCHANGED and stays WATCH, owner as above — U-fw100 does not flip the miner-reader backend and does not touch `invocation_sdk/backend.py`. |
| FW-101 | **`Containment.strict_mcp` is read by no production code, and that is the design, not a wart — recorded so a future "dead field" cleanup finds the reason before deleting the witness.** The SDK sets `strict_mcp_config=True` unconditionally; the CLI backend never consults the field either — each surface's argv builder decides the flag independently. The field's only consumer is `test_invocation.py`'s `CN2`/`CN10` pair: the second, independent witness that makes a one-sided argv edit red in both directions. **Ruled at this unit's `Q-3`:** keep it, and do not derive it — deriving the flag from the containment would collapse two witnesses into one and re-open the tautology class `U-seam`'s negative controls exist to prevent. Source: U-sdkr spec `R-4`/`Q-3`. | WATCH | Owner: **this spec** (`docs/specs/self-learn/drafts/u-sdkr-reader-contract-spec.md`) — no action expected; this row exists purely so the invariant is discoverable before it is "cleaned up" away |
| FW-102 | **The first post-merge nightly miner run is a WATCHED run — the binding enforcement for `U-sdkr`'s one live-behavior change (`Fix-1`, the reader's argv now carries `--strict-mcp-config`), now that live verification is an operator preflight rather than a build blocker.** The operator reads `miner.log` the following morning and confirms: the run reached the reader, the reader produced `mine-output.json`, and no new diagnostic line appeared. This row also carries the operator preflight command forward (§8.1 `Mit-a`, run once, attended, before the miner flip — never by an agent): seed a minimal settings file (`{"permissions":{"allow":[],"defaultMode":"default"}}`), then `timeout 10 claude -p --disallowedTools 'Bash,Edit,NotebookEdit,Task,WebFetch,WebSearch,Read,Grep,Glob' --settings <that file> --strict-mcp-config < /dev/null` — a `timeout`-killed run with no usage error is a pass. Source: U-sdkr spec `Mit-a`/`Mit-b`/`Mit-c`/`Mit-c1`. | WATCH | Trigger: fires once, on the first nightly miner run after this build merges. Home: **U-docs**' eventual runbook (no operations/watch-list/miner-flip section exists in-repo yet — `15-orchestration-runbook.md` is orchestration-only); until U-docs materializes, this row is the obligation's authoritative record. A builder who finds a watch list already present when they build should put this obligation in it and say so |
| FW-103 | **The worker's `sdk`-leg attribution proof (`WS6`) does not show that a real SDK tool executor honors the charter's verdict — only that the charter's verdict and the resulting filesystem state agree, because `Fake-3`'s `ok_write_real` writes from the scenario body after reading the permission response, not through an executor.** Ruling `V-2` closed the ORIGINAL gap (`RP4`/`WS6` are now real end-to-end drives, not absence assertions); what remains is a narrower, unclosable-in-any-suite property that folds into `R-2`/`FW-104` (no criterion anywhere observes a real model). Recorded by the U-sdkw spec (`docs/specs/self-learn/drafts/u-sdkw-worker-contract-spec.md`) as its `R-1` residual (narrowed from r1's coverage-hole form by `V-2`). | WATCH | Trigger: the worker's live burn-in (5 clean unattended cycles, ≥1 repair round) is where this class of gap would first surface — a real tool executor ignoring or misreading a charter deny |
| FW-104 | **No criterion in U-sdkw (or U-sdk before it) drives a session against a real model.** Every leg runs against a bash shim or the fake CLI; the classes only a live session can surface — a model that ignores the charter's deny reason, a `ResultMessage` shape the SDK changes, rate limits mid-batch — are structurally out of reach. Recorded by the U-sdkw spec as its `R-2` residual, mirroring `U-sdk`'s own (`FW-92`'s Bedrock analogue). | WATCH | Trigger: the worker's live burn-in IS the experiment (the approved migration plan's gate: 5 clean unattended mine→worker cycles including ≥1 repair round, 0 out-of-scope write attempts — `Outcome.denials` empty and the filesystem diff agrees, clean commit/push). That first live run's result belongs in the ledger |
| FW-105 | **`BG3`'s `sdk`-leg witness for the >128 KiB prompt rides an undocumented fixture behavior: `fake_claude.py`'s unknown-scenario echo (`B-6`), never specified as an interface.** U-sdkw makes it load-bearing and pins its bytes (`SU4`), but does not promote it to a documented contract. Recorded by the U-sdkw spec as its `R-3` residual. | WATCH | Trigger: the first unit that wants to change `fake_claude.py`'s error path (`error_unknown_scenario`) — it must re-read `BG3` first, since that criterion's length relation (`len(detail) == 30 + len(repr(BIG_PROMPT))`) depends on the echo's exact prefix |
| FW-106 | **Ruled (`V-3`): `worker.tool-events.<run_id>.jsonl` carries tool inputs (file paths today, whatever a future tool's input carries tomorrow) written to `cache_dir()` unscrubbed, retention 20 — no design change.** The cache is local-only, never committed, never synced, and already holds unscrubbed `worker.log` and batch prompts (existing practice, not a new exposure). The scan/scrub obligation attaches AT THE SURFACING BOUNDARY, if and when a consumer surfaces events into the ledger or the UI — not at capture. Recorded by the U-sdkw spec as its `R-4` residual (ruling `V-3`). | BUILD | Owner: `U-corrob` (the tool-events consumer, post-burn-in) — the surfacing-boundary sentence above is the obligation that unit inherits |
| FW-107 | **Ruled a REAL finding (`V-4`): a fully-denied `sdk` worker run and a wrote-nothing run are indistinguishable in the operator-visible log.** Measured: a run where every write attempt was denied by the charter produces the identical `run: FAILED — 1 eligible, 0 valid proposals (last-run not touched; staleness alarm is the detector)` line as a run where the model wrote nothing at all. The distinguishing information EXISTS (the denial is in `worker.tool-events.<run_id>.jsonl` with `"source": "charter"`) and nothing reads it. Surfacing a denial count in the run line is a one-line product change, out of scope for U-sdkw (`D-15`/`D-19` — this unit ships no product code). Recorded by the U-sdkw spec as its `R-9` residual. | BUILD | ✅ CLOSED (U-opsfix, 2026-08-26 — uncommitted, worktree rule, not a commit reference): `worker.py`'s batch/repair invocations now thread each `SdkOutcome.denials` entry with `source == "charter"` back through `_invoke_claude` (an additive `charter_denials` accumulator, not a return-value change — `_invoke_claude` still always returns `None`, per `test_invocation.py::test_wr1_invoke_claude_signature_and_never_raises`, pinned). When the run ends FAILED and that count is nonzero, a `run: {count} charter denial(s) this run — see worker.tool-events.*.jsonl in <cache dir>` line follows the byte-pinned FAILED line, which is itself unchanged; `<cache dir>` is the resolved `worker.cache_dir()` (gate r1 N-2 — no `run_id` is in scope at that point, so the reference is a resolved directory plus the file glob, not a literal filename). |
| FW-108 | **On the `sdk` backend, a `CharterPatternUnsupported` failure is SILENT in `worker.log`; the `cli` backend's equivalent `OSError` logs its line.** Found by U-sdkw's blind gate while falsifying `FL2`'s byte-identity claim. `invocation_sdk/backend.py`'s `CharterPatternUnsupported` handler inside `_drive` builds the `Outcome` directly (`failure="os-error"`) and returns WITHOUT rendering `LOG_TEMPLATES["worker"].os_error`; `CliBackend` on the same failure kind logs `run: {label}claude invocation failed ({exc})`. An operator reading `worker.log` after a flipped run sees nothing for a class of failure that is loud today — the only trace is the `Outcome` the worker discards. Recorded by the U-sdkw spec as its `R-10` residual (`FL2` is scoped to the shipped, silent behavior on this leg, not tested around). **Amended 2026-08-25 (U-cleanup-B):** the row's comparison partner (`CliBackend`'s equivalent, logged `OSError` path) is deleted — this is now a description of PAST behavior, not a live thing to diff against. The defect itself, on the sdk backend, is untouched by the cleanup and stays open on its own terms. | BUILD | Owner: the worker-flip decision or a small operability unit — unaffected by U-cleanup-B, not addressed by this build (out of its deletion-only scope). ✅ CLOSED (U-opsfix, 2026-08-26 — uncommitted, worktree rule, not a commit reference): the `CharterPatternUnsupported` handler in `_drive` (`invocation_sdk/backend.py`) renders the **same template under the same surface guard** as its `ClaudeSDKError`/`OSError` neighbours (`_CATCHES_OS_ERROR.get(surface, True) and templates.os_error is not None`), and — unlike them — **never re-raises**, because no session has started yet; the returned `Outcome` (`failure="os-error"`) is unchanged either way (gate r1 `M-1`, ruled). |
| FW-109 | **The analyst's SDK path has no `--settings` witness to cross-check, because it has none.** `U-seam`'s twin-witness design gives the analyst an absence-asserting leg (`CN6`) under `backend=cli`; under `SdkBackend` the containment is enforced by the charter callback (`build_can_use_tool`) instead, with no on-disk settings file to diff against. The cross-backend agreement between "what the charter denies" and "what the CLI's own rule evaluation would deny" is `U-sdk` §8 row 7 / residual `R-5`, and stays unmeasured — not reachable for the analyst today (its containment carries empty write sets on both backends, so there is nothing for a settings-witness comparison to catch). Source: `U-sdka` spec §7.3 `R-1`, folding forward `U-sdk`'s own unmeasured `R-5`. | WATCH | Trigger: the analyst's containment grows a non-empty write set (write_globs/write_exact), at which point the charter-vs-settings agreement becomes reachable and worth a real cross-backend measurement — building the comparison harness before then would be exercising a check with nothing to catch |
| FW-110 | **`U-seam`'s `R-2` residual — the surfaces' log wording still diverges — never got its own row here; recorded now, folding forward `U-sdka`'s addition to the divergence.** `U-seam`'s own spec (§ residuals) named this a "new row" obligation at its merge (alongside `R-1`/FW-87 and the `R-3` fold/FW-86), but only those two landed in the U-seam code-gate fold (2026-08-09 night, see the entry below) — this one was missed. Recorded here rather than left an orphaned citation once `U-sdka`'s spec pointed at "U-seam's existing FW row" and found nothing there. The divergence itself: worker/worker-repair share one `LogTemplates` table, the miner carries no `run: ` label and different wording, and the analyst has no `run: ` prefix, no truncation, and strips — four surfaces, three distinct registers. `U-sdka` adds a **fifth** analyst string in the same divergent register rather than unifying (`os_error`, `"analyst invocation failed ({exc})"` — FW-87/this build): unification is operator-visible wording change and needs its own unit, out of scope for a bug fix. Source: `U-seam` spec `R-2`; `U-sdka` spec §7.3 `R-2`. | BUILD | Not scheduled. Whichever unit next touches `LogTemplates` for any surface should read this row first — the four (now five-leg) registers are divergent BY HISTORY, not by a decision anyone has ratified, and unifying them is a wording change every operator log line would show |
| FW-111 | **The `cli` rollback switch (`SELF_LEARN_BACKEND_ANALYST=cli`) restores the F3 record-body-in-argv exposure the flip was built to remove — dormant, not deleted.** `U-sdka`'s flip moves the analyst's default off the cli transport, whose `prompt_via_argv=True` row puts the record body on the process argv (visible to any local `ps`/`/proc` reader) — the exposure `U-seam`'s `R-3`/this build's `F3` framing names. The rollback switch exists deliberately, for exactly the case where the sdk path itself needs to be bypassed (e.g. `V-1`'s dev-dependency-group condition, FW-112) — and using it re-opens F3 for the duration. This is the correct trade for an operator-facing escape hatch, not a defect, but it means F3 is closed on the default path only. Source: `U-sdka` spec §7.3 `R-3`. | WATCH | ✅ CLOSED (U-cleanup-B, 2026-08-25 — uncommitted, worktree rule, not a commit reference): the named trigger fired exactly as anticipated. `SELF_LEARN_BACKEND_ANALYST=cli` (or any selector) is no longer a rollback switch to a real `CliBackend` — it is a NAMED REFUSAL (`BackendUnavailable`), and there is no `prompt_via_argv=True` transport left for it to route the record body through. F3's argv exposure and the rollback switch that dormantly re-opened it are gone together, as this row predicted. |
| FW-112 | **The analyst's shipped `sdk` default is buildable only because `claude-agent-sdk` sits in a DEV dependency group — an environmental condition, not a code property, and `V-1`'s refusal-of-Option-B ruling rests on it.** Both entry points (`~/bin/self-learn`, `install.sh`) resolve `[dependency-groups] dev` through `uv run`/`uv sync`, so the SDK import succeeds today; `U-sdka`'s `V-1` ruled **Option A (refuse to ship if the SDK is unimportable)** rather than falling back to cli silently, and wired the refusal to be loud and never-lost (`FL7` — a fail-closed `BackendUnavailable`, not a silent downgrade). This is an accepted condition, not a defect, but it is contingent on how this host installs, not on anything the code guarantees. **Trigger, any one re-opens `V-1` (`V-1a`):** (1) an entry point stops resolving the dev group — a `--no-dev` invocation, or a uv default change; (2) the CLI starts being installed from a built wheel, where PEP 735 dependency-groups are absent from the metadata entirely; (3) a second surface flips its default to `sdk`, widening the failure from "the analyst stops routing" to "the worker or miner stop working" — a materially worse failure mode than today's single-surface exposure. Source: `U-sdka` spec §7.3 `R-5`. | WATCH | ✅ CLOSED (U-cleanup-B, `03-decisions.md` `S-49`, 2026-08-25 — uncommitted, worktree rule, not a commit reference): superseded, not merely triggered. `claude-agent-sdk` moved from a dev-only dependency group to a HARD dependency of `self-learn-cli` (`S-49` amends `S-43`) — the whole "buildable only because it sits in a dev group" condition this row worried about no longer applies, because it ships unconditionally now. Condition (3) (a second surface flipping its default to `sdk`) also happened and then some — all four surfaces default to `sdk`, and there is no `cli` fallback left for a broken SDK import to fail open to; a broken install now refuses loudly (`registry.py`'s `_SDK_UNAVAILABLE_MESSAGE`) rather than silently degrading. |
| FW-113 | **A rung-1 env shadow (`SELF_LEARN_BACKEND_<SURFACE>=sdk` set ambiently) is a LOWER BOUND on a default-rung flip's blast radius, not the built state's actual failure set — and the next flip's test surface is far larger.** `U-sdka` measured its own flip's blast radius this way (`SHADOW_22`, spec §9 `E3`) before building, and found the technique exact for every test that never touches the shadowed variable, but wrong in both directions for tests that do: one that clears the variable mid-test diverges from the built state INSIDE a single test (missed by the shadow — `E12`), and one that sets a lower rung to the same value the shadow already forces is MASKED by the shadow rather than exercised (`E13`). The two casualties this missed (`LATENT_2`) were only found by a second pass reading `_clear_backend_env`/`_clear_provider_env`'s call sites directly, not by the shadow run. `Wave 3+` flips the worker and miner-reader, whose test surfaces (`test_route_cli.py`, worker/miner integration suites) are far larger than the analyst's ~23-test blast radius. Source: `U-sdka` spec §7.3 `R-6`. | WATCH | ✅ CLOSED (U-cleanup-B, `S-49`, 2026-08-25 — uncommitted, worktree rule, not a commit reference): the worker and miner-reader flips (U-flip, 2026-08-23) both landed, and there is no further flip left to measure blast radius for — `KNOWN_BACKENDS == ("sdk",)`, so every surface's only possible value is already `sdk`. The shadow-technique caveat this row recorded (masked-by-shadow tests, `E13`) is preserved here as a lesson for any FUTURE multi-backend migration, not a live trigger. |
| FW-114 | **`skill:<a> → skill:<b>` re-scope is refused in M1, deliberately — u-rescope §4/§9 item 5.** It is safe in principle (the same mechanism as `user ↔ skill:<name>`) but has no live motivating instance today, and it is the direction where the proposal-sweep hazard (u-rescope §5.3) has its sharpest form: a trace whose destination judgment was owned by skill `a`, re-derived under skill `b`, with no evidence yet gathered on whether that leg's carried-proposal failure mode matches the measured `user ↔ skill` legs or diverges. Source: u-rescope spec §4/§9 item 5. | WATCH | Trigger: a live capture-cwd-under-skill-a-firing-at-skill-b case (the skill-scope analogue of Y-18's ancestor-project case). Building it needs: the target-parsing branch in `_resolve_rescope_target`, the third leg of §5.3's evidence gathering (does a carried skill→skill proposal degrade gracefully like `skill → user`, or survive intact like `user → skill`?), and a ruling on whether the same sweep-with-disclosure design suffices or whether a `skill:<a> == skill:<b>` self-move needs its own refusal too |
| FW-115 | **`project ↔ user/skill` re-scope is refused in both directions — `rescope` excludes `project` on either side, and `rehome` stays `project → project` only — u-rescope §4/§9 item 5.** `project` buckets carry a `meta.yaml` identity invariant (`ensure_project_meta`) that `user`/`skill:` buckets do not, so a project-side move needs a `project_path` that a scope literal (`--to <scope>`) cannot carry; supporting it under `rescope` would make it a superset of `rehome` and force a deprecation ruling neither this unit nor its rationale attempted. No verb today repairs a record captured at project scope that should have fired at user or skill scope, or the reverse. Source: u-rescope spec §4/§9 item 5. | WATCH | Trigger: a live capture-cwd case crossing the project/user-or-skill boundary (distinct from Y-18's ancestor-PROJECT case, which stays inside `project → project`). Whichever unit takes this needs to rule on ownership first — a third verb, or a `rehome`/`rescope` widening — since neither existing verb's `--to` vocabulary (path/slug vs. scope literal) fits a project↔scope move without either inventing a hybrid target syntax or accepting two `--to` shapes on one verb, which u-rescope §3 rationale 1 specifically argued against |
| FW-116 | **The campaign's rollback story ends with U-cleanup — opened, not closed, per `14`'s own docs-table instruction (§16 of the U-cleanup spec).** Every earlier row in this map that treated `SELF_LEARN_BACKEND_ANALYST=cli` (or any selector) as a working escape hatch — FW-88, FW-89, FW-95, FW-111, FW-112, FW-113 above — is now closed BECAUSE that hatch stopped working, not because the underlying operational need for a rollback went away. `17-invocation-runbook.md` §6 states the replacement plainly: rollback is a code revert (`git checkout <pre-U-cleanup-B sha> -- plugins/self-learn/cli && uv sync`), not a flag flip. **What this row exists to flag, forward:** nobody has yet exercised that revert path for real — it is a documented procedure, not a measured one. If the Agent SDK transport breaks in a way that makes an operator reach for it, that will be the FIRST live test of "rollback is a revert," and its outcome (does it actually work cleanly? does a partially-migrated `config.yaml` — Bedrock provider settings written after the cleanup — survive a revert to pre-cleanup code that never learned to read them?) is unknown. Source: `docs/specs/self-learn/drafts/u-cleanup-cli-path-removal-spec.md` §16 ("open a row for the lost env-var rollback"). | WATCH | Trigger: the first real incident that makes an operator attempt the revert-based rollback. Until then: this is a documentation gap, not a code gap — the fix, if any is warranted, is a dry-run of the revert procedure against a throwaway install, recorded here with what it actually found · **✅ CLOSED (FW-116 dry-run, 2026-08-26 — measured, not merely documented).** A dry-run revert to 163a93e (pre-U-cleanup-B) in a throwaway worktree answers both halves of this row. **Revert works cleanly**: `uv sync` restores `claude-agent-sdk` (via the `dev` group, not the `[sdk]` extra — the runbook's "pip install" phrasing would have missed it), cli+ui suites stay green (only the one documented pre-existing UI failure), roll-forward returns byte-identical `pyproject.toml`/`uv.lock`. **Config survives**: `config.py` is untouched by U-cleanup; a synthetic post-cleanup Bedrock `config.yaml` parses with zero unknown-key warnings under reverted code. Two real gaps, in the telling not the doing, both fixed in `17` §6 the same day: the path-filtered `git log` never surfaces 163a93e (history-simplified away), and any CLI verb can silently spawn a real miner run against a stale ledger copy. Full findings: `misc/fw116-dryrun-2026-08-26.md` (local). |
| FW-117 | ✅ **CLOSED 2026-08-28** (unit `u-fw117`, T1 gate-characterized fix; blind Opus code gate CLEAN r3 with mutation verification; merged 2026-08-28). Root cause closed by deletion, per the user's standing ROOT CAUSE preference: `worker.write_repair_settings_file` (`worker.py:917`) — the DEAD WRITE this row named — is DELETED outright, along with its `__all__` entry, its docstring cross-reference (`stage_reset`), and its call site inside the repair round (`worker.py`, `run()`'s repair branch, was `:3250`). Original **`worker.write_repair_settings_file` is now a DEAD WRITE — called, its result discarded, never read**: `options_kwargs()` (`invocation_sdk/backend.py`) builds every sdk session with `settings=None` unconditionally, so the file the function wrote (`worker.repair.settings.json`) had no reader anywhere in the sdk path — the cli-era reader, `--settings <path>` on the argv, left with `CliBackend`. U-cleanup-B §8.1 had deliberately kept the function rather than delete it, because `test_rp1`, `test_ha1_hatch_open_omits_default_mode`, and `test_repair.py::d5` all asserted the file exists on disk as part of the repair contract's own on-disk-artifact guarantee — a guarantee that outlived the transport that used to read it back. **Full deletion census, every site and its disposition:** `worker.py` — `__all__` entry, docstring mention (`stage_reset`), the function body (~46 lines), the call site (all deleted; no orphaned helper/constant — the function's disk-path/rule-rendering logic had no separate extraction, `_p`/`json` stay in use elsewhere; `_enforce_scope`'s docstring rewritten, it no longer describes "both settings files" since neither round writes one). `tests/test_invocation.py` — `_HY3_SHAS`/`test_hy3_witness_b_is_sha_pinned` trimmed from three witnesses to two; `SETTINGS_WITNESS`, `test_cn6_witnesses_a_and_b_agree_statically`, `test_cn7_repair_leg_over_both_enforce_values` deleted outright (no witness function left to agree against — `_FORBIDDEN_TAINT_SOURCES`'s own entry is left in place, matching the standing precedent for the two earlier deleted settings-writers). `tests/test_attrib.py` — `test_gr1_settings_files_enforce_defaultmode`/`test_gr3_repair_invocation_is_exact_path_over_staged_paths` rewritten onto a new `_repair_permissions` helper (mirrors the already-shipped `_batch_permissions` pattern, reading `invocation.containment_for`/`containment_permissions` instead of the deleted function). `tests/test_worker_contract.py` — `test_rp1_repair_round_wiring`/`test_ha1_hatch_open_omits_default_mode` rebased onto the new no-file contract (mutation-verified RED under a reintroduced write, reverted); a new dedicated test, `test_rp1a_repair_round_writes_no_settings_artifact_under_cache_dir`, added (also mutation-verified). `tests/test_repair.py` — `test_b9_kill_switch_disables_composition` drops its now-vacuous "file does NOT exist" leg (true either way once the function is gone, no longer discriminates the kill switch specifically); `test_d5_the_narrowed_repair_scope_is_real` rebased onto a `invocation.write_session` spy capturing the real `SessionSpec.containment` (mutation-verified). `tests/test_lock_invariant.py` — the walker's exemption entry removed (a deleted function needs no exemption); walker re-run, green. `tests/test_u_fake.py`/`test_u_sdka.py` — the two independent function-level armor systems (`REWRITTEN`/`DS1_ADDED`/`_DS1_EXPECTED` re-pins; `_AR3_REMOVED`/`_AR3_REASONS` updates; the `test_hy5_numstat_bounds_hold` insertion/deletion bound widened) both updated and green. **Suite**: 2454 passed/6 skipped at base (`61c30b3`) → 2452 passed/6 skipped/0 failed (net −2: two `test_cn6`/`test_cn7_repair_leg` pytest-collected IDs removed — the latter was parametrized over 2 legs — offset by the +1 new `test_rp1a`). Pyright: 284 errors at base, 284 after (unchanged; the touched `worker.py`/production module itself is 0 errors both before and after). `git diff --stat`: **8 code files** (the `plugins/self-learn/cli/` subset -- `worker.py` plus seven test files) changed, 348 insertions(+), 196 deletions(-); **12 files total** including the four docs edits this row and its siblings represent -- gate r1 fold (2026-08-28) widened this to **15 files total**, 435 insertions(+), 198 deletions(-), after three more sibling specs (`u-seam-invocation-seam-spec.md`, `u-attrib-producer-attribution-spec.md`, `u-sdk-backend-spec.md`) gained superseded banners, per the fold's own disposition (measured just before this sentence's own edit -- this row's own further wording is not re-measured against itself). Per this unit's build report. | BUILD | Closed — see the ✅ disposition above. Direction (2) from the original row (thread the file's content into `options_kwargs`'s `settings` field, restoring a settings-file witness for worker-repair) was NOT taken: `A-2`'s "the charter is the only authority" design is unit policy across every surface (worker, worker-repair, miner-reader, analyst all pass `settings=None`), and reopening a witness for one surface alone would be a regression against that policy, not a fix — direction (1), delete, is the one the user's standing ROOT CAUSE preference calls for and the one this unit took. |
| FW-118 | **The two SDK engines share a session LIBRARY; the two charters stay two.** `sdksession/` owns the session lifecycle end to end and takes policy as an object; both `build_can_use_tool` decision tables stay with their clients, held apart by the `AGR2` disagreement pin. The library is `async`-native and multi-session: `new_run_id` collisions, the per-surface sidecar singleton, start-of-session retention and the import-time staleness anchor are all fixed here (§4.6 F-1..F-4 of the `U-engine` spec). Kill-ladder step 3 becomes conditional on `loop_closing` (§4.6 R-1). **Measured before the move: 18 of the 24 operator-visible messages the library owns were asserted by no test, 10 of the 13 shutdown messages included; pinning them with their prefixes is the unit's real product.** | BUILD | Landed by `U-engine` Phase 1 |
| FW-119 | **`self-learn serve` — one long-lived host process replaces three systemd units and a covert any-verb watchdog.** ADOPTED by user ruling 2026-08-26 22:01, decisively on portability. **Register item #11 (the cgroup patch for the miner-to-worker handoff) is SUPERSEDED, not fixed:** `self-learn-miner.service` is `Type=oneshot` with `KillMode=control-group` (measured live 2026-08-26) and `worker._spawn_window`'s `start_new_session=True` escapes the process group but not the cgroup, so the handoff has never once completed (register `:880-895`). With one live process starting both jobs there is no detached child to reap, so the three unit-file remedies that row named are not applied. H-5 is preserved: `serve` is a SCHEDULER of producers, never a watcher — each job still takes its own `commit_lock` and commits its own paths. | BUILD | Landed by `U-engine` Phase 2 |
| FW-125 | **Umbrella-as-destination: route a child-bucket record's canon into a registered ANCESTOR host without moving the record.** Today the only host-changing knob is `rehome`, which moves the record's bucket; `route --dest` carries a destination *qualifier* (`reference:<file>`, `claude-md:rules:<topic>`, …) and cannot name a host, so **0 of 107 resolved records** ever crossed from a child bucket into an ancestor host. U-ancestry refused this deliberately: it needs a host axis on `verbs._resolve_target` (U-hostmode's surface), a `--dest` grammar or a verb (U-verbs' surface), and it forks the compile set — `verbs._compile_set` gathers a host's records **by bucket** (its project leg iterates `discover_buckets` and keeps only buckets whose recorded project path resolves to the host), so a foreign-bucket record in a host's managed section would be invisible to `recompile` and would drift on the next run (13 §4 item 2). Source: `drafts/u-ancestry-ancestor-canon-spec.md` §4.3. | WATCH | Trigger: a live case where a lesson genuinely fires in ≥2 sibling projects AND the human declines the re-home (the record's own repo is still the right home for its evidence). Whichever unit takes it must first rule on compile-set ownership — does the ancestor host's compile set gain a foreign-bucket member, or does a routed record grow a `routing.host` field the compilers key on? — because that ruling, not the grammar, is the hard half |
| FW-126 | **The already-canon scan has no term source that exists before the analyst runs.** `gates.t3.scan_terms` are an OUTPUT of the gate procedure, so the cheap design for a wide hand-written scan — grep the terms over the host's docs, feed back only matching spans — cannot be built: there are no terms yet. U-ancestry therefore widened the *read* (whole `CLAUDE.md` + `references/**`, capped) instead of adding a *filter*, and priced the unfiltered alternative at **43.3 MB / ~10.8 M tokens** across the 9 registered hosts (de-duplicated; exclusion set stated in the spec's §3.4). A record-derived proxy exists (`worker._tokens`' tokenisation over `record_title` at a ≥6-char / first-12 cut, the recurrence detector's basis) and was used to retro-measure the corpus (11–12 of 58 project/user-scope resolved records, depending on that cut, have ≥50% of their tokens in a hand-written surface the analyst never read) — but promoting a Jaccard proxy to a canon-presence gate would let a threshold decide graduation. Source: `drafts/u-ancestry-ancestor-canon-spec.md` §5.3. | WATCH | Trigger: a term source that exists before the analyst does — e.g. the miner emitting candidate terms alongside a candidate, or a cheap pre-pass model whose only job is term extraction. Building it needs a ruling on what a term-match may and may not decide: U-ancestry's HW-1 pins that a hand-written hit never auto-resolves, and any filter design inherits that pin |
| FW-127 | **Two ancestry-shaped gaps left open by U-ancestry, both measured.** (1) `worker._recurrence_suspects` compares a new pending record only against `entry.bucket_dir / "resolved"` — same bucket only — so a child-host capture restating an **ancestor-routed** lesson raises no suspect. The miner's crossover basis does not share this limit (`miner._canon_index` iterates every bucket), so the two suspect producers now disagree across the ancestor edge. Widening was refused because it changes the Jaccard base rate for every bucket at once, and FW-49 records that this basis was already narrowed once on measured evidence. (2) `compilers.surface_names_target` resolves a relative pointer token against `surface.parent` (*"the token is read as the AUTHOR meant it"*) while a live session resolves it against the session **cwd**; ancestry makes the two disagree by construction, and all three live self-learn pointer blocks are byte-identical (`sha256:9b8c6e64…`), so a k1c session sees two blocks saying "captured lessons for this project" about two different projects. U-ancestry closes only the ancestor/descendant case, and only by adding a base-naming sentence to the surrounding **prose** (the token is unchanged, so U-pointer's predicate and tests are untouched; an absolute token was refused because a `CLAUDE.md` is tracked and one host has a public remote). The general divergence — any two loaded surfaces at different depths, `--add-dir` surfaces, a future Model B remap — is unclosed. Source: `drafts/u-ancestry-ancestor-canon-spec.md` §3.6, §6.5. | WATCH | Trigger for (1): a confirmed recurrence whose earlier routing lives in an ancestor bucket. Trigger for (2): a third pointer-bearing surface in one session, or the first `--add-dir` host |
| FW-120 | **The `sdksession` library is deliberately NOT its own distribution.** It ships inside `self-learn-cli` because the UI already depends on that package (`ui/pyproject.toml:20`, `:53`) and the CLI cannot depend on the UI. **Trigger for revisiting: the first consumer outside this repository.** Until then a separate pyproject, lockfile entry, install step and third version-range is cost with no benefit. `U-engine` spec §4.5 states the four things "portable" is allowed to mean, and §4.8 R-h names the shapes it refuses (plugin systems, session-kind registries, middleware chains, transport abstraction). | WATCH | Trigger: a non-self-learn consumer exists |
| FW-121 | **`S-48`'s recorded `M11` evidence names the wrong symbol — a SURFACE MISMATCH between the decision row and the code.** The row says *"reverting `TRANSPORT["analyst"]` … proving the contract had to close at the transport-table level"*, but `invocation_sdk/backend.py:60` folds the table once at import — `_CATCHES_OS_ERROR = dict(TRANSPORT)` — and the except ladder branches on the FOLD, at `:417`, `:471`, `:479`. Measured 2026-08-26: setting `contract.TRANSPORT["analyst"] = False` after import leaves `backend._CATCHES_OS_ERROR["analyst"] is True`, so the backend observes nothing. **The contract is NOT untestable — the live control point is monkeypatchable and is already exercised**: `test_u_opsfix.py::test_fw108_no_line_when_surface_does_not_catch_os_error` does `monkeypatch.setitem(backend_mod._CATCHES_OS_ERROR, "worker", False)` (`:171`) and drives the real `SdkBackend`, asserting the surface-conditional behaviour. What is missing is only that `S-48` points a reader at `TRANSPORT`, and `test_u_sdka.py::test_m16_transport_table_is_still_a_plain_mutable_dict` (`:1135`) — which asserts only that the TABLE supports item assignment, never that the backend observes a change — is the test a reader following that wording lands on. **The real cost:** a maintainer exercising `M11` as written monkeypatches `TRANSPORT`, sees no behaviour change, and concludes the contract is broken. Found by `U-engine`'s single-session enumeration (spec §2.9, G-6); **NOT fixed there** — `contract.py` and the seam's except ladder are outside that unit's files-may-touch table, and the library must not read `TRANSPORT` at all. | BUILD | Not scheduled. Two candidate fixes, and the choice is a ruling not a detail: (a) amend `S-48`'s evidence sentence to name `_CATCHES_OS_ERROR` and cite `test_fw108_...` — a docs-only change that makes the row true as written; or (b) drop the fold and read `TRANSPORT[surface]` at call time in `_drive`, which makes the row's original wording literally correct but changes the seam's failure-leg dispatch. Belongs with whoever next touches `S-48`'s evidence |
| FW-130 | **`~/.cache/self-learn` held 31,291 stray `home-<sha256(home)[:8]>` namespaces (1.1 GB), rising across suite runs — a per-ledger-home cache namespace (doc 13 §6 H-4) written to the REAL filesystem instead of a test's hermetic redirect.** Root cause, isolated by a namespace-diff instrument run against every suite in this repo: the CLI package (this repo's `plugins/self-learn/cli`) measured CLEAN across five full runs (parallel `scripts/suite`, and each of its three batches run serially) — its `_worker_test_defaults` autouse fixture already redirects `XDG_CACHE_HOME` per test, and no subprocess call site in the CLI suite drops it. The actual leak was in `plugins/self-learn/ui`: `test_js_dom.py`/`test_js_dom_pane_persistence.py`'s MODULE-scoped real-uvicorn-in-thread server fixtures process browser-triggered requests on a background thread/asyncio task — `self_learn_ui.ledger._invoke_json` reads `os.environ` fresh on EVERY subprocess call (never a value captured once, at server-construction time), and every production call site (`routes.py`, `pane.py`) passes no explicit `env=`, so it falls through to that live, PROCESS-GLOBAL `os.environ`. The UI package's `_redirect_env_defaults` fixture is FUNCTION-scoped: it is reverted at the end of every test and freshly reapplied at the start of the next, and per pytest's own fixture-scope ordering (session > module > class > function, regardless of file order) it has not even run yet for a module's FIRST test at the moment that module's `server` fixture instantiates — so there was a real gap, however narrow, during which `os.environ["XDG_CACHE_HOME"]` reverted to whatever it was before pytest started (unset, on a dev machine). A background task's subprocess call landing in that gap resolved the REAL `~/.cache/self-learn`; measured content shape confirms it every time — the created namespace carries only a `miner/` subtree (`journal.jsonl`, `cursors.json` naming REAL `~/.claude/projects/*.jsonl` transcript paths, lock files), the exact shape `miner.maybe_kick`'s verb-invocation watchdog leaves behind when it auto-spawns `mine run --trigger kick` after ANY of `_invoke_json`'s CLI subprocess calls (`list`/`status`/`report`/`mine status`, none of them `mine`/`init`/`serve`, so the watchdog always ticks first). **Fixed at the root** in `plugins/self-learn/ui/tests/conftest.py`: a new SESSION-scoped, autouse `_env_floor_session` fixture sets `XDG_CACHE_HOME`/`XDG_RUNTIME_DIR`/`SELF_LEARN_HOME`/`SELF_LEARN_CLAUDE_DIR`/`SELF_LEARN_TRANSCRIPTS_DIR`/`SELF_LEARN_{WORKER,MINER}_AUTOKICK` to a fresh `tmp_path_factory`-rooted floor ONCE, before every module-scoped fixture of every test, and never tears it down (no `yield`/undo) until the whole session ends — so `_redirect_env_defaults`'s per-test `monkeypatch.setenv` calls now always UNDO to this hermetic floor at each test's teardown, never to the real unset default, closing the gap regardless of exactly when a background task's env read lands. Verified: 3 consecutive clean re-runs of exactly the two leak-site files (98 tests each) post-fix, plus 2 full-suite runs, all zero new real namespaces; before the fix, 2/2 full-suite runs each produced exactly one new real namespace, attributed each time (by a namespace-diff pytest plugin correlating test nodeid to the moment of creation) to a different pane-lifecycle test in one of the two files — consistent with a genuine, non-deterministic race rather than a fixed call site. **A forward guard against regression** (`_litter_namespace_guard`, added to BOTH `plugins/self-learn/cli/tests/conftest.py` and `plugins/self-learn/ui/tests/conftest.py`, session-scoped autouse): a `pathlib.Path.mkdir` class-level patch gives 100%-certain in-process attribution (deliberately never a rebound FUNCTION name — `cli.py`/`worker.py`/`self_learn_ui.pane`/`middleware` all do `from ... import resolve_home`/`cache_dir` at import time, which a same-module patch installed after collection cannot reach); a `subprocess.Popen.__init__` class-level patch records every `SELF_LEARN_HOME` a spawned subprocess's own `env=` carries, for best-effort attribution of a namespace a CHILD process created. Concurrency on the shared build host: a namespace matching neither is reported (a warning naming it) but never fails the session, since a concurrent sibling builder's own suite may be adding namespaces at the same time and this session cannot know it is safe to blame them — measured directly during this build (+5 real namespaces appeared in a 13-minute window with zero test activity from this build). **Code gate r1 fold (same day, uncommitted):** three MUST-FIX findings were addressed by deleting and rewriting the guard section in both `conftest.py` files (never edited in place). M-1: the guard previously had no committed test proving it actually fires — a one-line `return` right after its `yield` left the whole suite green (2419 unchanged was the tell). Fixed with `test_litter_guard_probes.py` (both packages, byte-identical, `pytester`-driven): five probes (A–E) each spin up a genuinely separate pytest sub-session with its own scratch "real cache" root, splice in the SHIPPED `conftest.py` fixtures by absolute path (never a reimplementation), and drive one bypass class each — see the probe table below. All five assert the sub-session's own outcome; mutated (the same `return`-after-`yield`) all five go from green to red, reverted, sha256-diffed byte-identical to before. M-2: the CLI package had NO session-scoped floor at all before this fold — only the function-scoped `_worker_test_defaults` — so doc 13 §6's "whole session" claim was literally false for CLI. Fixed with a new `_env_floor_session` in the CLI `conftest.py` too, floor-matching the UI one. M-3: attribution previously hashed the raw `SELF_LEARN_HOME` string from a subprocess's explicit `env=` kwarg, so a child spawned WITHOUT `env=` (inheriting live `os.environ` instead — probe C, the exact shape `worker._spawn_run`'s detached miner uses) or one carrying a non-normalized path (a trailing slash — probe E) fell through to warn-only. Fixed by attributing on `sha256(Path(home).expanduser())[:8]` (and, defensively, the additionally-`.resolve()`-d form) for every home this session ever hands out — the floor home, every per-test tmp home, and every `SELF_LEARN_HOME` a spawned process's env carried, however it got there — so probes C and E now FAIL like B, while probe D (a namespace this session never tracked at all, simulating a concurrent sibling) still only WARNS. The warn channel moved to a `pytest_terminal_summary` hook, always printed even under `-q` (a bare `print` during session teardown is exactly what pytest's own capture manager can swallow there). N-fixes folded alongside: `_env_floor_session`'s docstring corrected to admit it DOES `yield` and `mp.undo()`; the litter-listing script derives the live ledger's own namespace from `SELF_LEARN_HOME`'s digest instead of a hardcoded `home-0f24de4d`; the two class-level patches (`Path.mkdir`, `Popen.__init__`) are now restored to their originals in the guard's own teardown, before its assertions run, instead of staying patched for the rest of the interpreter's life; and doc 13 §6 now notes the pre-existing (unrelated) bare-`pytest`-from-root collision between the two packages' same-named test modules. **Probe table** (A–E, normal operation, both packages): A in-process `delenv` + direct `worker.cache_dir()` call → FAIL (100%-certain in-process attribution); B subprocess with explicit `env=` carrying `SELF_LEARN_HOME` → FAIL (digest match); C subprocess spawned with NO `env=` kwarg at all, inheriting live `os.environ` → FAIL (post-M-3; warn-only pre-fix); D a namespace this session never tracked, created via a raw `os.makedirs` bypassing both patches (simulates a concurrent sibling) → WARN, never FAIL; E subprocess `env=` carrying a non-normalized (doubled-slash) `SELF_LEARN_HOME` → FAIL (post-M-3; warn-only pre-fix). This concurrency-aware attribution was also confirmed live, not just by the synthetic probes: re-running both suites for this fold, the guard's warn channel fired for real in every CLI suite batch that overlapped in time with a genuinely concurrent sibling builder's own suite on this host (`U-hostmode`'s background test run), naming that builder's own lock-file artifacts under the real cache root and correctly declining to fail either session over them. Positive control (both packages, pre-fold, still true post-fold): a throwaway test deliberately `delenv`s `XDG_CACHE_HOME` and calls `worker.cache_dir()` — the test itself passes (the mkdir genuinely succeeds), and the SESSION fails at teardown, naming the created namespace. A **read-only dry-run litter listing** (`misc/cachelit-litter-listing.py`, git-excluded, per this repo's own convention) lists namespaces with no `spool/*.jsonl` event log, no `serve.heartbeat`, and no `worker.log`, older than `--min-age-days` (default 7) — never deletes; deletion is the user's own call. Measured 2026-08-28: 31,204 litter namespaces / 682.9 MiB at the default 7-day floor (31,269 / 684.5 MiB at 0-day) — of ~31,305 total, only 35 carry a real activity signature (event log, heartbeat, or worker.log), one of which is the live ledger's own namespace (`home-0f24de4d`, hard-excluded by name regardless). Suite numbers, re-measured after the r1 fold (still guard/test-only additions, no production code touched): CLI 2419 → 2424 passed (+5, the new `test_litter_guard_probes.py`), 6 skipped, 0 failed, rc=0 across all three `scripts/suite` batches; UI 1270 → 1275 passed (+5, same probe count) plus the one known pre-existing failure (`test_service_unit.py::test_both_units_document_manual_registration_via_symlink`, does not block per this repo's own local convention). pyright still unchanged despite the guard-section rewrite: CLI 285/285, UI 162/162 (1 warning both sides) — measured by swapping the touched files (both `conftest.py`, `test_u_sdka.py`, `test_worker_contract.py`; the new `test_litter_guard_probes.py` removed) against `git show 84d0b78:<path>` copies, running pyright, then restoring and re-verifying sha256 byte-identical to before the swap. | BUILD | ✅ CLOSED (`U-cachelit`, landed 2026-08-28 at `ac2161a` after code gate r2 CLEAN) |
| FW-132 | *(Numbering: `FW-122`-`FW-131` are reserved by the three parallel T2 specs in flight alongside this unit — hostmode 122-124, ancestry 125-127, corrob 128-131 — hence the jump from `FW-121`.)* **A test-hermeticity hole invisible until the host state it depends on changes — the U-engine Phase 2 gate ran before the unit was linked.** `serve.unit_dir()` (`plugins/self-learn/cli/src/self_learn/serve.py`) resolved `SELF_LEARN_SERVE_UNIT_DIR` (explicit override) or else fell straight to the REAL `~/.config/systemd/user` — skipping `$XDG_CONFIG_HOME` — while the heartbeat compared against it came from the hermetic cache (`XDG_CACHE_HOME`, redirected by every test via `conftest.py`). The gate that shipped `serve` (`U-engine` Phase 2) ran on a host with no reference unit linked, so `unit_dir()` reading the real (then-empty) path looked identical to reading a hermetic fixture — the defect had no way to surface until this host actually linked `self-learn-host.service` (2026-08-27, 21:49/21:53), at which point 18 tests across six files started failing `self-learn doctor invocation`'s `serve` row, none of them touching `serve` on purpose. **The general shape:** a resolver with an explicit test-override env var is not thereby hermetic — it is hermetic only if EVERY fallback leg between the override and the real host path is also covered by the test harness's isolation, and that coverage is easy to lose silently when the harness's own isolation (here, `XDG_CACHE_HOME`) evolves without a matching update to every consumer that shares its resolution shape (here, `XDG_CONFIG_HOME`). Fixed by `U-servehermetic`: `unit_dir()` now checks `XDG_CONFIG_HOME` before falling back to the real path (systemd's own resolution order), and `conftest.py`'s autouse fixture sets `XDG_CONFIG_HOME` to a fresh `tmp_path` subdir for every test, mirroring the pre-existing `XDG_CACHE_HOME` line. **Same root cause, same unit:** `install.sh`'s `UNIT_DIR` had the identical unconditional `$HOME/.config/systemd/user` literal (it is not `serve.unit_dir()`'s caller, but it is the OTHER writer of the same directory `unit_dir()` reads) — realigned to `${XDG_CONFIG_HOME:-$HOME/.config}/systemd/user`, so the installer and the resolver it feeds can never link and check different directories; the explicit `SELF_LEARN_SERVE_UNIT_DIR` override stays CLI-only (a test-hermeticity knob, not an installer concern). Code-gate r1 fold (M-1): the standalone `install-commands-test.sh` had the SAME class of bug one layer up -- its own header claims nothing outside its fake `$HOME` is ever touched, but its `run_install` pinned only `HOME`, so a caller's real `XDG_CONFIG_HOME` leaked straight through into the real `install.sh` it drives and left four unit symlinks under that external directory, invisible to every check (all scoped to the fake `$HOME`) and uncleaned by its own trap. Fixed the same way: `run_install` now pins `XDG_CONFIG_HOME="$FAKE_HOME/.config"` for the invocation, with a new positive-control test in its pytest wrapper (`test_regime_fixes.py`) proving an external `XDG_CONFIG_HOME` stays untouched. `plugins/self-learn/README.md:137` also carried the unconditional literal (M-2) -- the census immediately above had checked `docs/specs/self-learn` and `systemd/` but not the plugin's own README; aligned, and this note itself is the fix for that omission. | BUILD | ✅ CLOSED (`U-servehermetic`, 2026-08-27, uncommitted, worktree rule, not a commit reference; code-gate r1 fold applied) — suite 2393/18/6 (broken) -> 2417/0/6 (fixed, +6 new tests: 4 in `test_serve_hermetic.py`, 1 `PORT2` sibling in `test_serve.py`, 1 positive control in `test_regime_fixes.py`; no env prefix on this host); pyright unchanged (285/0/0) |
| FW-128 | **The `tool_events`/`denials` consumer: an in-memory corroborator emitting counts.** Closes `S-44`'s consumer clause and gives `17` §5.4's two-instrument gate an automatic instrument. Corpus, `FW-107`'s never-printed line, and the never-run burn-in are the evidence; §5.7 is the sizing; `src/self_learn/corroborate.py` is the placement and §4.0 is why. | BUILD | Landed by `U-corrob`. |
| FW-129 | **Surfacing tool events into the ledger, `report`, or the UI is REFUSED, with the measurement.** `scan.scan()` — the product's only scrubber — returns **0 hits** over the whole 81 613-byte corpus while **65** occurrences of the user's home path and whole 16 200-byte proposal bodies sit inside it; `scrub-personal-literals-spec.md` is a source-tree de-personalisation spec, **not** a runtime redactor, so there is nothing to reuse. Reading a written log back additionally requires re-pinning `_ARMOR_SHAS["…/test_invocation_sdk.py"]`. | WATCH | Trigger: a second consumer that genuinely needs past-run events (`G-1`, or a second human user) **AND** a real personal-literal redactor. Until both, the answer is no. |
| FW-131 | **The analyst runs blind and loud-failing on the one ATTENDED surface.** Measured over the only window with analyst tool events (2026-08-21, 8 runs): **16 of 22 tool calls (72.7%) errored** — 8 charter `Bash` denials plus 8 `Read` calls returning `File does not exist. Note: your current working directory is ~/.self-learn.` The analyst's `SessionSpec` passes `log=lambda _msg: None` (`analyst.py:236`), so none of it reaches the operator. **`U-corrob` CLOSES the DENIAL half (`DEN3`)**, on the coordinator's ruling 2026-08-28 ("the cap ... is armor BOOKKEEPING, not a design constraint"): `analyze()` gains a keyword-only `charter_denials` accumulator, `FW-107`'s exact shape, and `teach --route` prints the denial-count line on both branches of its `try`. The first attempt (2026-08-27) reverted rather than shipped, because `test_u_sdka.py::test_hy5_numstat_bounds_hold` pinned `analyst.py`'s diff against base `442385d` at exactly `(4, 18)` insertions/deletions with zero headroom; the second attempt re-pins that row instead, to the measured `(22, 20)`, dated and justified in place — the same motion `U-papercuts`/`U-servehermetic`/`U-kl4` used for their own hy5 rows that same night. `DEN1`/`DEN2` close the equivalent denial-visibility gap on the miner-reader; the analyst's denial half is now closed too. The missing-file half is a prompt/doctrine question — the analyst was hunting for `card-sections.yaml` with a tool its charter forbids — and is **not** closed here. | BUILD | Denial half CLOSED 2026-08-28. The missing-file half is not scheduled — belongs with whoever next touches `compose_single_prompt` or the routing doctrine. |
| FW-122 | **Canon hosts are git-optional: `mode: git \| plain` on the hosts.yaml entry, set once at `host add --mode`, defaulting to `git`; a ledger-side compile record is plain mode's integrity instrument; user scope becomes a first-class plain host and the chezmoi adopt surface is deleted.** Measured before the build: 38 of 94 git call sites are host-side, all in `verbs.py`, held by 11 functions; 3 of 9 registered hosts are repos only to satisfy the requirement (`keyboards` `2085fe3`, `.config` `fc5b7dc`, `3d-printing` `d4f7c17`), 2 of those needing whitelist `.gitignore`s; **3 of 9 have no remote**; `~/.claude` has no VCS and holds the largest managed section on the machine (19 279 B). The record is **stricter than `git status`** for the region self-learn owns and rides the resolution's own ledger commit. `TargetSpec.host_repo is None` — which meant "chezmoi user scope" in 17 sites — is retired at the root. Git hosts stay byte-identical, proven by the `UN` group. | BUILD | Landed by `U-hostmode` (Phase 1 + Phase 2) |
| FW-123 | **The lock-invariant walker cannot see whether a `host_repo is None` write is actually serialized, by design, and plain mode widens that blind spot.** `test_lock_invariant.py`'s `_is_lock` walks the whole expression subtree specifically so `lock = commit_lock(r) if r else nullcontext()` — `verbs._host_phase`'s real shape — counts as guarded, without evaluating the condition (its own docstring says so). **Gate-replicated 2026-08-27:** the ternary with a `nullcontext()` plain branch leaves the walker GREEN; replacing the assignment outright leaves zero guarded lines and turns it RED. **Mitigated, not closed:** plain hosts get a real cache-dir lock (`${XDG_CACHE_HOME:-~/.cache}/self-learn/host-<slug>.commit.lock`, keyed by host path for the sentinel's own reason) and a **two-process** runtime serialization test — never threads, because `gitops._held_locks` makes a same-process re-acquire a pass-through. **The residual is the walker's approximation itself**, a deliberate trade. | WATCH | Trigger: a third `nullcontext`-shaped host phase appears, or a real concurrent-write corruption is observed on a plain host |
| FW-124 | **`09-surface-spec.md` §11 Y-17's committability sentence is stale on one of its three clauses, and the row is a ratified decision, so it is not corrected in passing.** It reads: *"canon writes are commits; audit, rollback, and recompile all diff against git (13 §4)"*. Measured 2026-08-27: **recompile does not diff against git** — `verbs.recompile` branches on `compile_result.changed`, a content comparison inside `compilers` (`verbs.py:4829-4838`); its git use is the dirty skip and the commit. Nor does drift detection: `selfcheck._check_drift` looks for the `(lrn-…)` entry marker in the target's managed section, and **zero of the nine `--selftest` rows diff a HOST against git** (three consult `ledger.home_state`, whose `is_repo_root(home)` runs one git call against the LEDGER — `ledger.py:70`). Audit and rollback are genuine, and are why `git` mode stays the default. **The real cost:** a reader following Y-17's wording concludes that removing git removes drift repair, which is false, and would over-scope any future git-optional work. `U-hostmode` scopes the *requirement* to git mode in `13 §4` and leaves Y-17's text alone. | BUILD | Not scheduled. One-line docs correction: restate as "audit and rollback diff against git; drift detection and recompile do not". Belongs with whoever next touches Y-17 |
| FW-133 | **Retract / un-route (routed → pending) has no verb, and `U-verbs` deliberately does not build one.** `reroute` covers the case with a live motivation — a wrong destination — by rewriting `routing` and moving canon in one motion. A true retraction must un-write canon *and* return the record to the draft plane, which 02 §2's freeze-at-routing pin forbids. Measured: **0 of 92 routings** have ever been retracted, and both shipped workarounds (`teach --supersedes` + `route`, or `supersede` + a new record) mint a new id — arguably the correct provenance for a re-decision. Source: `drafts/u-verbs-ledger-verb-completion-spec.md` §1, §9. | WATCH | **pending (U-verbs)** — trigger: a live case where the record's *substance* was right and only the decision to route at all was wrong, distinct from a wrong destination. Whoever takes it must first rule on whether un-freezing a routed record is permitted, which is a `03` row, not a verb |
| FW-134 | **The producer exit-code integers stay uninformative, deliberately — the outcome rides `--json`.** `worker kick` has five outcomes and `mine run` eight; after `U-verbs`, `mine run` still maps six of its eight to `0` and `worker kick` all five. Refused: `busy → 6`, defensible in isolation but it would make a benign concurrent timer tick report failure to systemd — FW-85's own stated blast radius. `PROD3` pins the absence with a negative criterion so a later builder cannot "improve" it silently. FW-85's automation prerequisite is met by the envelope. Source: `drafts/u-verbs-ledger-verb-completion-spec.md` §3.7, §2.7. | WATCH | **pending (U-verbs)** — trigger: a caller that genuinely cannot read stdout (a systemd `OnFailure=` unit, a shell `&&` chain nobody will change). Count the live unit files that would change meaning first — under FW-119's `serve`, the scheduler calls the library directly and never sees these codes |
| FW-135 | **Seven live `references/LEARNINGS.md` files carry a header saying "this file is append-only" that is one word stale once retirement is real, and self-learn may not fix it.** Measured: 7 files, 27 entries, 570-15,907 B. The header is **compiler-written** (`compilers._LEARNINGS_HEADER`, `compilers.py:173`, applied at `:1174`, byte-identical to the live files) — but a references file has **no managed marker region**, so there is no bounded span the compiler owns and no idempotent way to rewrite a header a human may since have edited; a blind overwrite is the hand-edit-clobbering the marker contract exists to prevent. The constant is corrected for newly created files only. Source: `drafts/u-verbs-ledger-verb-completion-spec.md` §3.5. | WATCH | **pending (U-verbs)** — trigger: a references file is recreated (it then gets the new header), or a human asks. A `recompile`-time header refresh is refused for the reason above and should not be re-proposed without a decision row widening what the compiler owns in a references file |
| FW-136 | **The review UI's keymap is four letters from full, and `U-verbs` spends two of them.** Measured at `b206800`: 22 entries, used letters `abcdefgijnopqrstuvwxy`, free `hklmz` — and `h` is unusable (printed on the header back-link, bound to nothing; `fixtures/ui-walks.md` W2-F1). `U-verbs` binds `k` (dismiss-suspect, on the existing holding card) and `m` (confirm-held, on the existing resolved card), leaving `l` and `z`; `reroute`/`reopen`/`undefer`/`rescope`/`supersede` get POST-surface parity with no key. The structural blocker is `keymap.py`'s own docstring: *"app.js dispatches on the FIRST key match with no context filter, so every key must be unique across the whole table"* — context gates DISPLAY only. Source: `drafts/u-verbs-ledger-verb-completion-spec.md` §4.9, §2.8. | WATCH | **pending (U-verbs)** — trigger: the third verb that needs a key. Building it means giving app.js a real context filter, which turns `Context` from a display field into dispatch logic and re-opens every existing binding's uniqueness argument — a design round, not an addition |
| FW-137 | **`host remove --gate-only` still orphans canon, by design, and there is no honest bulk retirement.** After `U-verbs`, `host remove` refuses while ≥1 routed record compiles into the host and names both repairs; `--gate-only` is the explicit override and prints the consequence (`recompile` WARNs and skips). What remains open is the case the override serves: a host that is genuinely gone, whose records are still `routed` with a `routing.destination` resolving nowhere. `graduate` ("canon already covers it") and `supersede` ("another record replaces it") are both **false statements** about those records, and `reroute` needs a new target a human must choose per record. Measured: `host remove` uses all-time **0**, so this has never happened. Source: `drafts/u-verbs-ledger-verb-completion-spec.md` §3.6. | WATCH | **pending (U-verbs)** — trigger: the first real deregistration of a host with routed records. Rule first on whether a fourth terminal status ("orphaned") exists or the honest answer stays "a human reroutes each one" — adding a status is a `02 §2` amendment and touches every `report` metric |
| FW-138 | **One live record's `scope:` disagrees with its bucket, and no verb can repair it.** Measured 2026-08-28 by walking all 18 buckets and comparing each record's `scope:` against its bucket's own identity: **1 of 152** mismatches — `user/resolved/lrn-c826137f.md` carries `scope: skill:cron-claude` while sitting in the `user` bucket, so it is reachable through the user bucket and invisible to anything that re-derives the bucket from the scope (`ledger_ops.bucket_dir_for_scope`). It is **`resolved`**, so `U-verbs`' move verbs refuse it on status — deliberately: 02 §2 freezes substance at routing. It is also one of the 27 reference routings (`routed_at: 2026-08-10`, `by: analyst`), its entry sitting in the cron-claude skill's `references/LEARNINGS.md`, so Phase 2's retirement lands on the right file for it; the residual is that `discover_buckets`-driven and `bucket_dir_for_scope`-driven consumers disagree about where this record lives. `MOVE1` leg 9 covers the *pending* record with the same defect. Source: `drafts/u-verbs-ledger-verb-completion-spec.md` §3.2a (Finding V-6). | WATCH | **pending (U-verbs)** — trigger: a second instance, or a `recompile` that visibly mis-files this record's canon. The repair needs a ruling first — does a filing-only fix on a resolved record count as "substance"? That is a `03` row, not a verb |
| FW-140 | **The anchor advance is automated; the EXEMPTION FOLD it implies is not, and nothing verifies the fold was correct.** The mechanical half is settled: the landing chain re-anchors inside the merge commit (`merge --no-commit` → `--remeasure` → `commit`), and `ARM5` fails loudly on a stale anchor, and `--remeasure` re-derives every node count and `dump_sha` from the new anchor. What survives is the judgement half. When the anchor advances, every accumulated `repinned` / `missing` / `edited` / `edited_exports` entry whose subject node is now **inside** the new anchor becomes vacuous and must be dropped — `FIX2`/`BEH8`/`BEH9`'s anti-rot legs will fail on the stale ones, which is the right direction (loud, not silent), but they fail **after** the chain has already pushed, and nothing checks that the human who emptied the doors did so for the right reason rather than to get the suite green. `--remeasure` deliberately re-derives the *owed* entries but never silently deletes an existing one, because a tool that quietly drops an exemption is a tool that quietly forgets why it existed. | WATCH | Trigger: the first landing where an anti-rot leg goes red *after* a chain run, or the first merge train where any single door exceeds ~5 entries. Whoever takes it writes the fold procedure into `15-orchestration-runbook.md` §5, beside the existing merge rules, and rules on whether a dropped exemption owes a line in the review record |
| FW-141 | **Nothing connects a test to the file it belongs in, so the suite fragments by unit rather than by subject.** Measured 2026-08-28: **34** test files created since 2026-08-01; of the **17** created after the armor landed (`29f5d67`, 2026-08-19), **11 — carrying 299 tests** — exercise a production module that a pinned behaviour file also exercises, and none owed a re-pin. U-armor removes the *incentive* (an in-file addition is now free) but adds no *rule*. Two of the seventeen are the shape most clearly forced by the old pin: `test_litter_guard_probes.py` (a guard-of-the-guard for a fixture inside the pinned `conftest.py`) and `test_serve_hermetic.py` (created by the same commit that re-pinned `conftest.py`). Source: `U-armor` spec §2.7, §3.1 option E. | WATCH | Trigger: a third file appears whose whole subject duplicates a protected file's, or a reviewer cannot find where a behaviour is tested. Building it means deriving a subject→file map over 34+ files and ruling on what "belongs" means — a design round, not an addition. **Do not conflate it with U-armor**: the armor's job is to stop silent weakening, not to organise the suite |
| FW-142 | **The docs-only landing lane's test set cannot see a docs path built from PARTS.** `U-land`'s lane runs the test modules that hold a real (non-docstring) string constant matching `docs/`. Measured at `9c7ebdd` over 144 modules: naive 43, docstring-inclusive 39, **strict 8** — and the strict count over `src/` is **0**, so the indirect-reader route through a `src/` helper **does not exist** (the seven modules that look like readers carry the path in a module docstring, a citation rather than a read). What does exist is the part-built route: `test_reader_contract.py` holds `'docs'` and `'specs'` as **separate** constants and matches no `docs/` literal. The lane's set is the union of direct-hit and part-constant modules (direct 8, part-built 2, **union 8**), and a detector keeps the strict `src/` count pinned at 0, forcing the full lane the moment it moves. Neither catches a path assembled from fragments no single constant reveals (`"do" + "cs"`, an f-string, `Path(*parts)`). | WATCH | Revisit if a docs-only landing ever ships a defect the full suites would have caught. The fallback is stated and cheap: delete the lane and always run the full suites — the saving is 94 % of wall clock, not a correctness argument |
| FW-143 | **The pre-runner landing scripts are the only written record of the chain `U-land` replaces, and they live outside version control.** Four ad-hoc scripts (**268** lines: one checks module plus three per-landing resolvers, each hardcoding the repository root as an absolute home path) sit in the git-excluded `misc/` tree. They are the source `U-land`'s §2.2/§2.3 census reads — 13 conflict blocks across three landings, four recurring shapes (keep-both 6, per-key 3, count-line 2, numeric-rows 1, plus one keep-both variant). | BUILD | Retire them once `scripts/land` has completed one real landing, and record which landing that was. Until then they are the only artefact that shows what the runner replaced |

> **DISPOSITION 2026-07-27 — this section predates the routing/pin audit and
> should not be read as current.** It was written 2026-07-18. None of its
> three moves touches routing, delivery, or the analyst, and
> `research/2026-07-27-routing-monoculture-and-pin-audit.md` subsequently
> established that all three are defective (FW-40…FW-45). An independent
> review (`research/2026-07-27-status-review.md` §3.1) flagged the mismatch;
> its argument on move 3 specifically: the repo is **already public**, so
> packaging no longer "freezes first impressions" — what a new user meets
> first is the routing layer, which is the broken part.
>
> **Two consequences pinned here, because they bind regardless of what the
> next sequence turns out to be:**
> - **FW-35 (fast lane) is HELD pending FW-40's ruling.** It is gated SOUND
>   and build-unblocked, and it tiers `reference` FAST on the grounds that
>   it is an "unloaded surface… affects zero activations" — true, and the
>   reason the tiering is wrong until `reference` delivers. Building it
>   first would industrialise the defect at throughput.
> - The replacement sequence is **not yet decided** — §4's outstanding user
>   rulings gate most of it. Do not treat the list below, or the review's
>   proposed ordering, as authorised.

1. **FW-17 + FW-18 (+ FW-26/27 riding along)** — small, protective,
   and every later UI round gets cheaper for it. No user decisions
   required; pure build + records hygiene.
2. **FW-16 (round 4)** when the user unparks it — *before* packaging,
   because packaging freezes first impressions and round 4 is
   predicted to be an information-architecture pass, not styling
   (rationale in `forward/ui-ux.md` §2).
3. **FW-10…FW-15 (packaging)** as the declared next major phase — with
   migrations (FW-12) and external docs (FW-13) treated as first-class
   parts, not afterthoughts.

Riding alongside on their own clocks, independent of the sequence:
FW-1 (next miner cycle), FW-5 (2026-08-17), and every WATCH item.

## 4. The consolidated decision queue (user rulings outstanding or foreseeable)

The rulings below bind work; none should be absorbed silently. Existing
open ones are listed with their home row; foreseeable ones name the
moment they'll be asked.

| Decision | Home | State |
|---|---|---|
| ~~**What should `reference` DO?**~~ | FW-40 | **ANSWERED 2026-08-02 → S-23 (1).** `reference` survives and gets its pointer (the 14 stranded records become reachable), but `paths:`-scoped rules become the **primary** cheap tier and DEMAND shrinks to lessons that are genuinely not file-scoped. No longer gates anything |
| ~~**Should user scope get a cheap surface at all**~~ | FW-42 | **ANSWERED 2026-08-02 → S-23 (2).** Yes, and it is **pathed rules only** — explicitly NOT a user-level reference file. Note the consequence: the `verbs.py:950-955` refusal **stays** (rewrite its stale chezmoi reason to cite S-23; do not delete it). FW-42 narrows to the UI destination menu |
| ~~**S-21 (a): is the analyst's proposed skill name trusted or CLI-regenerated?**~~ | FW-46 | **ANSWERED 2026-08-02 → S-21 amendment.** Neither: the analyst proposes, the CLI mechanically validates (kebab-case, plugin collision, marketplace entry), the human confirms. A rejected name comes back **with its reason**, never silently rewritten |
| ~~**S-21 (b): "change the name" has no UI**~~ | FW-46 | **ANSWERED 2026-08-02 → S-21 amendment.** Yes — the review surface gets a text field. Costed at ruling time as genuine UI work (new input, validation feedback, re-render), not a one-liner |
| Reverse either agent-authored narrowing of an explicit user request? (a pane agent that *acts* → may only propose, `09:1652`; *"potentially autonomous review, are goals"* → M-1 *"No exception, no flag"*) | `research/2026-07-27-…-pin-audit.md` §5 | **OPEN — surfaced, not recommended either way** |
| Tighten terminal `host add` (no `--init`) against paths inside a parent work tree? | 03 G-3 dated note / Y-17 fold | **OPEN — awaiting yes/no** |
| Widen the pane agent's canon read scope to whole host roots? | 03 G-3 row (user-ratifiable) | OPEN — default conservative, unexercised |
| Distribution shape: binary vs `uv tool install` vs both | FW-10 | Foreseeable — opens packaging |
| Unpark round 4 — when? | O-9 | User's call; sequencing above recommends pre-packaging |
| D3 manual-push posture at fleet scale | FW-22 | Foreseeable — only if a second host captures |
| Miner autonomy ladder: any step up? | 12 §staged-autonomy | Foreseeable — only after FW-2's evidence window |
| FW-30 settings exposure tiers: what's freely exposed vs guarded vs never | `forward/ui-ux.md` §5 | Foreseeable — routes first when FW-30 is spec'd |
| S-18 model split — reopen? | 03 S-18 | Only on material model/pricing change |

## 5. The trigger table (event → dormant mechanism → FW item)

The lifecycle mechanisms below are **built and shipped but essentially
unexercised** — each activates on a first occurrence. The plan is
readiness, not rebuilding; details in `forward/canon-lifecycle.md`.

| First occurrence of… | Activates | FW |
|---|---|---|
| 02 §4 over-cap WARNING on a route | Graduation-pressure flow (oldest-entries card) | FW-6 |
| A routed lesson observed stale in live canon | G-6 gate (03) — staleness revalidation build | FW-7 |
| A confirmed recurrence (`confirm-recurrence`) | Revise/escalate/tolerate/retire resolution flow | FW-8 |
| A routed lesson found wrong | Supersede-and-recompile path (S-12) end-to-end | FW-9 |
| A second host/user wanting an install | G-2 proper + FW-21/FW-22 | — |
| Anthropic ships native per-skill memory | S-1 reopens (its stated input changed) | FW-24 |
| 2026-08-17 arrives | O-3/O-7 metrics revisit | FW-5 |

## 6. Non-goals, restated (the map's guard rails)

Unchanged from 03/06 and binding on this map's execution: no autonomous
writes to canon, ever (P1 generalizes, never relaxes); no team-scale
mechanism (PR routing, scope tiers, provenance tiers) before its 06
stage trigger; no modeled metrics — counts only; no vector/retrieval
infrastructure without G-5's observed failure; the Go port stays parked
(O-8) and is not packaging's fallback plan until packaging *demonstrably
fails* on Python; round 4's principles bind all interim UI work even
while the pass itself is parked (O-9).

## 6a. Dated dispositions

- *2026-07-18*: **FW-26 SHIPPED** (`15-orchestration-runbook.md`).
  **FW-27 SHIPPED** (`records-index.md`, agent-built, 21/11/18 rows).
- *2026-07-19*: **FW-17 SHIPPED** (22 js-marked Playwright tests, gate
  CLEAN, merge 9c722e0) and **FW-18 SHIPPED** (unreadable-record
  degradation per the gated 09 §5/08 §1 pair, SSE pane_block
  root-cause fix, swapError NIT; gate CLEAN, merge 4358a9d) — record:
  `reviews/2026-07-19-maintenance-round.md`. Master counts: CLI 976/3,
  UI 758, pyright ui 0. **FW-30…FW-36 (minus FW-33's build) now have
  GATED BUILD-GRADE SPECS** in `drafts/` — five drafts, all SOUND
  (record: `reviews/2026-07-18-deep-specs.md`); doc-16 Stage C rides
  its stated ratification gate; builds await the user's go + the §4
  ruling queue.
- *2026-07-19*: **FW-31 + FW-32 SHIPPED** (Y-22 proposal-time lint +
  Y-23 destination-bounded contradiction check; gate CLEAN first pass,
  merged da9e538) and **FW-34 SHIPPED** (Y-24 near-miss visibility +
  canaries + 12 §12; gate CLEAN + 2 delta-verified MINOR folds, merged
  8e48621). The four blocking rulings were resolved and folded into
  the fast-lane/settings drafts (both re-gated SOUND, a01147e) —
  **FW-35 and FW-30 are build-unblocked**. The DoD walk found and this
  round fixed a latent Y-8 defect: the post-route contradicts offer
  was unreachable live (proposal swept before the handler read it;
  FakeRunner masked it) — U-C3/C3b shipped the pre-verb capture +
  reload-defer + the instrumented falsification of the follow-up
  alarm (stale cached app.js). Record:
  `reviews/2026-07-19-rider-nearmiss-round.md`. **New backlog minted:**
  multi-edge offer abandonment; promote bucket targeting (FW-34
  amendment); near-miss snippet cap calibration (WATCH — 600 chars
  refused the first real draft); static asset cache-busting.

- *2026-07-19 (evening)*: **FEEDBACK ROUND 5 SHIPPED** — nine user
  items investigated (2 were silent-no-op UX gaps, not dead keys),
  two rulings taken (guided commit-first, pin intact; pane transcript
  persist+resume). U19 (overlay containment + no-op hints +
  collapsible raw YAML + humanize_ts + destination glosses), U20
  (host commit-drift verb + armed strip leg), U21 (post-iterate
  change summary), U22 (durable pane transcripts + Tier-2 SDK resume,
  Y-28) — all two-gate CLEAN, merged, live-walked. The walk found and
  the round fixed two latent defects gates couldn't see: action-bar
  error strips reload-wiped (leg-(a) marker missing) and every Tier-2
  resume aborting (SDK identity-probe vs pyright stubs). Record:
  `reviews/2026-07-19-feedback-round-5.md`. Backlog minted:
  bulk-graduate errors invisible (hx-swap="none"); keyboard path to
  Resume; js-flake watch; guided re-add for chezmoi drift.

- *2026-08-26*: **FW-100 env-override half closed** by U-fw100. `miner.reader_timeout_secs()` added (miner.py), env-overridable via `SELF_LEARN_READER_TIMEOUT_SECS`, sharing `worker._timeout_secs` with `worker.invoke_timeout_secs()`/`SELF_LEARN_INVOKE_TIMEOUT_SECS` — identical parsing, validation, and fallback semantics (no import cycle: `miner.py` already imports the `worker` module). Both reader call sites (`timeout=`, `timeout_display=`) now route through it, so the displayed timeout can no longer diverge from the enforced one. The `max_turns=60` reconciliation half of the row stays WATCH, owned by the miner-flip decision — unchanged by this unit, which does not touch `invocation_sdk/backend.py`.

- *2026-08-26*: **FW-116 measured** — the revert-based rollback exercised for real in a throwaway worktree (revert clean, config survives, both suites green, roll-forward byte-identical); the two runbook gaps it found are fixed in `17` §6 (full-history log for the sha; `SELF_LEARN_MINER_AUTOKICK=0` before touching a ledger copy). `plugins/self-learn/cli/scripts/suite` is now the sanctioned CLI suite runner (`dc82298`).

- *2026-08-27 (U-papercuts, P-3)*: **FW-81 recurred and self-cleared a second time** — `test_js_dom.py`/`test_js_dom_pane_persistence.py` were excluded from the UI baseline at 03:50 (58 run / 2 failed / hung at #59, `close_pane` never clickable), then measured clean again at 22:45 (98/98 twice on unmodified `50fa815`, full UI suite 1267/1268 — the one known pre-existing failure). FW-81's row (~:136) carries the full recurrence entry, the new untested observation (orphaned `playwright-mcp --headless --isolated` processes from earlier `walk4`/`walk5` UI-walk runs during the failure window), and the now-twice-confirmed standing rule (bisect the HOST axis only, judge on no-new-failures). No `--ignore` incantation for the two files was found documented anywhere in `docs/specs/self-learn` or `plugins/self-learn/ui` outside `reviews/` (not read, per this unit's scope) — the two files are simply back in the baseline as of this entry.

- *2026-08-27 (U-papercuts)*: **`17` §6.1 item 6's rollback instruction fixed** — bare `systemctl --user enable --now self-learn-miner.timer` fails with "Unit file does not exist" once the timer was ever taken down with `disable --now`: `disable` on a linked unit deletes the symlink itself, and a linked unit has no package to restore it from (measured 2026-08-27 on this host: `disable --now` printed `Removed '~/.config/systemd/user/self-learn-miner.timer'`, and `is-enabled` then reports `not-found`). The item now says to run `./install.sh` first (re-links the timer unit, `daemon-reload`) before enabling. Checked `13-hosting-and-separation.md` and `12-transcript-miner.md` for the same bare-enable instruction — neither contains it (doc 13's rollback section is about the product-repo migration, not the miner timer; doc 12 only names the timer, it never tells you to enable it). **Gate r1 N-3, checked-and-fine, not re-audited:** the four remaining `systemctl --user enable --now self-learn-miner.timer` occurrences in the repo are all FRESH-INSTALL context, not rollback context — bare enable is correct there because the unit was just linked by that same install pass and was never disabled: `README.md` (repo root, the quickstart), `install.sh:105` (the script's own printed instruction), `plugins/self-learn/README.md:142`, and `docs/specs/self-learn/drafts/public-release-spec.md:696`. One further, non-matching occurrence noted but NOT touched (out of this unit's scope): `docs/specs/self-learn/drafts/u-engine-shared-sdk-core-spec.md` §12.4 describes (not quotes verbatim) `17` §6's now-fixed rollback wording — a spec-draft description of the bug this unit fixed, not a fresh-install instruction and not the literal enable command a grep for it matches.

- *2026-08-27*: **U-servehermetic fixed a test-hermeticity hole in `serve.unit_dir()`.** `serve.unit_dir()` fell back straight from `SELF_LEARN_SERVE_UNIT_DIR` to the REAL `~/.config/systemd/user`, skipping `$XDG_CONFIG_HOME`, while the heartbeat it is compared against came from the hermetic cache (`XDG_CACHE_HOME`, redirected by every test via `conftest.py`). Once this host linked `self-learn-host.service` (21:49/21:53 the same day), 18 tests across six files (`test_doctor_invocation.py`, `test_selftest.py`, `test_hosting.py`, `test_lifecycle_cli.py`, `test_new_skill.py`, `test_selftest_hooks.py`) started reading the real unit as "configured" with no heartbeat ever written into the hermetic cache — all FAILed `self-learn doctor invocation`'s `serve` row. Invisible to the U-engine Phase 2 gate, which ran before any host had linked the unit. Fixed at the resolver: `unit_dir()` now checks `XDG_CONFIG_HOME` before falling back to the real path; `conftest.py` mirrors `XDG_CACHE_HOME`'s hermetic redirect for `XDG_CONFIG_HOME`. `install.sh`'s `UNIT_DIR` carried the same unconditional `$HOME/.config/systemd/user` literal — realigned to the same `${XDG_CONFIG_HOME:-$HOME/.config}` rule, plus a new `PORT2` sibling test proving it (positive control: `XDG_CONFIG_HOME` set → the link lands there, not under `$HOME/.config`). Doc literals aligned: `17-invocation-runbook.md` §10's `install.sh` bullet; the three shipped unit files' header comments (`systemd/self-learn-{miner,ui,host}.service`). `13-hosting-and-separation.md` §7 and `17` §6.1 were checked and do not name this path — no edit needed there. **Code gate r1 fold, same day:** (M-1) `install-commands-test.sh` had the identical leak one layer up — its `run_install` set only `HOME`, so a caller's real `XDG_CONFIG_HOME` passed straight through to the `install.sh` it drives, leaving four unit symlinks under that external directory (measured: the script's own pass line, exit 0, four stray links, none cleaned by the script's own trap). Fixed by pinning `XDG_CONFIG_HOME="$FAKE_HOME/.config"` in `run_install`, plus a new positive-control test in the pytest wrapper (`test_regime_fixes.py`) and the negative-control test (`test_port2_negative_control_enabling_the_unit_is_caught`) routed through the shared helper instead of duplicating its env setup. (M-2) `plugins/self-learn/README.md:137` carried the same literal — the FIRST doc-literal census (above, this same entry) had checked `docs/specs/self-learn` and `systemd/` but never the plugin's own README, so it read as exhaustive while omitting a real site; fixed, and the omission itself is why this follow-up sentence exists. Also folded: `test_serve.py`'s `XDG_CONFIG_HOME`-clearing comment corrected (it claimed no default value exists in this test process, which is false — this unit's own conftest sets one for every test); `17-invocation-runbook.md` §10 gained one sentence distinguishing `install.sh`'s invoking-shell-environment evaluation from the systemd user manager's own (`systemctl --user show-environment` shows the manager's view). Suite: 2393/18/6 (broken) -> 2417/0/6 (fixed, +6 new tests: 4 in `test_serve_hermetic.py`, 1 `PORT2` sibling in `test_serve.py`, 1 positive control in `test_regime_fixes.py`; no env prefix on this host). Registered as `FW-132` below.

- *2026-08-27*: **Register item #11 SUPERSEDED, not fixed, by `U-engine` Phase 2 (gate r1 M-3).** The miner-to-worker handoff's cgroup-reap problem (`self-learn-miner.service` `Type=oneshot`/`KillMode=control-group`; `worker._spawn_window`'s `start_new_session=True` escapes the process group but not the cgroup) is not patched — it is made moot: `self-learn serve` starts both jobs from one live process, so there is no detached child for a cgroup to reap. The three unit-file remedies register item #11 named (`KillMode=process`, `systemd-run --user --scope`, a chained `self-learn-worker.service`) are superseded, not applied. **FW-116-adjacent:** Phase 2 adds a SECOND thing an operator can revert (`17` §6.1) — `serve` reverts by reverting its own commit and re-enabling `self-learn-miner.timer`; Phase 1 (the shared library) reverts under Phase 2, so the runbook's phase-ordered rule is revert Phase 2 first. FW-116 stays OPEN: this is a documented procedure for the NEW revert surface, not yet a measured one — no dry-run of the Phase-2 revert has been run, mirroring FW-116's own original gap before its 2026-08-26 dry-run.

- *2026-08-28*: **U-kl4 SHIPPED** — root-cause fix for `test_invocation_sdk.py::test_kl4_hang_sigterm_ignored_child_is_gone_after_run_sync_returns`'s coin-flip false red. The old check polled a HOST-GLOBAL `pgrep -f fake_clau[d]e.py`, which matches ANY process on the machine whose argv names the fixture — including a concurrent builder's or gate's own `scripts/suite` run (sanctioned to run its three batches in parallel) or a stale orphan; measured 2/2 parallel base runs red, solo green. Rebuilt to identify the child by the PID this exact run spawned, exposed on a new `SdkOutcome.child_pid` field (`invocation_sdk/backend.py`) rather than a name pattern, with a positive control (`test_kl4a_...`, monkeypatches `sdksession/teardown.py`'s `kill_child` to a no-op and confirms the check reddens) and a PID-reuse guard (`/proc/<pid>/stat` start-time comparison). A first version threaded the pid through `spec.log()` as a new operator-visible line instead — measured to break five byte-pinned log-shape tests (`test_lg1`/`test_lg6`/`test_fk2` in `test_invocation.py`, `test_ou4` in `test_invocation_sdk.py`, `test_fl2` in `test_worker_contract.py`) before it shipped; reverted for the `SdkOutcome` field instead. Verified 5/5 green under real concurrent host load (a full `scripts/suite` run backgrounded via shell job control while the test looped 5x in the foreground) and against a manufactured decoy `fake_claude.py` process left running throughout — the old pgrep idiom would have seen it (confirmed separately), the new check did not. Full CLI suite green under `SELF_LEARN_SERVE_UNIT_DIR=$(mktemp -d)` (this host's linked `serve` unit otherwise fails the `doctor` `serve` row, unrelated to this unit): 2413 passed / 6 skipped / 0 failed (base 2411/6/0 + this unit's two added tests, `test_kl4a` (the kill-neutered positive control, orphan-safe via a `finally` that re-checks the child's start ticks before `SIGKILL` and reaps with `_reap_best_effort`) and `test_kl4b` (`child_pid is None` when no child was spawned)). pyright: 285 → 284 (strictly better; the removed `pgrep` polling loop's `reportPossiblyUnboundVariable` on `found` is gone, no new errors).

- *2026-08-28*: **`U-cachelit` fixed the cache-litter defect (31,291 stray `~/.cache/self-learn/home-<digest>` namespaces, 1.1 GB) at its root — the UI package's module-scoped real-server test fixtures, not this package's own worker.** A namespace-diff instrument (a temporary pytest plugin snapshotting the REAL `~/.cache/self-learn` before/after every test item, resolved from the pre-fixture invoking-shell env) cleared this repo's CLI suite across five full runs — parallel `scripts/suite` and each of its three batches run serially, all zero leaks — then caught the actual site on the first UI-suite run: `plugins/self-learn/ui/tests/test_js_dom.py`/`test_js_dom_pane_persistence.py`'s module-scoped real-uvicorn-in-thread server fixtures process browser-triggered background requests through `self_learn_ui.ledger._invoke_json`, which reads live `os.environ` on every subprocess call (no production call site passes an explicit `env=`) — outside the boundary of any one test function's `monkeypatch` window, because pytest instantiates a module-scoped fixture before a module's first test's own function-scoped one. Root-fixed with a session-scoped, never-torn-down hermetic env floor (`_env_floor_session`, `plugins/self-learn/ui/tests/conftest.py`) that the pre-existing per-test `_redirect_env_defaults` fixture now always reverts INTO, never to a real unset default; verified with 3 clean targeted re-runs of exactly the two leak-site files plus 2 full-suite runs, zero new real namespaces every time, against 2/2 leaking full-suite runs before the fix (each attributing the single leak to a different pane-lifecycle test — a genuine race, not a fixed call site). A forward guard (`_litter_namespace_guard`, both packages' `conftest.py`, session-scoped autouse — `pathlib.Path.mkdir`/`subprocess.Popen.__init__` class-level patches, chosen specifically because this codebase's `from ... import resolve_home`/`cache_dir` pattern makes a same-module function patch installed after collection unreachable) fails either suite's own session loudly, by name, on any FUTURE regression of the same class — positive-control-verified in both packages (a throwaway `delenv` test triggers the exact bypass; the guard's teardown assertion fires naming the created namespace; a one-line mutation disabling that assertion was applied, confirmed the same leak then goes unreported, and was reverted, sha256-diffed byte-identical to before). A read-only dry-run litter listing (`misc/cachelit-litter-listing.py`, git-excluded) measured 31,204 litter namespaces / 682.9 MiB at a 7-day age floor (31,269 / 684.5 MiB at 0 days) for the user's own intervention — it never deletes. Armor re-pinned in the CLI package (pure whole-file appends, nothing existing edited): `test_worker_contract.py::_ARMOR_SHAS`'s `conftest.py` sha256, `test_u_sdka.py::_AR1_SANCTIONED_PIN_LINES` (44 → 195 lines) and `test_hy5_numstat_bounds_hold`'s `conftest.py` bound (44,0) → (195,0). No production code touched — CLI 2419/6/0 and UI 1270-passed-plus-the-one-known-pre-existing-failure both unchanged before/after; pyright unchanged both packages (CLI 285/285, UI 162/162, measured by swapping the four changed files against `git show 84d0b78:<path>` copies).

- *2026-08-28 (code gate r1 fold, same day):* gate r1 on `U-cachelit` returned NOT CLEAN — 0 Blockers, 3 Majors, 4 Nits/Defers — all addressed by deleting and rewriting the guard section in both `conftest.py` files (never edited in place; a pure suffix extension over the pre-`U-cachelit` baseline either way). **M-1** (no committed test proved the guard fires — a `return` right after its `yield` left the whole suite green): added `test_litter_guard_probes.py` to both packages (byte-identical), five `pytester`-driven probes (A–E) that splice the SHIPPED `conftest.py` fixtures into a genuinely separate sub-session by absolute path and drive one bypass class each; the same `return`-after-`yield` mutation turns all five red. **M-2** (the CLI package had no session-scoped floor at all — only the function-scoped `_worker_test_defaults`, so doc 13 §6's "whole session" claim was false for CLI): added `_env_floor_session` to the CLI `conftest.py`, floor-matching the UI one; doc 13 §6 reworded from "per test" to "session floor + per-test". **M-3** (attribution hashed the raw `SELF_LEARN_HOME` string from a subprocess's explicit `env=` kwarg, so a child spawned with NO `env=` at all — probe C, `worker._spawn_run`'s own detached-miner shape — or a non-normalized path — probe E — fell to warn-only): attribution is now digest-based, `sha256(Path(home).expanduser())[:8]` (plus, defensively, the additionally-`.resolve()`-d form) registered for every home this session ever hands out, regardless of how a creating process received it; probes C and E now FAIL, probe D (a namespace this session never tracked — a concurrent sibling) still only WARNS; the warn channel moved to a `pytest_terminal_summary` hook so it is visible even under `-q`. **N-fixes**: `_env_floor_session`'s docstring corrected to admit it DOES `yield`/`mp.undo()`; `misc/cachelit-litter-listing.py` derives the live ledger's own namespace from `SELF_LEARN_HOME`'s digest instead of a hardcoded `home-0f24de4d`; the two class-level patches (`Path.mkdir`, `Popen.__init__`) are restored to their originals in the guard's own teardown before its assertions run, instead of staying patched for the rest of the interpreter's life; doc 13 §6 now notes the pre-existing bare-`pytest`-from-root collision between the two packages' same-named test modules (measured: 18 collection errors), unrelated to this unit but adjacent to the same section. Re-pinned again: `test_u_sdka.py::_AR1_SANCTIONED_PIN_LINES` 195 → 311 lines, `test_hy5_numstat_bounds_hold`'s `conftest.py` bound (195,0) → (311,0), `test_worker_contract.py::_ARMOR_SHAS`'s `conftest.py` sha256 re-measured — all three re-verified passing. Suites re-measured post-fold: CLI 2419 → 2424 passed (+5 probes), 6 skipped, 0 failed, rc=0 across all three `scripts/suite` batches; UI 1270 → 1275 passed (+5 probes) plus the one known pre-existing failure, unchanged. pyright unchanged both packages (CLI 285/285, UI 162/162), re-verified by the same swap-against-base technique. The guard's concurrency-aware attribution was also confirmed live (not just by the synthetic probes): both suite re-runs overlapped in time with a genuinely concurrent sibling builder on this host (`U-hostmode`'s background test run) and the guard correctly WARNED, never failed, naming that builder's own lock-file artifacts under the real cache root. Registered as `FW-130` above.

- *2026-08-28*: **U-verbs spec landed** (S-54, FW-133..138 reserved;
  FW-85/114/115 close at build time).
- *2026-08-28*: **U-armor spec landed** (S-55, FW-140/FW-141
  reserved; FW-139 deliberately skipped — a tombstone in the
  `U-verbs` spec). Five blind spec gates, SOUND at r5.

## 6b. Dated additions log

- *2026-07-18*: FW-30 (settings surface) added on user request.
- *2026-07-18 (later same day)*: FW-31…FW-36 added from the user-seat
  pain-point analysis + the user's re-ranking + the worker-domain
  ruling ("(c)-ish": three domains pinned now — per-record judgment /
  transcript intake / portfolio synthesis — auditor built only when
  FW-33's digest is spec'd). Full ecology design:
  `forward/worker-ecology.md`. Downranked in the same ruling:
  challenge verb (deferred), cold-read audit (superseded by FW-31 at
  entry level).
- *2026-07-25*: FW-37…FW-39 added per `drafts/ui-inflight-feedback-
  spec.md` §7.3/§9.2 (unit gated SOUND, builder-landed; corpus
  amendments per its own §7). FW-37 (per-verb latency measurement)
  is R-1's explicit deferral — indicators landed on the three confirm
  routes + the bulk-graduate loop only, refusing to decide the other
  39 htmx verbs' latency without data. FW-38 (the state-resync
  envelope) closes the residual post-reconnect silent window the unit
  measured and disclosed rather than fixed (§8 of that spec; R3-M2;
  recorded standing in `03-decisions.md` S-20 per that spec's R4-m3).
  FW-39 (the reusable perceptual harness) is that spec's Unit 2 —
  explicitly out of scope for the unit that just shipped (R-5: no
  harness generality there), factored out as its own future item
  instead.
- *2026-08-08*: FW-82 (background review automation) added per the S-29
  ruling — user-configurable tiered autonomy, supervised-first
  sequencing (Checkpoint A before any automation), FW-76 promoted to
  its prerequisite.
- *2026-08-08 (evening)*: FW-83 (unattended trace elicitation fails at
  13–30% yield, blind unthrottled retry) added from the flip's maiden-run
  measurements; prerequisite to FW-82. Evidence:
  `research/2026-08-08-worker-maiden-run-elicitation-failure.md`.
- *2026-08-08 (later)*: FW-84 (producer-attribution race — worker landing
  destroyed attended-session proposals, ledger commit `efd5ebd`) added,
  measured from both seats in one live incident; binds U-repair's design.
- *2026-08-08 (night)*: `U-repair` built against FW-83's gated r4
  spec — an exemplar-driven elicitation contract, one bounded repair
  round guided by a checklist rather than a second refusal line,
  `INVOKE_TIMEOUT_SECS` 900→1800, a leftovers-conditional follow-on
  backoff, `--strict-mcp-config` hygiene, and Rule-F (foreign-proposal
  detection). Code gate: **0 BLOCKER / 3 all-test MAJORs**, folded and
  re-verified; merged the same night. Two accepted residuals recorded
  as `S-30` (a repair that clears one Set-C defect and leaves a
  sibling is deleted, not re-repaired — one round, not iterated,
  until the post-merge `repair round: X of N cleared` series says
  otherwise) and `S-31` (Rule-F leaves a proposal it did not write,
  matched by `record_sha`, entirely alone). **FW-84** (added earlier the same day from the live `efd5ebd` incident) now carries:
  full producer attribution for the seven residuals `U-repair` §7.3
  names and only partly closes — five are FW-84's (four via one
  `_written_since` provenance root, a fifth via a different
  create-only-write direction), two are not (the foreign-hook-commit
  trade stays `U-repair`'s own; backoff visibility is FW-82's).
  Sequenced with or immediately after `U-repair`. Evidence: `U-repair`
  spec §7.1–§7.4
  (`docs/specs/self-learn/drafts/u-repair-unattended-elicitation-spec.md`),
  `03-decisions.md` S-30/S-31.
- *2026-08-09*: **FW-76 FIXED** (`U-forcefail` — `worker_kick`/`mine_run`'s
  error leg: a Map-scoped `data-verb-error` marker + render-scoped
  `role="alert"`, `error` as a fourth `inflight` Map rule distinct from
  `done`'s bare delete, and the server dropping its erase-on-failure
  `_force_refresh`/`HX-Redirect` on `not result.ok`; spec
  `drafts/u-forcefail-error-leg-spec.md`). Code gate CLEAN — 0 BLOCKER /
  0 MAJOR / 5 NOTE; all 13 named mutations independently reddened; an
  end-to-end real-`.click()` probe reproduced the original 2026-08-06
  failure shape pre-fix and passed post-fix. Residuals (a)/(b)/(h)
  recorded on the row. **FW-85 added** (the CLI exit-code contract unit
  residual (a) named but refused to fix from inside the UI gate —
  `worker kick`/`mine run` both return `EXIT_OK` for outcomes a human
  would call a non-fire) — **FW-82's prerequisite item (4) re-pointed**
  from FW-76 to FW-85, since FW-76 gives the UI's human a truthful
  signal but not automation's machine-readable one.
- *2026-08-09*: `U-attrib` built against FW-84's own gated r4 spec — an
  EXCLUSIVE stage (`Stage-1`) the analyst writes to and nothing else
  can, with `Grant-1`'s batch invocation granted the stage alone;
  `Install-1` moves a validated staged file into the ledger under the
  commit lock, atomically, with an install-journal recovery path for a
  crash between the copy and the stamp; `SELF_LEARN_STAGE=0` reverts
  the whole namespace for an incident window, independent of
  `SELF_LEARN_ENFORCE_SCOPE=0`. This is the create-only direction the
  spec adopted over stream-json parsing and a per-record lease (both
  refused: two producers legitimately write the same path, so a
  record of the model's writes alone cannot answer whose bytes these
  are), closing four of `U-repair`'s five named residuals and
  retiring `E-4`/`E-5`. All 50 of the spec's mutations REDDENED, full
  CLI suite green (1566 passed, 5 skipped, 0 failed), pyright clean
  against the standing 50-error baseline. Two accepted residuals
  recorded as `S-32` (attribution is now structural, not
  observational) and `S-33` (the permanently-blocked destination, and
  a mid-window destination change losing one window). Evidence: this
  unit's own spec §7, `03-decisions.md` S-32/S-33.

- *2026-08-09 (night)*: FW-86 and FW-87 added from the U-seam code
  gate's findings (Wave 0 of the Agent-SDK migration, merged
  `e2e63a2`): FW-86 records the CN9 taint-scan's measured module-level
  blind spot (WATCH — unreachable today, verified mechanically); FW-87
  graduates the U-seam spec's `R-1` finding (analyst bare-`OSError`
  traceback loses the lesson) into a build item owned by U-sdka.

- *2026-08-10*: FW-88 added at U-sdk's merge (`b1a3b83`) — the spec's
  `R-2` residual (sdk hatch wider than cli on strict-settings hosts;
  exact mirror on this one) graduated to a WATCH row per the U-seam
  gate's precedent of recording measured residuals where the next
  session can find them.

- *2026-08-18*: FW-89 through FW-92 added during the U-bedrock build
  (spec `docs/specs/self-learn/drafts/u-bedrock-provider-spec.md`
  §7.3), per that section's explicit "owner: an FW row" for four of
  its eight residuals — `R-1` (silent inertness under `backend=cli`,
  ruled `D-18`), `R-4` (IMDS/instance-profile mechanism unprobed, why
  the credentials row is WARN-never-FAIL), `R-5` (a test's NAME goes
  stale because `SU2` forbids renaming it in the same unit that
  changed the count), and `R-7` (the failure class — IAM denials,
  region/model access, throttling — that only a live Bedrock endpoint
  can surface, and that this unit's suite structurally cannot reach).
  Landed in the build commit, ahead of that unit's blind code gate.

- *2026-08-19*: FW-93 and FW-94 added at the U-bedrock code gate's fold
  (1 BLOCKER / 3 MAJOR / 9 NOTE; BLOCKER-1 and MAJOR-2 fixed in fixtures,
  MAJOR-3 fixed as a comment correction, MAJOR-1 ratified — `03-decisions.md`
  `S-38`). FW-93 records U-sdk's own spec text going VOID on one point
  (`A-4`'s stated mechanism for "`options.model` never `None`", superseded
  by the `IN3`/`Int-1` wiring this fold ratified). FW-94 records a new
  per-run operational fact the fold's NOTE (2) surfaced: `[sdk]`-extra
  installs now spawn `claude --version` on every `--selftest` run, where
  none ran before U-sdk + this unit's doctor wiring.

- *2026-08-19 (later)*: FW-95…FW-97 added by `U-docs` (Wave 2,
  docs-truth sweep). FW-95 (BUILD) records that the burn-in cost gate's
  `cli` baseline has no product-side instrument, ruled resolved by
  reading the operator's own billing surface rather than the product.
  FW-96 (BUILD) records `12-transcript-miner.md`'s self-contradiction on
  the miner's scheduler (§3 `cron-claude` vs §6 Q5 systemd), found by the
  sweep's `I-26` and left uncorrected as out of this unit's
  invocation-layer mandate. FW-97 (WATCH) records the README revision
  log's ~26-day, roughly-two-dozen-unit gap, deliberately not
  back-filled. Evidence: `drafts/u-docs-truth-sweep-spec.md` §3.5.

- *2026-08-19*: FW-98 through FW-102 added at the `U-sdkr` build (spec
  `docs/specs/self-learn/drafts/u-sdkr-reader-contract-spec.md` §7.2's
  explicit "owner: an FW row" for four residuals, plus `Mit-c1`'s watch
  carrier). FW-98 is the stale `test_invocation_sdk.py::_miner_argv()`
  mirror left behind by `Fix-1` (`R-1`); FW-99 is the analyst's
  identical strict-MCP gap, ruled no-CLI-fix-ever per the user's `F3`
  (`R-2`); FW-100 is the reader's timeout-vs-`max_turns` reconciliation,
  owned by the eventual miner-flip decision (`R-3`); FW-101 records
  `Containment.strict_mcp` as a deliberately unread second witness, not
  a wart to clean up (`R-4`); FW-102 carries the watched-first-nightly
  enforcement (`Mit-c`) and the operator preflight command (`Mit-a`)
  forward into a home outside this spec, pending `U-docs`. **Renumbered
  from the spec's own `FW-95`–`FW-99`** at the code-gate fold (NOTE 5):
  `U-sdkr`'s worktree branched before `U-docs` merged and both claimed
  the same id range from the same base; `U-docs` landed first and took
  `FW-95`–`FW-97` (and `S-39`–`S-44`) on master, so this unit's rows
  move to the first free ids per the loser-rebases rule. The spec
  draft's own prose (§"Files this unit may touch", §9's `E10`, §10's r3
  entry) still says `FW-95`/`S-39` — historical, describing the plan as
  gated, not the ids as shipped; `03-decisions.md`'s `S-39`/`S-40` move
  to `S-45`/`S-46` the same way, recorded there.

- *2026-08-19*: FW-103 through FW-108 added at the U-sdkw build (Wave 2
  of the Agent-SDK migration, spec `docs/specs/self-learn/drafts/
  u-sdkw-worker-contract-spec.md` r5, CLEARED FOR BUILD), per §7.4's
  explicit "handed to `14-forward-work-map.md`" list. FW-103 (`R-1`,
  narrowed by ruling `V-2`) and FW-104 (`R-2`) both fold into the same
  live-model gap the worker's burn-in is meant to close. FW-105 (`R-3`)
  flags `BG3`'s dependence on the fake's undocumented echo behavior.
  FW-106 (`R-4`, ruling `V-3`) carries the tool-events privacy
  surfacing-boundary sentence, owner `U-corrob`. FW-107 (`R-9`, ruling
  `V-4`) and FW-108 (`R-10`) are the two REAL product findings the
  blind gate surfaced and this no-product-code unit routed rather than
  fixed (§7.5) — a fully-denied `sdk` run reads identically to a
  wrote-nothing run in `worker.log`, and a `CharterPatternUnsupported`
  failure is silent on `sdk` where the `cli` equivalent logs a line.

- *2026-08-19*: **`U-sdka` built and merged into this worktree** (the
  analyst-surface SDK flip — `DEFAULT_BACKEND_FOR_SURFACE["analyst"]`
  now `sdk`, the other three surfaces unchanged). **FW-87 moves BUILD →
  DONE**, disposition note added to the row: the fix is a transport-table
  data change (not a `try/except` wrapped around `analyze`), the sdk leg
  needed its own catch (`CLIConnectionError`, not `OSError` — the row's
  literal prescription would have left it open), and it closes `U-seam`
  residual `R-1` at the same time. **FW-109 through FW-113 added** from
  this unit's spec §7.3 residuals: FW-109 (`R-1`, the analyst's SDK path
  has no settings witness — WATCH, unreachable until the analyst's
  containment grows a non-empty write set); FW-110 (`U-seam`'s `R-2`,
  the surfaces' log wording divergence — never got its own row at
  U-seam's 2026-08-09 code-gate fold, recorded now rather than left an
  orphaned citation, with `U-sdka`'s fifth divergent string noted);
  FW-111 (`R-3`, the cli rollback switch re-opens F3's argv exposure —
  WATCH, closes when `U-cleanup` removes the cli path); FW-112 (`R-5`,
  the sdk default is buildable only via a DEV dependency group, `V-1`'s
  refusal rests on it — WATCH, three re-open triggers carried verbatim);
  FW-113 (`R-6`, a rung-1 env shadow is a lower bound on a flip's blast
  radius, not the true failure set — WATCH, naming the worker/miner
  Wave 3+ flips as the trigger for the two-census-pass technique).
  `R-4` (the burn-in, attended and operator-run) got no row, per its own
  owner assignment. This worktree's own file (base `89f8ef7`) ends at
  `FW-94`, but `origin/master` had already independently reached `FW-102`
  by the time this build ran (`U-docs`/`U-sdkr` landed there first, per
  this unit's task brief) — **checked `origin/master` directly and
  assigned `FW-109`…`FW-113`, origin's true next-available range**,
  rather than sequencing from this worktree's own stale `FW-94` (which
  would have collided with `U-docs`/`U-sdkr`'s already-landed `FW-95`…
  `FW-102` at merge). Same reasoning applied to `03-decisions.md`'s
  `S-47`/`S-48` below. Per this unit's build report.

- *2026-08-25*: **FW-117 added** at the U-cleanup-B **code-gate r1** fold (NIT-4). `worker.write_repair_settings_file` (`worker.py:3224`) is now a dead write under the sdk-only seam — the file it writes has no reader, since `options_kwargs()` builds `settings=None` unconditionally and the cli-era `--settings <path>` reader is gone. Spec-conformant (§8.1 kept the function on purpose; `test_rp1`/`test_ha1_hatch_open_omits_default_mode`/`test_repair.py::d5` assert the file's existence as part of the repair contract). No code changed for this finding — the gate's own ruling was "recommend a forward-work row, do NOT change the code." Not scheduled; two candidate directions recorded on the row itself.

- *2026-08-26*: **FW-107 and FW-108 closed by U-opsfix** (task note `3pKzmkglwvyogdBYQqJaV`, no spec gate — user ruling routed this unit straight to a single blind Opus code gate; r1 came back NOT CLEAN, one blocker + one ruled major + nits, folded below; r2 CLEAN). FW-107: `worker.py`'s FAILED summary now carries an additional `run: {count} charter denial(s) this run — see worker.tool-events.*.jsonl in <cache dir>` line whenever the run's charter-sourced denial count is nonzero (`<cache dir>` is the resolved `worker.cache_dir()` — gate r1 `N-2`: no `run_id` is in scope at that point, so the file is named by glob against a real, `ls`-able directory rather than a filename that can't be produced); the byte-pinned FAILED line itself is untouched. FW-108: the `CharterPatternUnsupported` leg in `_drive` (`invocation_sdk/backend.py`) now renders the SAME `LOG_TEMPLATES[surface].os_error` under the SAME `_CATCHES_OS_ERROR` surface guard its `ClaudeSDKError`/`OSError` neighbours already use, and — unlike them — never re-raises, since no session has started yet (gate r1 `M-1`, ruled: the r1 build had reasoned its way to an equivalent unconditional-render form with an 11-line rationale for skipping the guard; the gate ruled the guarded form is what the mandate actually intended, cut to two lines). Gate r1 `B-1` (BLOCKER): the r1 build's `test_ev4_tool_events_string_confined_to_events_module` amendment was a whole-FILE exemption for `worker.py` — the gate proved it unenforced (a real `_gate_probe_tool_events_path()` appended to `worker.py` stayed green, and the same probe in `miner.py` also stayed green). Replaced with the narrowest pin that admits the build: `worker.py` may contain the literal substring `"tool-events"` EXACTLY ONCE, and that one occurrence must be the pinned FW-107 log-line fragment (checked against the literal text, not just presence); every other module, `worker.py` included beyond that one line, still holds to the original zero-occurrence confinement rule. Verified against a scratch copy of the source tree (not the worktree): a second `"tool-events"` occurrence added to a copy of `worker.py`, and the gate's own probe added to a copy of `miner.py`, both correctly go red under the narrowed check; both were reverted before the check was re-run clean. Tests: `tests/test_u_opsfix.py` gained the r1 build's two FW-108/FW-107 tests (updated for `N-2`'s wording) plus two r2 additions — `M-1`'s guarded-surface assertion (a surface whose `_CATCHES_OS_ERROR` entry is `False` renders no line) and `N-3`'s inverse (an `SdkOutcome.denials` entry with `source == "sdk-result"` — the SDK's own `permission_denials`, never seen by the charter callback — does not count toward the new line); `charter_denials`'s annotation is `list[dict[str, Any]]` (`N-4`). `tests/test_worker_contract.py`'s `WS5d` guard stays narrowed as r1 shipped it (gate r1 `N-1`: downgraded, left as is) — no `.stdout` read off a bound `write_session`/`text_session` result, rather than a blanket ban on ever binding the result.

- *2026-08-27*: **This entry, belatedly, for FW-118, FW-119, FW-120, FW-121** — the four rows themselves were added by `U-engine` Phase 1's own commit (`ead58ad`) per `docs/specs/self-learn/drafts/u-engine-shared-sdk-core-spec.md`, but this section's own convention of one dated entry per FW-row landing (e.g. FW-100/FW-116/FW-117 above) was never discharged for them — found by gate r1 M-3 on the Phase 2 build, fixed here rather than left owed a second time. FW-118: the shared `sdksession` library (`U-engine` Phase 1, landed `ead58ad`). FW-119: `self-learn serve`, the long-lived host process (`U-engine` Phase 2, landed THIS build — register item #11 superseded, see the §6a entry above). FW-120: the library ships inside `self-learn-cli` rather than as its own distribution (WATCH, no code owed). FW-121: `S-48`'s `M11` evidence names the wrong symbol (BUILD, not scheduled — a docs-only fix belongs with whoever next touches `S-48`).

- *2026-08-27*: **FW-51 CLOSED** by unit `u-verbguards` (T1 gate-characterized fix; blind Opus code gate CLEAN r3 with mutation verification; merged 2026-08-28). Root cause: `ledger_ops.resolve_record` resolved a record via `find_record_path` with NO status precondition at all, and every resolution verb (`route`/`reject`/`graduate`/`supersede`, plus `route --supersedes`'s in-line completion) inherited the hole — fixed in ONE place: a new `ledger_ops.require_status(home, record_id, allowed, *, verb)` that `resolve_record` and every resolution verb now consult before any lock/mutation, plus a dedicated `ledger_ops.supersede_cycle_check` graph walk for the supersession-cycle case `require_status` alone cannot see (a hand-edited or migrated ledger's live record with a stale `superseded_by` pointer — see the census/DODs in the build report). Allowed-source sets pinned by the 2026-08-26 verb assessment: `route`/`reject`/`defer` → `{pending, deferred}`; `graduate`/`supersede` (both sides) → `{pending, deferred, routed}`. Exit code 1 with a status-naming refusal (`confirm-held`'s wording shape, 11 §2.2), never 64 for a record that exists — `route`/`reject`/`defer` used to search `pending/` only and lied "not found" for a resolved record; `find_record_path`'s DEFAULT (pending AND resolved) fixes that lookup, and the status check supplies the correct refusal. `rehome`/`rescope` were already correct (the pin this unit generalizes) and were left untouched — their pinned refusal wording is asserted verbatim by `test_rehome.py`. `confirm-held`/`confirm-recurrence`/`dismiss-suspect` already had the correct BEHAVIOR (ad hoc `record.status != "routed"` checks) and, per code-gate r1's root-cause-extras fold, now route through `require_status` too — a new `reason` kwarg lets the helper emit their EXACT pre-existing message text (pinned wording asserted by `test_dismiss_suspect.py` stays byte-identical), so the whole family shares one implementation. `rehome`/`rescope` are the one deliberate exception: their predicate is the bucket (directory) AND status together, not status alone, and their wording is pinned by `test_rehome.py` — left as their own inline checks. The UI POST half (`routes.py` `action_arm`/`action_confirm`) was never an independent hole this build needed to close separately — it validates only `verb in _KNOWN_VERBS` and then shells the SAME CLI via `VerbRunner`/`RealRunner`, so the fix propagates by construction; `test_routes.py` adds a discriminator that exercises the real guard in-process (not a hand-typed stderr string) and asserts the UI renders it as a visible refusal, never a success redirect — gate r1 found its `data-armed`/`HX-Redirect` assertions inert and replaced them with the exact (markupsafe-escaped) refusal text plus a disk-bytes-unchanged check. `followup-done`'s own status gap (it could clear a stale `follow_up` block on an already-superseded record — `open_followups()` already excluded such a record from "open", but `followup_done` itself never checked) was found during this build's census — gate r1 CORRECTED this row's original "does not mutate status, out of scope" disposition: the criterion for needing `require_status` is whether a verb's correctness is GATED ON status, never whether it happens to WRITE `status` itself. Now fixed: `followup-done` requires `routed`, pinned by `test_followup_done_refuses_after_status_leaves_routed`. Gate r1 (0 B/3 M/10 N/D) also found `route_direct`'s OWN `--supersedes` guard had zero coverage (load-bearing — its absence produces an exit-2 half-write from inside `_ledger_write` at the `teach --route` surface — the orphan in `resolved/` makes teach's fallback capture fail, so the capture is LOST — not a clean refusal) and that 3 of the 10 original refusal tests asserted file bytes only, 1 HEAD only, and two cycle tests neither — all folded in this same pass, plus a direct `supersede_record(home, X, X)` self-cycle guard (one line, `supersede_cycle_check` alone cannot see it since a live record's own `superseded_by` is always `None`). `self-learn host commit-drift`'s unrelated exit 64 "not found" is a different, host-family verb — 64-mapped by design, out of this FW's scope, not touched.

- *2026-08-27*: **FW-128, FW-129, FW-130, FW-131 added by `U-corrob`** (`docs/specs/self-learn/drafts/u-corrob-tool-events-consumer-spec.md`, T1, one blind Opus code gate). FW-128: the in-memory tool-events/denials corroborator (`src/self_learn/corroborate.py`) closing `S-44`'s consumer clause — landed for the worker and the miner-reader; the analyst's `DEN3` leg (`teach --route`'s denial-count print) could NOT be built this cycle (see FW-131) and was reverted rather than shipped, so `analyst.py`/`teach.py` carried no changes from this unit **(SUPERSEDED 2026-08-28 — see the entry directly below: the coordinator ruled DEN3 BUILD IT, and both files now carry this unit's changes)**. FW-129: surfacing tool events into the ledger/report/UI stays refused, with the scrub measurement recorded. FW-130: the 31 269-namespace/1.1 GB cache litter finding, owned by `U-cachelit`. FW-131: the analyst's blind denial/missing-file finding — its denial half was attempted and blocked by `test_hy5_numstat_bounds_hold`'s zero-headroom `analyst.py` bound, recorded on the row itself.
- *2026-08-28*: **FW-131's denial half (`DEN3`) landed, coordinator ruling.** The 2026-08-27 disposition above is superseded for that one half: the `(4, 18)` `analyst.py` hy5 row was armor bookkeeping, not a design constraint, so it was re-pinned to the measured `(22, 20)` — dated and justified in place, `test_u_sdka.py` — rather than treated as a hard boundary a second time. `analyst.analyze` gained the keyword-only `charter_denials` accumulator (`FW-107`'s shape) and `teach --route` now prints the denial-count line on both branches of `_route_now`'s `try`, which passes `charter_denials=` UNCONDITIONALLY. A first attempt made that call conditional on `inspect.signature(analyst.analyze)`, to sidestep an armor collision without editing a pinned test file — the coordinator reversed this: product code must not bend to a test double. The root-cause fix instead re-pins the armor the collision actually belongs to: `test_route_cli.py`'s `fake_analyze` stand-in (pinned by `test_u_fake.py`'s `DS1`, itself whole-file-hashed by `test_worker_contract.py`'s `_ARMOR_SHAS`) was edited to accept the new kwarg, and both armor layers were re-pinned to match, each with a dated justification in place — three sanctioned armor motions total (`test_u_sdka.py`, `test_u_fake.py`, `test_worker_contract.py`). `FW-131`'s row and this file's own convention are both updated to the BUILT form.

- *2026-08-28*: **FW-117 CLOSED** by unit `u-fw117` (T1 gate-characterized
  fix; blind Opus code gate CLEAN r3 with mutation verification; merged 2026-08-28).
  Root cause closed by deletion, per the user's standing ROOT CAUSE
  preference: `worker.write_repair_settings_file` — the dead write this
  row named — and every witness of it are gone (see the FW-117 row above
  for the full deletion census, suite/pyright numbers, and `git diff
  --stat`). The row's own two candidate directions: direction (1),
  delete, is the one taken; direction (2), give the sdk path a reader
  and restore a settings-file witness for worker-repair alone, was
  considered and rejected — it would reopen a witness the unit's own
  `A-2` design ("the charter is the only authority", `settings=None`
  unconditionally for every surface) deliberately closed for the other
  three surfaces already, making worker-repair a policy exception rather
  than a fix.

- *2026-08-28*: **FW-122, FW-123, FW-124 added** at `U-hostmode` Phase 1's build (this session). FW-122: canon hosts are git-optional (`mode: git | plain`), a ledger-side compile record is plain mode's integrity instrument, user scope becomes a first-class plain host (BUILD, landed Phase 1 + Phase 2 — Phase 2's `chezmoi.py` deletion is a separate build). FW-123: the lock-invariant walker's `nullcontext()`-shaped blind spot, mitigated by a real per-host lock in both modes, never closed by the walker itself (WATCH, two named re-open triggers). FW-124: `09-surface-spec.md` §11 Y-17's committability sentence is stale on its recompile/drift-detection clause — a docs-only fix, not scheduled, belongs with whoever next touches Y-17 (BUILD, not scheduled). See `03-decisions.md` `S-51` for the full decision text these three rows summarize.

- *2026-08-28*: **`U-hostmode` Phase 2 built.** `chezmoi.py` is deleted
  wholesale (every CLI importer, verb, and except-tuple entry it held),
  and the UI adopt surface (`_extract_adopt_path`, `_adopt_offer_response`,
  the `/record/{id}/adopt-offer/dismiss` route, `partials/adopt_offer.html`,
  `action_bar.html`'s `kind == "adopt"` branch, `models.py`'s `adopted`
  parameter) is deleted with it, by census. `COMMIT_DRIFT_MARKERS` narrows
  to the one git marker. FW-122's disposition cell now reads "Landed by
  `U-hostmode` (Phase 1 + Phase 2)". See `03-decisions.md` `S-51` (the
  Phase-2 parentheticals are removed now that both phases exist under
  this unit) and this spec's own build report for the census accounting,
  the criteria→test map, and the mutation table.

- *2026-08-28*: **FW-142, FW-143 added by `U-land`**
  (`docs/specs/self-learn/drafts/u-land-landing-runner-spec.md`, T2, six blind
  spec gates r1-r6, SOUND at r6 with 0 blockers and 0 majors; `S-56`
  reserved). The unit makes landing a program —
  `plugins/self-learn/cli/scripts/land` — after three landing-chain defects
  in one day, each fixed forward and so each still in a public history: a
  newline-chained script that pushed an unfolded spec carrying 6 absolute
  home paths, an armor pin re-derived after the merge commit, and a
  `grep -c` exiting 1 on a zero count that broke an `&&` chain between the
  commit and the push. FW-142: the docs-only lane's derived test set (8 of
  144 modules, strict `src/` count 0) cannot see a docs path built from
  separate string constants — one module already does this, and the
  fallback (delete the lane) is stated. FW-143: the four pre-runner
  scripts (268 lines, git-excluded) are the only record of the chain the
  runner replaces, and are retired once `land` has completed one real
  landing. The build is the next step; no runner code exists under
  `scripts/` from this unit yet.
- *2026-08-28*: **U-xdist** moved the CLI suite's sanctioned runner
  (`plugins/self-learn/cli/scripts/suite`) from three parallel serial
  batches (A/B/C, 275-276s measured from master's own runner, 2 runs;
  batch C alone 274.8s, gated by the slowest batch on a 28-core host) to
  one `pytest-xdist` `-n auto` run over the whole tree (85.72s/86.00s/
  86.61s measured, 3 runs) -- a 3.20x-3.21x speedup on adjacent pairs,
  measured interleaved with the old runner under the same live load
  (gate r1, 2026-08-29), fixing at the root the two things that made a
  shared worker pool unsafe or blind: `pytest-xdist` is now a real dev
  dependency (`uv add
  --dev pytest-xdist`, 3.8.0, survives the script's own `uv sync` instead
  of being silently wiped by it); and `test_u_corrob.py`'s M2 mutation
  test no longer writes the mutated source into the LIVE, shared
  `src/self_learn/corroborate.py` — it builds the mutation into a private
  `tmp_path` copy of the whole package and shadows it onto a fresh
  interpreter via `PYTHONPATH`, removing a real race (~20% per-run in the
  scout's sampling) where a sibling worker's own fresh-interpreter
  subprocess test could read the file mid-mutation. The litter guard's
  "concurrent sibling" warning, previously silent under `-n` (the
  controller executes zero tests, so its own tracking list was always
  empty), now travels worker → controller via xdist's own
  `config.workeroutput`/`pytest_testnodedown` channel, deduplicated so
  one real sibling namespace reports once even when two workers both
  observe it. `SUITE_WORKERS`/`SUITE_SERIAL` env overrides exist for
  bisection only, never routine use. Under `test_armor.py`'s node-census
  armor (landed by `U-armor` mid-branch), the two nodes this unit touches
  carry their own dated record: `conftest.py`'s `Fixture.repinned` entry
  re-pins the file's byte sha for the litter-guard relay appended at its
  end, and `test_invocation_sdk.py`'s `Behaviour.edited` entry exempts
  RS8's lockfile-drift-bound node, widened to allow exactly the
  sanctioned `pytest-xdist`/`execnet` addition.

## 7. Change control

Same discipline as 08 §9: items get dated disposition notes here when
they fire, die, or graduate into a spec; a new foreseeable-work class
gets a new FW number and a home in the right theme doc; anything that
would alter a settled decision's inputs goes through the 03 register
first. This file is re-audited whenever the README status header
advances a phase.
