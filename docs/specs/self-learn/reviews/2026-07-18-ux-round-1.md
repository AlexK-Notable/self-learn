# Review record — UX round 1: survey + U16/U17/U18 (2026-07-18, agent-built)

Origin: the user's UX-enhancement mandate ("what should be automatic,
what should preload, what extra information belongs at routing, should
the miner write a brief") → Opus survey
(`research/2026-07-18-ux-enhancement-survey.md`) → the user picked the
top-5 shortlist for build. First round under the model-split directive:
**Opus authored specs and reviewed everything; Sonnet built everything;
no subagent ran Fable.** Every verdict below is a blind reviewer's;
reviewers never saw `reviews/*`.

## U16 — queue-walk trio (Y-19: prefetch, worker Force-run, auto-focus)

Dated amendments rode the build (round-2 Fix A precedent). Blind code
gate: **CLEAN first pass** — 3 mutations, each killed by its claiming
test (serve-stale, per-record-invalidation, worker argv). The reviewer
independently confirmed the never-stale hunt clean: the watcher's
`events.jsonl` catch-all covers even a bucket-creating rehome, and the
prefetch invalidates on exactly the SSE publish signals, so the cache
can never be staler than an ordinary render. The mid-build cross-track
constraint (global-on-any-verb-completion invalidation, required by
Y-20's budget datum) was already satisfied by construction —
single-slot cache on one generation counter — and got its own
cross-record test.

## U17 — loaded-surface budget indicator (Y-20)

Spec gate: **NOT SOUND** first pass — F1 BLOCKER: `surface_fill` keyed
`reference`, which is unimplementable (cap-free compiler, target=None,
no compile-set branch) and semantically inverted (reference IS the
overflow sink the cap graduates into). Folded: reference excluded, a
template-static no-cap line displays instead; the armed-bar budget
obligation dropped (no datum path through the cycle round-trip — the
Why region is the single budget surface); the field gated behind
`list --json --surface-fill` (+ `--id` per delta F9) so only the
Detail paint pays; any-VerbError → key omitted. Delta: **SOUND**.

Code gate: **NOT CLEAN** — F1 MAJOR: the "lands near the cap" clause
rendered unconditionally, so an EMPTY surface (0/10) claimed to be
near the cap — objectively false and inverting the feature's purpose
(the emptiest surface is the best target), with the wrong sentence
enshrined by a test; F2 MAJOR (demonstrated by live repro): a
corrupted managed-section marker file raised uncaught CompileError
through `list --json`, blanking the ENTIRE proposal region for every
record sharing that target. Folded: nearness gated on actual
proximity (entries within 2 of cap / words ≥80% / over-cap) with
parametrized low-fill tests; the probe body wrapped to degrade
per-key on VerbError/CompileError/OSError/UnicodeDecodeError with the
repro as regression tests; plus a spy test making the
reference-exclusion mutation killable. Delta: **CLEAN** (reviewer
re-ran the falseness probe and the corrupted-marker repro against the
folded code; 3+3 mutations total across the two passes).

## U18 — miner episode briefs (Y-21)

Spec gate: **SOUND** with pre-build folds — the two that mattered:
F1 MAJOR pinned the brief's compose site as `_build_record` BEFORE
`_scan_candidate` (a builder composing it in the landing writer would
have shipped an unscanned attacker-influenceable prose field), and
F2 MAJOR struck an undefined obligation (regex-gating refs makes no
sense for free prose — the whole-record secret scan + char cap are
the complete defenses). Delta: SOUND with one wording residual (F8,
"permitted" vs producer-convention) folded at merge with the
reviewer's exact wording.

Code gate: **CLEAN first pass** — 3 mutations, each killed: relocating
the compose after the scan failed the planted-secret-only-in-brief
test (the security invariant is genuinely test-pinned); making a
compiler read the brief failed the per-path no-leak tests; leaving the
brief in the finding body failed the byte-identical split test. The
reviewer verified compiler exclusion by construction across every
`.body` consumer in the CLI (all hash-or-named-section reads).

## Merge + verification + trials

Merged in sequence (U16 → U18 → U17) with hand-resolved register/row
ordering (Y-19/Y-20/Y-21, U16/U17/U18) and one semantic merge
obligation executed by the orchestrator: U17's `--surface-fill --id`
request landed INSIDE U16's cached detail bundle, so the budget datum
inherits the generation gate's global invalidation — closing the
staleness loop the two specs pinned toward each other. Combined
master: **CLI 970 passed / 3 skipped, UI 722 passed; pyright ui src
0, cli src 56 pre-existing baseline.** Sandbox live trials all PASS
(log: `fixtures/ui-trials.md` "UX-round DoD trials"), including the
F1 0/10 bare-fact rendering observed live and the `b`-key brief
disclosure; the miner's live-run leg rides the next real miner cycle.

**UX round 1 gate CLOSED 2026-07-18: U16 + U17 + U18 shipped.**
