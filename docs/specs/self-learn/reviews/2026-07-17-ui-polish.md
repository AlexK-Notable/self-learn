# Review record — ui-polish visual pass (2026-07-17)

Scope: `git diff master...ui-polish` (330dbca feat + c79c3e6 fold), the
G-3 surface's first dedicated visual/UX pass. Blind adversarial review by
an independent Opus agent (no access to this directory), then a scoped
delta re-check by the same reviewer.

## Pass 1 — adversarial (VERDICT: NOT CLEAN)

1. **MAJOR** — the new sticky `header` occluded keyboard-selected row
   titles on upward `k` navigation: `moveSelection()`'s
   `scrollIntoView(block:"nearest")` aligned the row's top to viewport
   y=0, under the opaque ~2.4rem strip; no `scroll-margin-top`
   compensated. Introduced by this very commit (master had no sticky
   header).
2. **MINOR** — `_theme_defs()`'s line filter dropped the *whole*
   `.highlight { … }` theme rule; for github-dark that rule also carries
   the default foreground (`color: #E6EDF3`), so the dark theme's
   untokenized-text color silently fell through to the body color
   (legible today, latent trap if they diverge).

Verified clean in the same pass: pygments file==generator byte-for-byte;
Y-10 deuteranope audit (every hue pair text- or glyph-disambiguated,
including replay allow/deny and the live-block accent border); W-9 CSP
(no inline style/script anywhere new); template additions introduce no
htmx/keymap/aria contract changes; `.detail-right` sticky offset clears
the header; `:has()` and overflow containment correct for the pinned
chromium-only client.

## Fold (c79c3e6)

- `[data-row] { scroll-margin-top: 3.4rem; }` — proven live in Playwright
  (420px viewport, k-walk upward: selected row top 54px vs header bottom
  40px, occluded=false).
- `_theme_defs` drops only `background*` declarations from the theme's
  own `.highlight` rule, keeps the rest; `pygments.css` regenerated.

## Pass 2 — delta re-check (DELTA VERDICT: CLEAN)

Both findings RESOLVED with proof; the delta is exactly the two fixes
(three files, nothing else); suite 481 green; pinned
generator==served-file test holds.

## Empirical checks run during the pass (orchestrator)

- Full-page screenshots of Front / Bucket (registered, unregistered-host,
  bulk-canon, cluster) / Detail (fresh card, hook script + replay rows,
  armed state) / Report / help overlay, dark AND emulated-dark→light.
- Dark-mode Change preview defect in shipped v1 (light-only pygments →
  light-gray-on-white + page-wide line overflow) confirmed pre-fix and
  gone post-fix.
- No horizontal document overflow at 800px and 700px viewports.

Gallery: `~/Pictures/self-learn-ui/` (baseline-* = shipped v1,
v2-*/v3-*/v4-* = this pass).
