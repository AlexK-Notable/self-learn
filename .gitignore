.playwright-mcp/
.claude/worktrees/

# CodeBoarding: keep the diagram/report artifacts, drop regenerable local cache
.codeboarding/cache/
.codeboarding/logs/
.codeboarding/*.lock
.codeboarding/static_analysis.pkl
.codeboarding/static_analysis.sha

# per-machine Claude Code instructions (the committed CLAUDE.md is the project's)
CLAUDE.local.md

# self-learn canon host (plain mode, registered 2026-09-12): everything the compiler
# writes into this checkout stays out of the public repo. CLAUDE.local.md is above.
/.self-learn-host
/references/
/.claude/rules/
