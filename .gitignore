.playwright-mcp/
.claude/worktrees/

# CodeBoarding: keep the diagram/report artifacts, drop regenerable local cache
.codeboarding/cache/
.codeboarding/logs/
.codeboarding/*.lock
.codeboarding/static_analysis.pkl
.codeboarding/static_analysis.sha

# per-machine Claude Code instructions (the committed CLAUDE.md is the project's)
CLAUDE.local.md
